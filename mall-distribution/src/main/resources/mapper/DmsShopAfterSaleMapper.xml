<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.distribution.dao.DmsShopAfterSaleDao">

    <resultMap id="BaseResultMap" type="com.macro.mall.distribution.entity.DmsShopAfterSale">
        <id column="id" property="id"/>
        <result column="after_sale_no" property="afterSaleNo"/>
        <result column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="member_id" property="memberId"/>
        <result column="user_id" property="userId"/>
        <result column="apply_type" property="applyType"/>
        <result column="refund_amount" property="refundAmount"/>
        <result column="product_refund_amount" property="productRefundAmount"/>
        <result column="freight_refund_amount" property="freightRefundAmount"/>
        <result column="refund_quantity" property="refundQuantity"/>
        <result column="reason" property="reason"/>
        <result column="proof_images" property="proofImages"/>
        <result column="return_address_id" property="returnAddressId"/>
        <result column="return_address" property="returnAddress"/>
        <result column="return_delivery_company" property="returnDeliveryCompany"/>
        <result column="return_delivery_no" property="returnDeliveryNo"/>
        <result column="return_shipped_at" property="returnShippedAt"/>
        <result column="return_received_at" property="returnReceivedAt"/>
        <result column="status" property="status"/>
        <result column="audit_remark" property="auditRemark"/>
        <result column="audit_user_id" property="auditUserId"/>
        <result column="audit_user_name" property="auditUserName"/>
        <result column="audit_time" property="auditTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, after_sale_no, order_id, order_no, member_id, user_id, apply_type, refund_amount,
        product_refund_amount, freight_refund_amount, refund_quantity,
        reason, proof_images, return_address_id, return_address, return_delivery_company, return_delivery_no,
        return_shipped_at, return_received_at, status, audit_remark, audit_user_id, audit_user_name,
        audit_time, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_shop_after_sale
        WHERE id = #{id}
    </select>

    <select id="selectByIdForUpdate" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_shop_after_sale
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <select id="selectOpenByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_shop_after_sale
        WHERE order_id = #{orderId} AND status IN (0, 4, 5, 6)
        ORDER BY id DESC
        LIMIT 1
    </select>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_shop_after_sale
        WHERE order_id = #{orderId}
        ORDER BY id DESC
    </select>

    <select id="selectByMemberId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_shop_after_sale
        WHERE member_id = #{memberId}
        ORDER BY id DESC
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_shop_after_sale
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    after_sale_no LIKE CONCAT('%', #{keyword}, '%')
                    OR order_no LIKE CONCAT('%', #{keyword}, '%')
                    OR reason LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <insert id="insert" parameterType="com.macro.mall.distribution.entity.DmsShopAfterSale" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dms_shop_after_sale
        (after_sale_no, order_id, order_no, member_id, user_id, apply_type, refund_amount,
         product_refund_amount, freight_refund_amount, refund_quantity,
         reason, proof_images, return_address_id, return_address, return_delivery_company, return_delivery_no,
         return_shipped_at, return_received_at, status)
        VALUES
        (#{afterSaleNo}, #{orderId}, #{orderNo}, #{memberId}, #{userId}, #{applyType}, #{refundAmount},
         #{productRefundAmount}, #{freightRefundAmount}, #{refundQuantity},
         #{reason}, #{proofImages}, #{returnAddressId}, #{returnAddress}, #{returnDeliveryCompany}, #{returnDeliveryNo},
         #{returnShippedAt}, #{returnReceivedAt}, #{status})
    </insert>

    <update id="updateAudit" parameterType="com.macro.mall.distribution.entity.DmsShopAfterSale">
        UPDATE dms_shop_after_sale
        SET status = #{status},
            audit_remark = #{auditRemark},
            audit_user_id = #{auditUserId},
            audit_user_name = #{auditUserName},
            audit_time = CURRENT_TIMESTAMP,
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="updateReturnShipment" parameterType="com.macro.mall.distribution.entity.DmsShopAfterSale">
        UPDATE dms_shop_after_sale
        SET status = #{status}, return_delivery_company = #{returnDeliveryCompany},
            return_delivery_no = #{returnDeliveryNo}, return_shipped_at = #{returnShippedAt},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="updateReturnReceived" parameterType="com.macro.mall.distribution.entity.DmsShopAfterSale">
        UPDATE dms_shop_after_sale
        SET status = #{status}, return_received_at = #{returnReceivedAt},
            audit_remark = #{auditRemark}, audit_user_id = #{auditUserId}, audit_user_name = #{auditUserName},
            audit_time = CURRENT_TIMESTAMP, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="markRefundCompleted">
        UPDATE dms_shop_after_sale
        SET status = 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id} AND status = 6
    </update>
</mapper>
