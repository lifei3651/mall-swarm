<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.distribution.dao.DmsImportDetailDao">

    <resultMap id="BaseResultMap" type="com.macro.mall.distribution.entity.DmsImportDetail">
        <id column="id" property="id"/>
        <result column="batch_id" property="batchId"/>
        <result column="batch_no" property="batchNo"/>
        <result column="row_num" property="rowNum"/>
        <result column="raw_data" property="rawData" typeHandler="com.macro.mall.distribution.security.EncryptedStringTypeHandler"/>
        <result column="status" property="status"/>
        <result column="error_msg" property="errorMsg"/>
        <result column="target_id" property="targetId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, batch_id, batch_no, row_num, raw_data, status, error_msg, target_id, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_import_detail
        WHERE id = #{id}
    </select>

    <select id="selectByBatchId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_import_detail
        WHERE batch_id = #{batchId}
        ORDER BY row_num ASC
    </select>

    <select id="selectByBatchIdAndStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_import_detail
        WHERE batch_id = #{batchId} AND status = #{status}
        ORDER BY row_num ASC
    </select>

    <insert id="insert" parameterType="com.macro.mall.distribution.entity.DmsImportDetail" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dms_import_detail (batch_id, batch_no, row_num, raw_data, status)
        VALUES (#{batchId}, #{batchNo}, #{rowNum},
                #{rawData,typeHandler=com.macro.mall.distribution.security.EncryptedStringTypeHandler}, #{status})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO dms_import_detail (batch_id, batch_no, row_num, raw_data, status)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.batchId}, #{item.batchNo}, #{item.rowNum},
             #{item.rawData,typeHandler=com.macro.mall.distribution.security.EncryptedStringTypeHandler}, #{item.status})
        </foreach>
    </insert>

    <select id="selectSensitivePlaintextCandidates" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM dms_import_detail
        WHERE raw_data IS NOT NULL AND raw_data != '' AND raw_data NOT LIKE 'enc:v1:%'
        ORDER BY id
        LIMIT #{limit}
    </select>

    <update id="encryptRawData">
        UPDATE dms_import_detail
        SET raw_data=#{rawData,typeHandler=com.macro.mall.distribution.security.EncryptedStringTypeHandler}
        WHERE id=#{id} AND raw_data IS NOT NULL AND raw_data != '' AND raw_data NOT LIKE 'enc:v1:%'
    </update>

    <update id="update" parameterType="com.macro.mall.distribution.entity.DmsImportDetail">
        UPDATE dms_import_detail
        SET status = #{status},
            error_msg = #{errorMsg},
            target_id = #{targetId}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE dms_import_detail
        SET status = #{status},
            error_msg = #{errorMsg},
            target_id = #{targetId}
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        DELETE FROM dms_import_detail WHERE id = #{id}
    </update>

    <update id="deleteByBatchId">
        DELETE FROM dms_import_detail WHERE batch_id = #{batchId}
    </update>

</mapper>
