<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.distribution.dao.DmsAdminUserDao">

    <resultMap id="BaseResultMap" type="com.macro.mall.distribution.entity.DmsAdminUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="salt" property="salt"/>
        <result column="nickname" property="nickname"/>
        <result column="role_code" property="roleCode"/>
        <result column="permissions" property="permissions"/>
        <result column="merchant_id" property="merchantId"/>
        <result column="merchant_name" property="merchantName"/>
        <result column="status" property="status"/>
        <result column="failed_login_count" property="failedLoginCount"/><result column="lock_time" property="lockTime"/>
        <result column="must_change_password" property="mustChangePassword"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumnList">
        u.id, u.username, u.password_hash, u.salt, u.nickname, u.role_code, u.permissions, u.merchant_id,
        m.merchant_name, u.status,
        u.last_login_time, u.failed_login_count, u.lock_time, u.must_change_password, u.create_time, u.update_time
    </sql>

    <select id="list" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM dms_admin_user u LEFT JOIN dms_merchant m ON m.id=u.merchant_id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (u.username LIKE CONCAT('%', #{keyword}, '%') OR u.nickname LIKE CONCAT('%', #{keyword}, '%') OR m.merchant_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND u.status = #{status}
            </if>
        </where>
        ORDER BY u.id DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM dms_admin_user u LEFT JOIN dms_merchant m ON m.id=u.merchant_id
        WHERE u.id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM dms_admin_user u LEFT JOIN dms_merchant m ON m.id=u.merchant_id
        WHERE u.username = #{username}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dms_admin_user (username, password_hash, salt, nickname, role_code, permissions, merchant_id, status, must_change_password)
        VALUES (#{username}, #{passwordHash}, #{salt}, #{nickname}, #{roleCode}, #{permissions}, #{merchantId}, #{status}, #{mustChangePassword})
    </insert>

    <update id="update">
        UPDATE dms_admin_user
        SET nickname = #{nickname},
            role_code = #{roleCode},
            permissions = #{permissions},
            merchant_id = #{merchantId},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE dms_admin_user
        SET password_hash = #{passwordHash},
            salt = #{salt},
            must_change_password = #{mustChangePassword},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE dms_admin_user
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateLastLoginTime">
        UPDATE dms_admin_user
        SET last_login_time = NOW(), failed_login_count = 0, lock_time = NULL, update_time = NOW()
        WHERE id = #{id}
    </update>
    <update id="increaseFailedLogin">UPDATE dms_admin_user SET failed_login_count=failed_login_count+1, lock_time=CASE WHEN failed_login_count+1 &gt;= #{lockThreshold} THEN NOW() ELSE lock_time END, update_time=NOW() WHERE id=#{id}</update>
    <update id="clearLoginLock">UPDATE dms_admin_user SET failed_login_count=0, lock_time=NULL, update_time=NOW() WHERE id=#{id}</update>
</mapper>
