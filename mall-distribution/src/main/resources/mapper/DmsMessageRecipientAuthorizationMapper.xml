<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.distribution.dao.DmsMessageRecipientAuthorizationDao">
    <sql id="AuthorizationColumns">id,tenant_id,member_id,channel,endpoint_hash,authorized,authorized_time,expires_at,revoked_time,consent_version,consent_surface,create_time,update_time</sql>
    <select id="selectByMemberChannel" resultType="com.macro.mall.distribution.entity.DmsMessageRecipientAuthorization">
        SELECT <include refid="AuthorizationColumns"/>
        FROM dms_message_recipient_authorization
        WHERE tenant_id=#{tenantId} AND member_id=#{memberId} AND channel=#{channel} LIMIT 1
    </select>
    <select id="selectActive" resultType="com.macro.mall.distribution.entity.DmsMessageRecipientAuthorization">
        SELECT <include refid="AuthorizationColumns"/>
        FROM dms_message_recipient_authorization
        WHERE tenant_id=#{tenantId} AND member_id=#{memberId} AND channel=#{channel} AND authorized=1
          AND revoked_time IS NULL AND (expires_at IS NULL OR expires_at &gt; #{now}) LIMIT 1
    </select>
    <select id="countActiveConsent" resultType="int">
        SELECT COUNT(*) FROM dms_message_recipient_authorization
        WHERE tenant_id=#{tenantId} AND channel=#{channel} AND authorized=1
          AND consent_version=#{consentVersion} AND revoked_time IS NULL
          AND (expires_at IS NULL OR expires_at &gt; #{now})
    </select>
    <insert id="insert" parameterType="com.macro.mall.distribution.entity.DmsMessageRecipientAuthorization" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dms_message_recipient_authorization
          (tenant_id,member_id,channel,endpoint_hash,authorized,authorized_time,expires_at,revoked_time,consent_version,consent_surface)
        VALUES
          (#{tenantId},#{memberId},#{channel},#{endpointHash},#{authorized},#{authorizedTime},#{expiresAt},#{revokedTime},#{consentVersion},#{consentSurface})
    </insert>
    <update id="updatePreference" parameterType="com.macro.mall.distribution.entity.DmsMessageRecipientAuthorization">
        UPDATE dms_message_recipient_authorization
        SET endpoint_hash=#{endpointHash},authorized=#{authorized},authorized_time=#{authorizedTime},expires_at=#{expiresAt},
            revoked_time=#{revokedTime},consent_version=#{consentVersion},consent_surface=#{consentSurface},update_time=CURRENT_TIMESTAMP
        WHERE tenant_id=#{tenantId} AND member_id=#{memberId} AND channel=#{channel}
    </update>
</mapper>
