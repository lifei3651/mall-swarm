<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.distribution.dao.DmsShopCategoryDao">

    <resultMap id="BaseResultMap" type="com.macro.mall.distribution.entity.DmsShopCategory">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="category_name" property="categoryName"/>
        <result column="icon_url" property="iconUrl"/>
        <result column="sort_order" property="sort"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumnList">
        id, tenant_id, category_name, icon_url, sort_order, status, remark, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM dms_shop_category
        WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM dms_shop_category
        <where>
            <if test="tenantId != null">
                AND tenant_id = #{tenantId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort_order DESC, id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dms_shop_category (tenant_id, category_name, icon_url, sort_order, status, remark)
        VALUES (#{tenantId}, #{categoryName}, #{iconUrl}, #{sort}, #{status}, #{remark})
    </insert>

    <update id="update">
        UPDATE dms_shop_category
        SET tenant_id = #{tenantId},
            category_name = #{categoryName},
            icon_url = #{iconUrl},
            sort_order = #{sort},
            status = #{status},
            remark = #{remark},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE dms_shop_category
        SET status = #{status}, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
</mapper>
