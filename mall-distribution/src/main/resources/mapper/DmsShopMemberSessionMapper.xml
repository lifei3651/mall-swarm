<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.distribution.dao.DmsShopMemberSessionDao">

    <resultMap id="BaseResultMap" type="com.macro.mall.distribution.entity.DmsShopMemberSession">
        <id column="id" property="id"/>
        <result column="member_id" property="memberId"/>
        <result column="user_id" property="userId"/>
        <result column="token" property="token"/>
        <result column="status" property="status"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByToken" resultMap="BaseResultMap">
        SELECT id, member_id, user_id, token, status, expire_time, create_time, update_time
        FROM dms_shop_member_session
        WHERE token = #{token}
    </select>

    <insert id="insert" parameterType="com.macro.mall.distribution.entity.DmsShopMemberSession" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dms_shop_member_session (member_id, user_id, token, status, expire_time)
        VALUES (#{memberId}, #{userId}, #{token}, #{status}, #{expireTime})
    </insert>

    <update id="disableByToken">
        UPDATE dms_shop_member_session
        SET status = 0, update_time = CURRENT_TIMESTAMP
        WHERE token = #{token}
    </update>

    <update id="disableByMemberId">
        UPDATE dms_shop_member_session
        SET status = 0, update_time = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
    </update>
</mapper>
