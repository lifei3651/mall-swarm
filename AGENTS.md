# 仓库约定（Codex 自动执行规则）

## 开始修改前先与远程同步

本仓库同时在“公司 Mac”和“家里 Mac（Codex 运行环境）”两边使用，必须通过远程仓库保持双向同步：

- 每次在本会话中开始修改代码之前，先执行 `git pull`（必要时使用 `git pull --rebase`），把远程最新代码同步到本地。
- 如果本地存在尚未提交的改动，先按下方“代码修改后自动提交推送”规则提交并推送，再执行拉取。
- 如果拉取出现冲突：停止修改，保留双方改动，立即向用户说明冲突的文件，由用户决定如何合并；禁止 force push，禁止丢弃或覆盖对方的提交。

## 每日双机接力

- 每天 18:00 之后由“家里 Mac”负责修改和发布；第二天白天由“当前 Mac”接手。
- 当前 Mac 白天开始工作前，除同步远程仓库外，必须先阅读 `document/RELEASE_REPORT.md`，再核对本地 `VERSION`、最新 Git 提交和线上实际版本。
- `document/RELEASE_REPORT.md` 是两台电脑之间的版本交接依据；发现线上或报告存在更高版本时，禁止用较低版本覆盖、重复发布或回退。
- 18:00 之后当前 Mac 不再开始新的修改或发布，除非用户在当前对话中明确要求继续。

## 代码修改后自动提交推送

本仓库的 Codex 会话必须遵守以下规则：

- 每次完成代码修改（用户要求修改代码之后）必须自动执行：
  1. `git add .`
  2. `git commit -m "AI更新代码"`
  3. `git push`（推送到当前分支对应的远程仓库）
- 用户已明确授权此自动提交与推送行为，执行前无需再次征求同意。
- 如果 `git push` 失败（例如远端网络或认证问题），保留本地提交并向用户说明原因，不要擅自回退、改写或删除提交记录。

## 每日修改日志

- 每次完成代码修改并提交推送前，检查 `document/DAILY_LOG.md` 是否已有当天条目：
  - 没有：在文件顶部新增当天条目，简要记录日期、修改方、改动内容、测试结果、是否发布。
  - 已有：按需补充当天新增的改动，不重复整段重写。
- 公司 Mac 上的修改由用户在日志中自行补充；未补充时以 git 提交历史为准。
