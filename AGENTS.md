# 仓库约定（Codex 自动执行规则）

## 仓库位置与远程地址

- 唯一开发与发布主机：当前 Mac，唯一产品基座位于 `/Users/minmatemp/Documents/mall-swarm-app-h5`
- 远程仓库：`git@github.com:lifei3651/mall-swarm.git`
- 所有命令都必须先通过 `git rev-parse --show-toplevel` 确认当前仓库根目录，不依赖固定电脑路径。

## 唯一产品基座

- 自 2026-08-22 起，只维护 App / 小程序公开商城 + 团队 H5 拆分版，以及同一源码的可选一体化 H5 构建。
- `/Users/minmatemp/Documents/mall-swarm` 原一体版只保留为历史备份，禁止继续开发、修复、合并或发布。
- 任何候选修复必须先确认仓库根目录为 `mall-swarm-app-h5`；发现改动位于旧一体版时停止，不得将旧版构建覆盖线上。

## 开始修改前同步远程备份

自 2026-08-13 起只使用当前 Mac 开发和发布，远程仓库作为备份与协作来源，不再等待或假设家里电脑存在未同步改动：

- 每次开始修改前，先检查工作区和远程；远程可用时执行 `git pull`（必要时使用 `git pull --rebase`）。
- 如果本地存在尚未提交的改动，先按下方“代码修改后自动提交推送”规则提交并推送，再执行拉取。
- 如果远程网络暂时不可用，确认当前工作区状态和本地提交后可以继续在唯一主机开发；完成后仍需尝试推送，并在日志与最终答复中记录待推送提交数和错误原因。
- 如果拉取出现冲突：停止修改，保留双方改动，立即向用户说明冲突的文件，由用户决定如何合并；禁止 force push，禁止丢弃或覆盖对方的提交。

## 每日单机接力

- 每次开始工作前必须先阅读 `document/RELEASE_REPORT.md`，再核对本地 `VERSION`、最新 Git 提交和线上实际版本。
- `document/RELEASE_REPORT.md` 是当前主机跨会话的版本交接依据；发现线上或报告存在更高版本时，禁止用较低版本覆盖、重复发布或回退。
- 不再执行公司 Mac / 家里 Mac 的 18:00 交接限制；发布仍必须以用户当前指令、版本报告、线上只读核验和备份验收为准。

## 代码修改后自动提交推送

本仓库的 Codex 会话必须遵守以下规则：

- 每次完成代码修改并通过必要验证后、发送最终答复前，必须自动执行：
  1. `git add .`
  2. `git commit -m "AI更新代码"`
  3. `git push`（推送到当前分支对应的远程仓库）
- 用户已明确授权此自动提交与推送行为，执行前无需再次征求同意。
- 如果暂存后没有改动，跳过 `git commit` 和 `git push`，直接说明没有需要备份的新内容。
- 提交前检查 `git status`，不得把密码、密钥、验证码、私有配置、`.env` 或其他敏感文件加入提交。
- 如果 `git push` 因网络或 SSH 认证失败：保留本地提交，说明本地领先的提交数量和错误原因，不得回退、改写或删除提交记录。
- 如果 `git push` 因远端已有新提交而被拒绝：禁止 force push；先停止推送并向用户说明，需要在保留双方改动的前提下执行 `git pull --rebase` 或人工处理冲突。
- 如果出现合并、变基或子模块冲突：立即停止，列出冲突文件并等待用户决定，禁止自动选择一方覆盖另一方。
- 最终答复必须明确说明提交哈希、推送是否成功、当前分支以及是否仍有未提交内容。

## 每日修改日志

- 每次完成代码修改并提交推送前，检查 `document/DAILY_LOG.md` 是否已有当天条目：
  - 没有：在文件顶部新增当天条目，简要记录日期、修改方、改动内容、测试结果、是否发布。
  - 已有：按需补充当天新增的改动，不重复整段重写。
- 当前主机上的用户手工修改可由用户补充日志；未补充时以 Git 提交历史为准。
