# 灵启商城 1.0.94 正式发布记录

发布日期：2026-08-28（Asia/Shanghai）  
当前状态：已发布，未触发回滚

## 发布身份

- 版本：`1.0.94`
- 版本形态：App / 小程序公开商城 + 团队 H5 拆分版
- 冻结业务 Git：`cc420456aa64a4ce173074904bd85a801cc6ae29`
- 构建号：`20260828-1012-1.0.94`
- 后端 JAR SHA-256：`c60a2261b96e0854696e16ce87e3fd427e2b9bb6346059e978715f20bc45eef3`
- 正式候选包：`lingqimall-v1.0.94-20260828-1012.tar.gz`
- 候选包大小：`145222216` 字节
- 候选包 SHA-256：`98fdd895e1d1a5437a532a399b51baf4ee017a966d17256f9ad02678e02d529f`
- 发布脚本 SHA-256：`a400e82fd45f5dc2fccc24c84eb2ef5ec80b096823f175a4535174f2453fd1af`

## 本版内容

- 正常订单的奖金计算强制使用支付时关系快照中冻结的唯一客户制度版本；同一订单出现多个版本或冻结版本不存在时阻止不确定资金计算，无关系快照的历史兼容路径保持原回退行为。
- 异步奖金补算使用任务所属客户编号，不再回落默认客户，保护未来客户独立项目的制度隔离。
- 商城基座没有新增固定奖金比例、等级、复购或报单规则，也没有增加公开前台奖金说明。本版无数据库迁移、环境变量、前端页面或客户配置变更。

## 构建与候选门禁

- 公共后端：`14/14`
- 分销后端：`517/517`
- 后端合计：`531/531`
- 商城前端：`93/93`
- 管理后台：`139/139`
- 后端正式打包、管理后台、公开商城、团队 H5、一体化 H5 生产构建及三端包边界全部通过。
- 候选包含管理后台 `209` 项、公开商城 `107` 项、团队 H5 `51` 项、一体化 H5 `128` 项生产文件；一体化 H5 只构建和验签，不部署到生产。
- 候选内部 `40/40` 项 SHA-256、外层及四个内包的路径、扩展属性、macOS 隐藏文件、source map、敏感文件、权限和远程只读预检全部通过。

## 数据库与生产边界

- 本版没有新增迁移，生产迁移总账必须保持 `27/27`、失败 `0`；发布脚本仍会逐项按原校验和安全核验和跳过。
- 发布只允许从线上 `1.0.93` / `132f2defa59e8e6051f24fec0d23752498ef6833` / `20260828-0835-1.0.93` 升级。
- 发布前后必须保持现有客户启用的 `NEW_RETAIL_SIMPLE_DEFAULT` 奖金程序、核心计数、完整装修配置、主题、品牌文化轮播、字段加密配置、外部通知与实名认证门禁不变。
- 未写客户数据制造验收样本，不开启真实外部通知、实名认证或一体化 H5，不更新 XMind。

## 备份与发布结果

- 独立发布前备份：`/opt/lingqimall/backups/full/20260828_102121`，SHA-256、数据库 gzip 和文件配置包通过，并在隔离数据库真实恢复；迁移 `27/27`、失败 `0`，核心计数、客户奖金程序、短信授权字段和完整装修哈希一致，临时库已删除。
- 受控脚本发布前备份：`/opt/lingqimall/backups/full/20260828_102308`。
- 受控脚本发布后备份：`/opt/lingqimall/backups/full/20260828_102343`。
- 三份备份均包含数据库、应用、三端静态文件、Nginx、systemd、字段加密配置及 `/etc/lingqimall` 外部服务配置，并通过 SHA-256、数据库 gzip 与文件配置包复验。
- 27项迁移全部按原校验和安全跳过，没有新 DDL；受控脚本返回 `release-success`，未触发回滚。

## 线上验收

- 公开商城、团队 H5、管理后台和后端统一为 `1.0.94` / `cc420456aa64a4ce173074904bd85a801cc6ae29` / `20260828-1012-1.0.94`。
- 生产 JAR SHA-256 与候选一致；管理后台 `209` 项、公开商城 `107` 项、团队 H5 `51` 项文件的逐文件集合 SHA-256 与本地候选一致。一体化 H5 未部署。
- Nginx、MySQL、Redis、商城后端全部 `active`，健康检查 `UP`，Redis `PONG`，发布后严重日志 `0`。
- 数据库保持 `27/27`、失败 `0`；核心计数保持 `45:2:18:27:58:13450.00:5:6:3`，现有客户启用奖金程序保持 `1:NEW_RETAIL_SIMPLE_DEFAULT`，完整装修配置哈希保持 `33bcd606...`。
- 外部通知授权、尝试、回执、启用预算和外部事件通道保持 `0:0:0:0:0`，实名认证记录和尝试保持 `0:0`，没有调用真实服务或产生费用。
- 首页、直播广场、新品速递、品牌文化及三端页面均为 `200`；个人消息与实名认证匿名访问继续为 `401`。服务器发布目录和上传包已清理，未写客户数据制造验收样本，未更新 XMind。
