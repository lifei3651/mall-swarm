# 灵启商城 1.0.83 正式候选发布准备

更新时间：2026-08-24（Asia/Shanghai）

状态：该候选未单独发布，其业务能力和第 23 项迁移已随 `1.0.84` 累积上线并验收。本文件以下内容保留候选冻结时的历史事实。

生产交接基线（历史）：候选冻结时线上为 `1.0.82`，数据库迁移交接记录为 22/22；当时未连接生产进行复核。

文件保留说明：原候选压缩包位于 Git 忽略目录。后续正式构建 `1.0.84` 时，旧构建脚本的根工程清理动作删除了该本地压缩包，精确文件当前无法恢复；业务冻结提交、源码、已跟踪发布脚本和本文档仍完整，线上从未运行该独立候选且不受影响。构建工具已修正为清理前保护、清理后恢复 `target/release-candidates`。

## 冻结身份

- 产品基座：`/Users/minmatemp/Documents/mall-swarm-app-h5`
- 分支：`codex/app-h5-split`
- 业务冻结提交：`41b0073a9318c49d875b727fe61bf31b0df287c9`
- 版本：`1.0.83`
- 构建号：`20260824-1540-1.0.83`
- 上一正式版本：`1.0.82`
- 迁移边界：生产交接基线 22/22；候选只新增第 23 项 `V202608242100__external_notification_delivery_kernel.sql`。
- 生产拓扑：公开商城、团队 H5、管理后台拆分部署；一体化 H5 仅作为同源可选产物封包和验签，不在拆分发布中启用。

业务产物只认上述冻结提交。候选说明、受控发布脚本和交接日志的后续提交不改变业务构建身份。

## 正式候选产物

候选目录：

`/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.83-20260824-1540`

| 组件 | 字节数 | SHA-256 | 版本 / Git / 构建号 |
| --- | ---: | --- | --- |
| 后端 `mall-distribution.jar` | 156,648,930 | `c3eda77ebd9dc4072456d0e3f6c2f15c1c06fac369860c3d635f5b87dc697320` | `1.0.83` / `41b0073a...` / `20260824-1540-1.0.83` |
| 管理后台 `admin.tar.gz` | 2,537,212 | `f7e2302abf0ce9d9f1fa755f8d6aa0f854d847a861c6ab390adba7389284ed70` | 同上 |
| 公开商城 `shop.tar.gz` | 857,670 | `36d525430e99694b9bf870c149684fccbe8a16d9b5075846ec5e2767ddf99739` | 同上 |
| 团队 H5 `team.tar.gz` | 573,279 | `174af309157b838be4d9d283061e1e14d7901608af8277d673c8a2763e83de20` | 同上 |
| 一体化 H5 `integrated.tar.gz`（可选、不启用） | 894,865 | `e072ae40f6c7e5f94531e6047cd5d1017cd45a9bde340c0703c4d49b565a041b` | 同上 |
| 受控脚本 `release.sh` | 25,996 | `3cc92475e9f6259a6786909c112b11b9b5d24a83cc8422672b45b3fe8d62fc60` | 双重显式授权门禁 |

完整传输包：

- 路径：`/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.83-20260824-1540.tar.gz`
- 字节数：144,280,325
- SHA-256：`43c58b0847fcf63fa21d30d35b671e7c9241b7268c5a4bc7f52b7a92ddd42daa`
- 内含五类业务产物、`VERSION`、`RELEASE_MANIFEST.json`、35 项 `SHA256SUMS`、23 项迁移总账、生产全量备份工具、迁移工具、拆分 Nginx 配置和 1.0.83 专用 `release.sh`。

候选产物位于 Git 忽略目录，不进入候选准备提交。正式传输时必须同时匹配路径、字节数、总包 SHA-256 和包内 35/35 哈希。

## 已完成门禁

- 准备开始时仓库根目录、分支、冻结提交和远程 0/0 一致，工作区干净；`VERSION`、发版报告和每日交接记录均为本地 1.0.83、线上 1.0.82。
- 冻结提交重新执行全量测试：公共后端 14/14、分销后端 462/462（合计 476/476）、商城 85/85、后台 123/123、私有部署结构校验 9/9 和完整安全流程全部通过，失败、错误和跳过均为 0。
- 后端执行干净生产打包；公开商城、团队 H5、一体化 H5、管理后台使用统一构建身份重新生产构建。公开商城扫描 66 个文件，一体化 H5 扫描 123 个文件，两项构建边界门禁通过。
- 四份前端 `version.json` 均为 `1.0.83`、`app-h5-split`、冻结 Git 和统一构建号，应用标识分别为 `storefront-public`、`team-h5`、`integrated-h5`、`admin`；后端 JAR 由 `RELEASE_MANIFEST.json` 以同一身份和精确 SHA-256 绑定。
- 完整包独立解压后再次通过 35/35 哈希、JAR ZIP、四个前端 tar.gz、23 项迁移计划及三份 Shell 工具语法检查；四端无 source map，候选文件名和文本扫描未发现 `.env`、私钥、证书密钥库或常见访问令牌特征。
- 包内 `release.sh` 无参数执行只返回 `waiting-for-explicit-release-authorization` 并以状态 3 结束。必须同时提供 `--authorize-release` 和版本绑定环境变量，默认不会读取或修改生产。
- 包内第 23 项在无端口、一次性 MySQL 8.4.11 中从 1.0.82 消息结构和既有任务数据连续执行两遍：最终 8 张消息/通知表、任务表 25 列、15 个模板、15 个渠道、19 条预算且启用与额度均为 0，授权/尝试/回执均为 0，既有四条任务的事件类型和四个幂等键完整，外部活跃任务为 0；临时容器已删除。

## 受控发布脚本门禁

发布脚本只有在后续独立任务获得用户明确发布授权和生产身份后才允许运行，且必须同时满足：

1. 包内版本、冻结 Git、构建号、JAR SHA-256、35 项内部哈希、23 项迁移计划和四端清单全部匹配。
2. 线上版本仍为 1.0.82；后端、Nginx、MySQL、Redis 均正常，Redis `PONG`、健康检查 `UP`。
3. 数据库迁移必须恰好 22/22、失败 0，第 23 项未登记；1.0.82 四张消息表、15 个模板、15 个渠道及全部外部渠道关闭状态必须正确。
4. 外部通知总开关、发送器、阿里云通知短信和两个模拟适配器的运行环境不得为开启；候选 JAR 内这些配置的默认值必须全部为关闭。
5. 核心业务计数、明文敏感字段、字段加密配置哈希和待处理奖金、ERP、移线、超时订单符合发布基线。

任一门禁不满足立即停止，不自动修数据、不跳过检查、不 force 执行。

## 全量备份与恢复性验收

正式发布必须重新生成当次全量备份，不能复用历史备份。备份对象包括数据库、上传目录、服务器现有配置、旧后端、公开商城/团队 H5/后台静态目录、systemd、Nginx、TLS、备份工具和字段加密配置。

发布脚本要求：

1. `SHA256SUMS`、数据库 gzip、文件包清单和关键对象全部通过。
2. 将发布前数据库备份完整导入固定名称的临时验证库，确认恢复后仍为 22/22、失败 0、1.0.82 四张消息表存在、第 23 项四张内核表不存在、外部渠道关闭。
3. 临时库验收成功后删除临时库；恢复演练或核心计数任一异常都停止发布。
4. 发布完成后再生成一份全量备份并复验，发布前后备份均保留。

## 第 23 项迁移与默认关闭验收

后端停止后运行完整迁移总账：前 22 项必须按原校验和跳过，只允许执行 `V202608242100__external_notification_delivery_kernel.sql`。执行后必须同时满足：

- 迁移 23/23、失败 0，第 23 项成功登记一次。
- 原四张消息表保持，新增尝试、预算、授权摘要、回执摘要四张表；发送任务表为 25 列。
- 预算恰好 19 条，全部未启用且日/月额度均为零。
- 授权、发送尝试和回执均为 0；短信、App 推送、小程序渠道全部关闭，无外部活跃任务。
- 新后端启动后运行环境的外部总开关、发送器、通知短信和模拟器仍全部关闭。
- 订单、售后、资金、站内消息及核心业务计数不因外部通知迁移发生变化。

## 分层回滚

- 迁移执行或结构验收失败：保持后端停止，使用已真实恢复验证的发布前 `database.sql.gz` 按原字符集整体重建并恢复 `mall_distribution`，再确认 22/22、四张 1.0.82 消息表、核心计数和敏感字段状态；禁止手工删表或改迁移记录。
- 第 23 项完整成功，但新后端健康失败：恢复旧 JAR 和旧版本文件；第 23 项为加法迁移，可保留等待修复后重试，不做破坏性降级。
- 静态资源或 Nginx 验收失败：原子恢复后台、公开商城、团队 H5 旧目录和旧 Nginx 配置；一体化 H5 从不切换。
- 备份工具切换失败：恢复旧备份工具。任何数据库恢复失败时应用保持停止，等待人工处理。

## 当前结论

1.0.83 正式候选已完成本地冻结、全量复测、统一身份构建、封包、独立验签和迁移兼容验证。当前只等待用户后续明确发布授权；本任务没有连接生产、没有上传或执行生产迁移、没有发送外部通知、没有发布，也没有修改任何 XMind、PNG 或 SVG 源文件。
