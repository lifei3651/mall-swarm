# 1.0.126 小程序配套后端发布候选

2026-09-05，当前状态：配套后端1.0.126已发布，微信登录/手机号注册运行配置已生效；公开商城、团队H5、管理后台静态仍1.0.115。原生小程序1.0.126已于17:22上传开发版，尚未设为体验版、提审或正式发布。真机成功授权待用户反馈。

准备验证：完整后端681/681（共用17、商城664）、新增直接环境变量绑定1/1通过，JAR打包、发布脚本语法、凭据处理语法及仓库只读主机/服务/Nginx门禁通过。准备阶段门禁允许尚未提交的脚本开发改动，正式候选必须提交推送后再以干净工作区执行。

## 本轮范围

- 按用户“修复/发布”授权部署累积1.0.124–1.0.126所需的后端JAR，并配置wxd26e0a4e41df392b的小程序登录与手机号快捷注册能力。
- 不覆盖公开商城、团队H5或管理后台静态资源，不修改Nginx、既有支付/提现/订阅/发货同步开关，不执行数据库迁移。前序管理后台微信同步台账UI仍需单独发布，不能把本轮后端发布称为全部前端都更新。
- 小程序代码上传、提审和正式发布分开记录；原生真机注册与授权测试需用户主动操作，凭据校验成功不等于真机流程验收通过。

## 门禁与回退

- 固定商城主机、旧版本1.0.115及旧JAR哈希；候选内部校验和、工作区远程同步、四服务和仓库Nginx模板校验。
- AppSecret只进入root-only私有配置，不在Git、候选压缩包、命令参数或日志中出现；微信校验不强制刷新令牌、不调用手机号或资金接口。
- 生产使用外部spring.config.location，因此新增实际SystemEnvironment属性源绑定回归，确认SHOP_WECHATMINIPROGRAM_*直接绑定，无需改变现有application.yml。
- 发布脚本默认需要显式版本授权，提供预检模式。切换前完整备份及独立临时数据库恢复，核对核心交易数据、装修/通知预算/奖金配置哈希；不删历史完整备份。
- 替换前保存旧JAR和版本；失败只恢复本轮程序与新增配置，不回滚数据库、不动其他前端。成功后再次完整备份，确认新私有配置纳入备份，比较既有文件与业务快照不变。

部署入口：`scripts/remote-deploy-20260905-v1.0.126-mini-backend.sh`；凭据处理：`scripts/wechat-mini-credential-setup.py`。凭据独立传输到权限700的发布临时目录，不装入源码候选。

## 最终候选与发布证据

- 源码：`de371838bad0bee436778a326cd425e2a7b1862f`；构建号：`20260905-mini-backend-1.0.126`。
- 本机候选：`/private/tmp/lingqi-126-backend-candidate.jHyNND.tar.gz`，SHA-256：`2fe3c2aae04d6d05ae94f1e22d88402d2de75031885125c90664a4e2dc99394e`。
- 后端JAR SHA-256：`9756cb0147fe0e4bd1c9f1a6dff603e93b912eb0d629a0e562aa4bbeb2eeac6c`，线上独立复核一致。
- 生产暂存：`/tmp/lingqimall-mini-backend-126.4khAPC`；只有该次临时上传密钥副本已删除，非敏感候选保留用于核验。用户本机私有文件未删除。
- 完整备份前：`/opt/lingqimall/backups/full/20260905_165628`；完整备份后：`/opt/lingqimall/backups/full/20260905_165714`，均验签及压缩校验通过。
- 隔离恢复库：`mall_distribution_release_verify_126_20260905165638`。已实际导入发布前备份、比对核心数据和配置哈希通过，随后仅删除该临时验证库；生产库未迁移或回滚，完整备份保留。
- 旧程序及发布诊断：`/opt/lingqimall/backups/mini-backend-126.GwmAPy`，未删除旧JAR。
- 生产`/etc/lingqimall/wechat-mini-program.env`为600 root:root；systemd新增引用该文件的独立drop-in。新文件包含实际直接Spring环境键，默认模板中的别名不再依赖打包YAML。旧配置及H5/管理后台/Nginx文件哈希逐项不变。
- 生产机与公网运行接口均验证enabled=true、phoneAuthorizationEnabled=true、privacyConsentVersion=MINI_PROGRAM_PRIVACY_V1，订阅和发货同步false。paymentEnabled为接口固定展示字段，支付/提现不变另由既有配置哈希及进程门禁验证。
- 四服务active，Redis PONG、健康UP；数据库迁移仍34/34、失败0。H5首页接口正常，订单/头像/订阅等匿名请求返回401。独立复查身份记录仍0，此时尚无真实注册成功证据。

## 发布中的日志验证修正

已部署脚本在journalctl检查时使用ISO带时区格式，当前Rocky版本返回无法解析时间；该处原if管道没有阻止后续检查。标准输出和错误输出的增量日志检查已执行且未发现严重错误。发布后立即以`2026-09-05 16:56:27`兼容格式独立读取journal并检查，严重错误0；记录保存在受限回退目录的`new-journal-verified.log`。仓库脚本已修正时间格式并将日志读取移出条件管道，后续读取失败必须中止验收。本修正只改发布工具，不重启或重复发布业务程序，源码提交与原候选哈希分别保留。

## 小程序复查与剩余事项

17:00开发工具从隐私页返回登录：服务未就绪提示消失，商城账号登录可点击，首次使用手机号注册入口显示；协议未勾选，手机号注册保持禁用，未代用户同意。17:01生成460 KB预览，二维码17:26失效，质量扫描显示“小程序表现良好，未发现代码质量问题”。工具仍显示21项编译提示及原有1项工具/基础库兼容错误、4项警告，不称控制台零错误。

用户已被邀请用手机扫码验证注册。尚未收到真实授权成功结果；头像/昵称/地址导入、邀请绑定及任何资金流程仍未完成真机验收。运营方仍需补真实隐私联系信息及核对微信平台授权项目，不能以本次配置生效代替客户交付验收。小程序正式代码发布、前序管理后台同步台账UI发布和资金开关保持在本轮之外。

## 17:22 小程序开发版上传

- 用户单独授权在微信开发者工具上传`1.0.126`。已填写版本号及新版装修/UI/资料导入/隐私和登录修复说明，点击上传后实际界面返回“代码上传成功”；17:22代码质量扫描显示未发现代码质量问题。
- 上传项目AppID：`wxd26e0a4e41df392b`；目录：`/private/tmp/lingqi-native-125-audit.P4Ttiv`，工程名称`lingqi-125-live-audit`沿用此前标签，实际package版本为1.0.126。上传前完整目录差异核验（排除项目配置、本地私有配置及依赖目录）无差异，代码对应已推送Git`cb1fe085455e896d1424a672eb5700200fa283d8`。项目使用正式HTTPS API，urlCheck=true。
- 再次执行工程结构/JSON/JavaScript/HTTPS检查及178项小程序测试，全数通过。上传成功弹层列明18个scripts/tests文件按packOptions.ignore排除；未将AppSecret放入小程序源码或上传包。
- 只完成开发版上传。公众平台受自动操作限制，未代设置体验版，未提交审核、撤销既有审核或正式发布；需用户在版本管理将1.0.126设为体验版后测试。不将上传质量扫描替代真实授权和业务验收。本轮没有修改产品代码、H5、后端、线上数据或运行开关，仅补上传记录。
