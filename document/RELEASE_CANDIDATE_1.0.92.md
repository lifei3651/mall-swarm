# 灵启商城 1.0.92 正式候选记录

更新时间：2026-08-27（Asia/Shanghai）

状态：正式候选已冻结并完成本地验签；首次上传在解包前被扩展属性门禁安全停止，第二次只读预检又在备份前发现备份脚本缺少执行权限；两项均已修正，最终清洁包待重新上传，尚未发布生产。

## 发布范围

- 修复品牌文化页把仅由 JPG/PNG/WebP/GIF 文件名或图片地址组成的旧正文直接展示给顾客的问题。
- 真正的旧文字介绍继续兼容；没有详情图且旧正文无效时显示“品牌内容正在准备中”。
- 管理后台装修工作台与手机预览使用同一判断，并在页面开启但没有详情图时给出运营提醒；不做硬性保存阻断。
- 无新增数据库迁移或 DDL，不修改客户装修配置、商品、订单、资金、实名或外部通知开关；一体化 H5 只封包验签、不部署；不更新 XMind。

## 生产只读基线

- 正式目标固定为 `lingqimall.com` / `VM-4-6-rockylinux`；非商城主机 `121.40.242.129` 被统一预检拒绝。
- 公网公开商城、团队 H5 和管理后台仍统一为 `1.0.91` / `6f7862a33b01adf4f3233874591d7f2b92fa5fd7` / `20260826-1502-1.0.91`。
- 四项服务均为 active，后端健康为 `UP`，仓库与生产 Nginx SHA-256 均为 `1f9e7e5caf1ad9e8aca7fb5860665828bfa323dcf005dd0e64d6ed448a99ef56`。
- 迁移 26/26、失败 0；核心计数为 `45:2:18:27:58:13450.00:5:6:3`，业务特性计数为 `1:0:0:0:0:0:0:0`。
- 完整装修配置 SHA-256 为 `33bcd606960fc4091057d5c9041e8d34cc84f5d57cad949b573b7f9af5805909`，品牌文化轮播为 `0:0`，主题色为 `#7c3aed`。候选脚本会在正式执行前重新核对这些值，任一变化即停止。

## 冻结身份与验证

- 业务冻结 Git：`9a774ccdc1a4cbf75fc72fd0481a8564a09098be`。
- 统一构建号：`20260827-1101-1.0.92`。
- 公共后端 14/14、分销后端 496/496（合计 510/510）、公开商城 91/91、管理后台 136/136 通过；后端正式包、公开商城、团队 H5、一体化 H5 和管理后台生产构建及端面边界检查通过。
- 候选共 40 个文件，其中 `SHA256SUMS` 覆盖其余 39 个文件。外层和四个前端内包均通过扩展属性、AppleDouble、危险路径、链接条目、source map、敏感配置、解包警告和内部哈希门禁；四内包清洁重封前后分别有 208/107/49/128 个文件，路径、类型、权限与逐文件 SHA-256 完全一致。
- 受控发布脚本默认执行返回拒绝码 3；只有同时提供 `--authorize-release` 和与 `1.0.92` 绑定的授权变量才可继续。只读预检模式、完整装修长 JSON 防截断哈希和发布失败有界恢复继续保留。

## 候选制品

- 外层候选：`lingqimall-v1.0.92-20260827-1101.tar.gz`，145,186,537 字节，SHA-256 `a76cdc47016f6483efdaa8052f1b8b3191ace3e35b19089997ed7b89b263c750`。首次上传包及四内包只携带 macOS provenance PAX 属性，业务文件字节未变；现已使用 `--no-xattrs --no-mac-metadata` 逐层清洁重封，并修正封包与递归预检的实际选项探测和内容误判。第二次服务器只读预检发现 `production-backup.sh` 内容正确但缺执行权限，已修正为 0750，并将三个候选执行脚本的权限纳入本地门禁。
- 后端 JAR：157,681,455 字节，SHA-256 `c1574f34303469e1ab681c24c343f3561ec85b4a969022472c765fda56e29a41`。
- 管理后台、公开商城、团队 H5、一体化 H5 的 SHA-256 依次为 `352391175ce666ff9ca07b95aec1dd9c1be625f8cbb775c08dc5e54409cd6997`、`49cc250b3ff72273528f44ce3bf1e4fa7ca27c73fd001b2bcf53161791815885`、`9a0438fff8a3a5dead72ad786dccd13730b52e55113a651b81c2f17aba6e2462`、`2130a36e55163029fa71dca238fe440c71b624d6225be87cb0646070b6fd28b8`。
- 受控发布脚本 SHA-256 为 `d98135893e25ebbc768d4d5bfaa5af802c9bcf5a1b65e8f412ae866408a31875`；候选清单 SHA-256 为 `21426dd3cf3e0636655fb54904c488e501695f9d7d375e85dc08140a394dc252`；`SHA256SUMS` SHA-256 为 `1189b61d04b95f4d0766e3e44b52b788a1be76ea845e8957ffeaf90c5f5e5767`。

## 当前结论

`1.0.92` 已取得用户明确发版授权。首次上传后的服务器检查在解包前发现外层 provenance 属性，第二次只读预检在备份前发现脚本权限；两次均未执行生产备份、迁移、应用替换或客户数据写入。最终清洁包须重新完成本地与服务器递归验签及只读预检后，才可进入独立备份、隔离恢复和受控发布。
