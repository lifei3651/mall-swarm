# 财务、权限与模块边界：非真机收口记录（2026-09-26）

## 版本与证据口径

- 本轮检查的是 `1.0.167` **之后的工作区源码**，不是已冻结的线上包。当前线上后端、管理后台、公开商城 H5 与团队 H5 的已记录基线仍为 `1.0.167 / c40a0e8cea82b8c5081ba0438dc3ced0db0daf37 / 20260924-closure-1.0.167`；`VERSION` 仍为 `1.0.167`。线上固定包、私有留档和服务器回执不能自动证明本轮代码已上线。
- C＝代码完成，T＝本地自动测试，S＝服务器部署及回读，W＝微信平台上传/体验版指向，R＝真机与真实交易。下文只记录本轮 C/T 与待验证项；**本轮没有新固定候选包，也没有新的 S/W/R 回执**。用户安排在其他非真机收口之后再进行真机和真实下单、退款全流程验收，暂缓不等于通过。
- 基座使用测试商品、地址和账号核对通用能力；本记录不要求填入客户正式经营资料，也不把模拟支付或 H2 用例当作第三方真实成功回执。

## 本轮源码边界

| 范围 | 当前源码处理 | 仍需证明 |
|---|---|---|
| 权限与最小披露 | 商户管理列表与商户敏感资料不再因普通商品权限或宽泛 GET 放行而暴露；商品页所需商户选择改走仅返回最小字段的选项接口，并限制租户/商户范围。后台菜单、直达路由与 API 权限同步收紧，低权限账号不应看到不可操作的财务、商品或商户管理入口。主要边界在 `AdminPermissionPolicy`、`AdminSecurityConfig`、`MerchantController`、`DmsMerchantMapper` 及后台对应页面。 | `SA/OP/MO/MS` 测试角色的菜单、直接 URL、接口、跨商户字段与拒绝时零副作用；当前没有逐角色运行验收。 |
| 优惠券与直播关闭态 | 多商户关闭后，商户优惠券不再新发行、领取或预占，公开可用范围与后台选项不再诱导选择商户商品；既有券/订单的追溯、核销与退款责任保留。公开直播的商品关联、主播操作和点击事件复查“仅平台自营”门禁，公开商品清单过滤已关闭模式下的商户商品；旧直播记录仍可按权限审计。主要边界在 `ShopCouponService`、`LiveRoomServiceImpl` 及各自 mapper。 | 租户开关与新领券/新交易并发、历史券及直播数据在隔离 MySQL 和实际项目组合中的行为；前端隐藏不能代替服务端门禁。 |
| 微信/支付宝退款恢复 | 外部渠道退款只在渠道成功后完成本地账务；状态 `6` 的微信与支付宝退款均可用原售后退款号重新核对。扫描按 `update_time,id` 游标轮转，单批有界；不触碰业务 `update_time`，避免掩盖既有超时告警。调度仍沿用原分布式锁键，从租户表轮转，在单轮总额度内设置各租户上下文；一个租户失败不阻断其余租户，结束后恢复原上下文。主要边界在 `ExternalRefundCoordinator`、`ShopAfterSaleServiceImpl`、`DmsShopAfterSaleMapper` 和 `ScheduleTask`。 | 支付提供方原路退款/查询、丢回调、网络超时及数据库失败后重放的真实回执；大量租户、进程重启和多实例锁租约边界需运行复核。内存扫描游标重启后会从头开始，不宣称彻底消除极端频繁重启下的饥饿。 |
| ERP 多客户重试及管理隔离 | 全局 ERP 自动重试按每条任务所属客户读取订单、执行并恢复原上下文，任务/集成/订单租户仍须一致；配置列表及保存限定当前客户，任务列表和人工重试按客户过滤。无效或异客户任务不得向适配器推送，人工重试异客户任务不产生失败计数。主要边界在 `ErpIntegrationServiceImpl` 与任务 mapper。 | 双客户 H2 与桩测试不能代替实际 ERP 提供方授权、停用/启用、回调及多实例任务运行；客户专属配置不属于当前基座验收资料。 |
| 多客户业绩视图 | 代理、会员、招募关系与历史导入业绩缺可证明的客户归属；原业绩排行榜、概览、来源和下属明细会跨客户读取。现在五个后台 GET 在检测到共库多个客户（包含停用客户）时于查数据前拒绝；单客户行为保持。不能通过只关联商城订单过滤，因为历史导入业绩没有对应商城订单行。 | 多客户模式下这些视图**暂不可用**；恢复需给代理/会员/关系/历史业绩设计客户归属、受控迁移及逐角色跨客户运行验收。当前只是安全关闭，不是完整多客户业绩能力。 |
| 未付、部分退、全退利润 | 未支付/失败/关闭不能计入实际支付和已实现利润；已支付未过售后期仅为预计利润，完成且过售后期才是已实现利润。退款处理中不提前冲减成本、净支付和奖金；完成的部分退款只冲销对应订单商品冻结成本，全额退款的净支付和展示利润归零，原始成本快照与退款追溯保留，未冲销奖金/分账另标风险。主要边界在 `DistributionAuditServiceImpl`、`DmsShopAfterSaleItemMapper` 与 `PerformanceServiceTest`。 | H2 新单测试之外的历史数据、外部渠道状态 `6`、完整商品/运费组合及真实账本；不能因代码公式更新就认定旧持久汇总已自动修正。 |

## 本地验证与证据限制

- 所有本轮补丁合并后，主任务重新独立执行 `scripts/run-mall-functional-regression.sh`，退出码 0，`mall-distribution` Surefire 报告合计 **895 项**、`mall-common` **17 项**，失败、错误、跳过均为 0；管理后台、小程序、H5 的自动测试、相应构建/工程检查及脚本 **39 项**也通过。首次测试因本机 Java 25 的 Mockito 动态代理附加限制未能启动，改用本机已有 Byte Buddy 代理后继续；首次全量回归有一项新增测试的排序预期写反，按实际 `update_time,id` 升序契约修正；ERP DAO 改签名后另一轮编译发现旧测试调用未更新，修正测试后最终完整回归全过。这些是本地 C/T，不是隔离 MySQL 或运行环境证明。
- 退款选择/批次、调度隔离、利润、ERP 双客户 DAO、业绩视图安全关闭、新建权限及模块关闭态已随最终全量重跑，对应自动用例包括 `ExternalRefundRecoverySelectionTest`、`ExternalRefundRecoveryBatchTest`、`ScheduleTaskIsolationTest`、`ErpTaskTenantDaoTest`、`PerformanceControllerTenantBoundaryTest`、`PerformanceServiceTest`、`AdminPermissionPolicyTest`、`ShopCouponIntegrationTest`、`LiveRoomFoundationTest` 与管理后台 `merchant-permission-boundary.test.js`。自动回归不代替角色矩阵和真实渠道回执。
- 本机当前无法启动 Docker，因此没有恢复隔离 MySQL、执行当前完整迁移集合的首跑/重跑，亦没有在 MySQL 上复核订单—退款—奖金—归集的锁序、并发和账本。H2/桩测试不替代这些检查；正式库本轮未写入。
- 历史已退款订单的 `dms_order_finance.company_profit` 等持久汇总值不会因新 `recalculate` 公式自动回填：订单详情会在读取时重算展示，但财务日/区间汇总仍对持久值求和。须先在隔离库确定影响集合、重算结果和回滚方案，再决定是否受控修复；**未回填、未称历史汇总已正确**。部分历史退款缺商品行成本快照时只保留人工核查口径，不推断精确成本。
- 本轮没有新候选封包、哈希、私有留档回读、服务器部署、微信开发版/体验版切换、真机页面或微信/支付宝真实支付与退款验收。线上 `1.0.167` 的既有 S/W 历史证据只能用于该精确包，不能沿用给本轮源码。

## 下一道门禁

本地全量测试与构建已通过。下一步在具备 Docker/MySQL 条件后做隔离迁移与并发、历史利润影响集合核验；多客户业绩视图保持安全关闭，另行确定代理和历史导入业绩的客户归属与迁移方案。若这些非真机项目通过，再按总账分配**新版本**，从干净、已推送提交固定唯一候选并分别取得留档、S、W 证据；用户恢复验机后才按同一候选完成角色矩阵、页面、支付、退款、售后、物流、余额、奖金和资金的 R。P0/P1 未按各自门禁关闭前，不提交正式发布结论。
