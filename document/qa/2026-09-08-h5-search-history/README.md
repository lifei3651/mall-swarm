# H5搜索与历史同步修复验收

日期：2026-09-08；基线f7d23b00；分支codex/security-membership-payout；VERSION保持1.0.134。当前Mac工作树修改前pull成功。本批未部署H5、未更新正式小程序工程、未上传腾讯或提审。

## 修复与对应范围

- H5首页原页搜索保持；显式清除输入不提交查询，结果标题显示已提交关键词。
- H5补“清空历史”，确认才清本机shop_recent_searches，取消保留，不清账号/购物车。失败保留记录并用持久弹窗提示。与上一轮小程序要求一致。
- H5历史写入失败不阻断搜索；搜索服务失败保留输入，显示弹窗及原位重试，不伪装无商品。
- H5同词在途请求合并，新搜索及卸载阻断旧响应/错误；结果区位于循环模板，兼容数组引用后再滚动。
- 后台、后端、资金/限购/登录规则未改。不是新增一轮所有页面/交易全流程通过结论。

## 自动验证

- 新增9项运行实际HomeView脚本的回归：搜索参数/原页、清空确认取消和限定存储键、失败保留、写入异常、去重/空词、并发与离页、错误重试、焦点计时、数组模板引用/损坏历史、模板入口绑定。
- `npm --prefix mall-shop-web test`：113/113通过。
- `npm --prefix mall-mini-program test`：400/400通过，无小程序代码新增修改。
- `npm --prefix mall-shop-web run build`：公开/团队/一体化构建和边界检查通过；仍有既有产物体积告警，不声称零告警。
- `git diff --check`通过。

## 实际浏览器验收

本地127.0.0.1:3184的公开H5，代理真实公开商品GET接口，无登录或交易写入：

1. 搜“复购补充装”，地址仍为首页，得到对应商品与搜索结果标题。
2. 聚焦输入出现最近搜索及清空入口，清空弹窗默认聚焦取消；取消后历史仍在。
3. 再次确认清空，清除输入后只剩热门词，原搜索结果未被误改成全部商品。
4. 390宽检查搜索浮层、输入清除、历史按钮，无挤压；320宽检查居中确认弹窗，说明与两个按钮完整可见。
5. 清除本次“礼盒”测试历史并刷新，清空入口数量为0；捕获的浏览器error日志为空。
6. 恢复默认视口、关闭临时标签和本地服务。截图在本轮工具记录中目测，未另存截图文件。

存储异常、并发/卸载等使用自动注入验证，未声称在真实浏览器强制制造存储故障。没有微信真机或真实资金操作。

## 发布边界

只读核对线上后端1.0.134、服务active、健康UP；未写服务器。H5线上静态仍沿用发布报告115状态，本轮未重新获取静态版本文件。本修复必须后续发布H5静态才会线上生效；小程序上一轮修复也仍需重新上传。版本号和固定正式上传目录保持不变。
