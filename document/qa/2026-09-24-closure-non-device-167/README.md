# 1.0.167 非真机收口复核（源码、本机测试与固定候选包）

日期：2026-09-24（Asia/Shanghai）。复核源码提交：`c1266617`。用户将真实验机暂缓，**不表示验收通过**；本记录不授权服务器部署、微信上传、提审或正式发布。

## 本轮直接完成

- 开始前确认当前仓库、干净工作区、`VERSION=1.0.167`，并从已配置的远程上游快进核对，无待拉取提交。
- 后端 `./mvnw -q test` 通过：180 个测试套件、809 项，错误/失败/跳过均为 0；`./mvnw -q -DskipTests package` 通过。
- 管理后台 `npm test -- --run` 通过：46 个测试文件、219 项；生产构建通过。
- 微信小程序 `npm test` 通过：529 项；`npm run check` 通过，涵盖工程结构、JavaScript/JSON、HTTPS、词汇与共用 UI 合同。
- 商城 H5 `npm test` 通过：183 项；公开、团队和一体化三种生产构建与边界检查通过。
- 发布脚本合同 `node --test scripts/tests/*.test.mjs` 通过：29 项；本地数据库迁移计划可读，迁移集合仍为 41 条，本次没有新增或执行迁移。
- 公网三个 `version.json` 只读回读：公开商城、团队 H5 与管理后台均返回 `1.0.165 / 60a6e5e3fe1255bace2156f0f59089c9a341db0f / 20260924-closure-1.0.165`；这不代替后端/数据库及微信平台验证。
- 源码差异仅涉及 1.0.166 收货地址展开和 1.0.167 后台“全部”页既有物流导入入口，后端发货状态/权限门禁未改。受影响场景的专门回归仍见各版本 QA 记录。

## 首轮复核时尚不能声称完成（后续固定包进展见文末）

- 首轮构建只是普通本机测试构建；后续已从干净不可变提交生成精确 1.0.167 统一候选包、manifest、逐产物哈希并验证固定微信工程。既有 1.0.165 留档回执不适用于 1.0.167；本包的独立留档及回读于 2026-09-25 另行完成，见文末。
- 正式服务器和网页仍为 1.0.165；1.0.167 随后执行服务器只读准入，但被缺失的 Node 20+ 运行时安全阻断，未做备份/隔离迁移演练或受控部署。微信 1.0.167 未上传，体验版指向未核实。
- 订单地址展开、批量物流导入及全部交易/角色路径均未做 1.0.167 真机或真实环境复核；用户已要求暂缓真实验机，不得按通过或 P0 关闭记录。
- 与验机无关但仍需收口：监控采集/告警链路、正式机运行时就绪和两件在售商品的真实退货仓库配置决策；逐角色权限 R、真实退款、渠道配置和平台状态仍依赖相应授权与直接证据。正式库权限配置的只读子项补充结果见下节。

## 正式环境只读补充核验

- 以登记的商城正式主机身份 `VM-4-6-rockylinux` 只读连接；`/opt/lingqimall/VERSION=1.0.165`，线上 JAR SHA-256 为 `934d345dee88ea7b9d12f8aeff5998e97c1f8d447caff5945e41d25cafbbd7ef`，后端、Nginx、MySQL、Redis 均 active，内部健康接口为 `UP`。未执行备份、迁移、服务重启或业务写入。
- 仅查询后台账号的角色/状态聚合及权限异常计数，没有读取账号名、密码、手机号或业务明细。`dms_admin_user` 共 2 个启用的平台账号：`SUPER_ADMIN`、`OPERATOR` 各 1；商户负责人/员工账号均为 0。空权限、历史幽灵权限 `admin:write`、未知权限 token、商户超级权限、角色与商户归属不一致、超级管理员缺 `*`、售后缺订单依赖、财务处理缺财务读取依赖的计数均为 0。这只关闭 P0-11 的**正式库账号配置只读扫描子项**，没有逐角色页面/接口/零副作用 R，尤其不能把不存在的商户角色写成已验。
- 正式机内部 `/actuator/prometheus` 为 404；未发现 Prometheus/Alertmanager 系统服务，也没有 9090/9093 监听。生产配置默认只暴露 `health`；监控采集、规则、路由、告警触发及恢复均未完成，P1-06 保持开放。正式机 `node` 不存在；新 1.0.167 准入脚本已补 Node 20+ 的只读预检。实际候选包的正式主机准入结果见下节，P1-10 保持开放。

## 固定候选包与准入（后续同日补充）

- 从干净、已推送提交 `c40a0e8cea82b8c5081ba0438dc3ced0db0daf37`（源码树 `51acd81e7ec88cc0a987d6adddd1b4c0342deffd`）重新运行完整收口回归：后端809、后台219、小程序529、H5 183、发布脚本33项均通过；后端、后台及三种 H5 生产构建和小程序工程检查通过。构建号 `20260924-closure-1.0.167`；41条数据库迁移仍为原集合，本轮未执行正式库迁移。
- 同一固定提交以 clean build 生成 `lingqi-mall-1.0.167-c40a0e8c.tar.gz`，大小 `146875450` 字节，SHA-256 `1e81bc02f11179b67ac8b5807934726118798acd0edcb0b8b956bf52e4de67ff`；包内后端 JAR SHA-256 `a1e978931d69ffae226860e32b660eaac4a7bcae5e6bfe4b0410a2e5a728d9b2`。四套静态文件数依次为管理后台226、公开商城126、团队H5 60、一体化H5 140；小程序源码296文件，固定上传工程232文件，聚合 SHA-256 `b67609a1ba72a351a624a725a8e302ed25a62e9334966882bb78718f024ad689`。包在本机 `target/releases/`；当日封包时尚无独立私有留档，次日完成，见文末。
- 本地递归准入已实际通过：外层与四个内包逐文件哈希、迁移集合、Nginx 模板和小程序源码身份均正确。微信固定工程仅运行 `--validate-only`，**没有上传微信**。
- 对该精确包执行正式主机只读准入，产物检查通过后在运行时门禁明确停止：`release-readiness-failed: 正式主机缺少 Node 20+ 运行时`。这是安全阻断，不是准入通过；没有临时补环境、传包、备份、迁移、重启或部署。P1-10 保持开放。

## 私有留档续办（2026-09-25 完成）

- 前次 Edge 文件选择器曾因扩展无文件 URL 权限拒绝本机包；用户临时开启后，已将原封不动的 `146875450` 字节候选包上传至确认为私有、开启不可变发布的 `lifei3651/mall-swarm-release-archive`，发布为明确标注“留档，未正式发布”的[不可变预发行](https://github.com/lifei3651/mall-swarm-release-archive/releases/tag/candidate-1.0.167-c40a0e8c)。平台附件显示 SHA-256 `1e81bc02f11179b67ac8b5807934726118798acd0edcb0b8b956bf52e4de67ff`、上传时间 `2026-09-25T09:31:35Z`；说明承诺至少保留至 2027-09-26。
- Edge 从该预发行附件实际下载至本机，下载文件与本机固定包分别重算：均为 `146875450` 字节、SHA-256 `1e81bc02f11179b67ac8b5807934726118798acd0edcb0b8b956bf52e4de67ff`；下载来源主机为 `release-assets.githubusercontent.com`。包内 `RELEASE_MANIFEST.json` 的 SHA-256 为 `455a1c067e6b5961b2f79c5cb56ece5e7c3e380e01c6f66a6f00cc4292a1da08`。机器回执见 [`artifact-retention.json`](artifact-retention.json)，P0-10 只对本精确 1.0.167 包关闭。
- 用户回复已关闭临时“允许访问文件 URL”权限；Codex 没有独立读回 Edge 扩展开关。此次只完成留档与回读，没有服务器备份/隔离演练/部署、微信上传/体验版绑定、真实退款或真机 R。

## 正式主机只读准入续办（2026-09-25 完成）

- 用户允许临时准备经校验的 Node 20+ 并只执行 1.0.167 只读准入。Node.js 官方 v22.23.2 Linux x64 压缩包在本机、正式主机两端分别按官方 `SHASUMS256.txt` 校验，临时运行时仅供未修改的准入和包内脚本使用。正式主机原版准入返回 `release-readiness-ok`；同一精确包在服务器再次验签，包内原版后端 `--preflight-only` 返回 `previous=1.0.165 target=1.0.167 migrations=41:41 migration-mode=verify-only`。完整边界见 [`PRODUCTION_READONLY_20260925.md`](PRODUCTION_READONLY_20260925.md)。
- 预检后正式后端 JAR、迁移 41/41、四服务与公网三站仍保持 1.0.165；临时 Node 链接、运行时和候选目录已清理。静态脚本的后置预检须等后端实际成为 1.0.167 后才可运行，未记为通过。没有备份、隔离演练、部署、微信上传或真机 R；下次服务器 S 必须重跑当时的准入并重新提供临时运行时。

结论：1.0.167 已有本机固定包、通过的本地 C/T/包内核验、独立私有留档回读及正式主机只读准入；仍缺备份/隔离演练、服务器 S、微信 W 和真机/真实交易 R。旧 1.0.165 的部署回执不适用于此包。用户暂停真机不等于通过，P0 未清零，不能进入统一发布候选。
