# 首页搜索与搜索历史修复验收

2026-09-08，基线5e2ca458，分支codex/security-membership-payout。本轮本地修复，未重新上传小程序、未部署服务器，VERSION保持134。

## 差异和修复

- H5 `HomeView.vue` 的submitSearch原页fetchProducts(true)；手输保留分类，applySearch清分类。小程序原search却switchTab分类，本次恢复同一交互，不改H5。
- 首页历史工具原只有list/remember，确实无法删除。新增clear仅删除mall_mini_recent_searches；界面提供“最近搜索 / 清空历史”，弹窗确认和取消，存储失败保留记录并准确提示。
- 新增输入框清除按钮，只清当前输入，不立即提交或删除历史。搜索结果有关键词说明；明确的“查看全部商品”保留分类跳转。
- 同词连点合并，新词与离页阻断旧响应；清空确认期间防重复弹窗，离页旧确认不能删除后续搜索词。不增加授权、网络写入或全局缓存清空。

## 验证

1. 将旧5项“必须跳分类”测试替换为新行为测试，首批8项在原代码失败，补完“查看全部商品仍可跳分类”后共9项；完整原生400/400通过。
2. 工程结构、JSON/JS、HTTPS和H5同源规则检查通过。官方wcc全32WXML、wcsc全34WXSS依赖编译通过。首次定位wcsc误指不存在的wcsc-exec目录，纠正为已安装wcc-exec/wcsc后通过，并非模板编译错误。
3. 当前Mac微信开发者工具，本次隔离工程`/private/var/folders/nk/gpz82vss0513h6pd0tkfgs6h0000gn/T/lingqi133-completion-ui-LEAIg3`；专用测试号、假数据请求层和qa-guard，禁止真实网络写入/授权/资金。未启用服务端口。
4. iPhone 12/13模拟器实际点击输入“礼盒”：点击输入仅展开建议；提交后仍为pages/home/index、底部首页高亮、结果区显示“搜索结果 / 关键词：礼盒”和匹配商品，没有跳分类。工具画面与可访问性树均核对。
5. 滚回搜索框，历史行右侧可见“清空历史”；实际点击弹出居中确认框；取消后“礼盒”记录保留。再次确认清空后历史行/按钮消失，结果与输入不受影响；再点输入清除按钮，输入为空、热门搜索出现，历史未恢复，未误发搜索或导航。本次测试产生的搜索词已删除；不涉及用户真实历史。
6. 本次隔离工程已关闭。当前工具仍有预加载/worker提示，不声称零告警；未进行真实手机键盘或授权/交易验证。正式134工程和同级发布清单未修改。
7. 修改前远程pull成功，生产只读后端VERSION134、服务active、healthUP；本次未重跑或发布H5/后台/后端，业务接口与客户配置未改。

发布边界：这份修复尚未包含在已上传的1.0.134体验版中，需下一次明确发版时从最新提交生成固定正式工程并上传。
