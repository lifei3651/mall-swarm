# 财务汇总与 44 条迁移隔离验证

日期：2026-09-26。基于 `122c30b1` 后续源码，仅本地代码/自动测试及隔离 MySQL 验证；**没有封包、部署、微信上传或真机/渠道交易**。线上仍为 `1.0.167 / c40a0e8c`，正式库仍 41/41。

## 1. 本次修复边界

单笔财务已按退款重算，但日汇总、区间汇总和工作台原先仍对历史 `dms_order_finance` 缓存字段求和。现三个入口共用 `DmsOrderFinanceDao.CurrentFinanceProjection`：

- 以订单客户、实际支付时间及已支付/关闭状态限定集合；未支付、未付关闭和无效支付状态不计实收。
- 退款取已完成退款流水；已完成实体退款冲销相应冻结成本，退款处理中及换货不提前冲成本。旧整单商品款全退可冲销全部原成本；缺明细的旧部分退款不猜造成本。
- 已结算奖金扣追回流水，待结算奖金不重复扣减，已取消/已退款奖金为零；按客户隔离，多个退款/奖金/分账明细不会因连接相乘。
- 全额退款利润归零；未冲销奖金/分账另报风险。阈值风险按当前结果计算，不沿用旧缓存标记。
- 审计汇总保留“原始产品成本”，工作台“未退商品成本”用于净拨出。后台与导出报表统一标明“账面利润（含预计）”，不再声称全退未冲销风险场景也简单等于净收款减拨出。
- 查询不改写历史财务行；**无需为本次汇总修复批量回填旧利润缓存**。其他直接读取旧字段的外部工具不在此结论内；仓库三个汇总入口已同类扫描覆盖。

## 2. 测试与独立核对

- 新增合成夹具 `mall-distribution/src/test/resources/db/finance-current-summary-fixture.sql`，9 张订单覆盖全退、部分退、退款处理中、待付款、未付关闭、无效支付状态、成交完成、全退遗留奖金及另一客户；故意把旧财务缓存全部置为 999。
- `PerformanceServiceTest.financeSummariesRebuildStaleValuesFromLedgersWithoutWritingHistory` 对日/区间/总汇总、工作台和逐单详情交叉核对，验证日期边界、空区间、客户隔离、两档风控规则和查看后缓存不变。
- H2 与正式结构副本的隔离 MySQL 8.4.10 均得到：当前客户有效支付 5 单，原始支付 520，退款 240，净收款 280，原始成本 300，未退成本 156，净奖金 35，分账 4，账面利润 95；两天利润分别 15、80。未冲销奖金风险 1，开启奖金阈值后 2，再开启利润阈值后 4。9 条原始缓存仍为 999。
- MySQL 测试执行源码中同一 SQL 投影及日分组，使用上述**合成数据**；不是运行服务连接 MySQL 的端到端交易，也未读取线上业务行。原测试首跑最后一项因未初始化默认风控规则而失败；补齐测试前置及受影响行数断言后通过，不算产品缺陷。
- 最终独立后端全量：`mall-distribution` **903**、`mall-common` **17**，失败/错误/跳过均为 0。后台 **227** 项、生产构建通过。新增后台文案合同及报表内容断言通过。本轮未改小程序/H5，未重复其构建；最终统一候选仍须同版跨端回归。

## 3. 正式结构副本 41→44 演练

沿用用户“仅只读导出结构及迁移登记”的授权。确认正式主机身份及 41/41 登记后，99 张表的 `--no-data --skip-triggers --no-tablespaces --skip-lock-tables` 结构和仅 `dms_schema_migration_history` 的 41 条元数据直接流入本机 MySQL，无本地 dump 文件、无客户/订单/资金业务行导出。导入后订单、客户表行数均为 0。

隔离容器 `mall-closure-44-20260926` 使用 `mysql:8.4.10`，无网络、无端口映射，数据目录 1 GiB tmpfs，仓库只读挂载。仓库原版持锁迁移器：

1. `apply`：原 41 条按校验和跳过，新增 3 条成功。
2. `verify`：本地 44 / 数据库成功 44。
3. 再 `apply`：44 条全跳过；再 `verify`：44/44。
4. 四个新增开关列均为 `tinyint NOT NULL DEFAULT 1`。独立合成旧客户表验证邀请迁移首次补值为 1；设为关闭后直接重跑 SQL，仍为 0。新客户服务默认关闭已由前轮服务测试验证，不能把 SQL 默认值写成 0。

| 新迁移 | SHA-256 |
|---|---|
| `V202609261800__tenant_coupon_module_switch.sql` | `c3ffa626c65d08acdd1045b464c0a3f53989df85420f843e96eba6e482f276fb` |
| `V202609262000__tenant_balance_and_merchant_mode_switches.sql` | `71f738563f3a8ce7c374078c8e6a97c3a038eaf9d73748aadd811eb1298819e6` |
| `V202609262130__tenant_invitation_switch.sql` | `5e0dca4b5ec3530a1853f6e753aa12395f2f64e9fdc50d80c312abb703a903a2` |

结束再次只读核对正式库 **41/41**。临时容器已停止并自动移除，内存中的结构副本和合成数据随之清理；可由原结构及测试夹具重建。没有删除用户数据。

## 4. 仍未完成

P1-19 现在是“源码/自动测试/隔离 SQL 通过，待同版运行对账”，不是最终关闭。仍需基座核心链路的应用层 MySQL 事务/并发验证、统一候选部署，以及用户恢复后同一版本的角色、真机和受控支付/退款/奖金资金对账。缺少明细的历史部分退款不能推算为精确成本；若实际验收遇到，再单独核对。可选 ERP 厂商定制、多客户业绩恢复等不重新拉入本轮停止线。
