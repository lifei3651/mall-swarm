# 订单详情参考图收口验收

本目录保存 2026-09-22 订单详情候选实现的隔离视觉证据。渲染对象是实际 `OrderDetailView.vue`，浏览器尺寸 `390×844`、DPR 1；所有接口都由 `scripts/qa-order-detail-layout.mjs` 本地拦截，没有读取或修改正式租户数据。

验收结果：通过。

- `order-detail-full.png`：移动端首屏实际滚动容器截图。
- `order-detail-content.png`：展开“全部订单信息”后的完整内容长图；为生成长图，仅在截图阶段解除滚动容器锁定，不改变宽度、模块或组件样式。
- `order-detail-products.png`：商品模块聚焦证据。
- `order-detail-all-info.png`：全部订单信息聚焦证据。
- `result.json`：模块顺序、横向溢出、商品图、按钮高度、标签与脱敏信息测量结果。

自动测得：无横向溢出；商品图 `112×112px`；操作按钮均 `28px` 且同排；四枚真实配置服务标签可见；完全虚构的验收姓名、手机号和地址按规则脱敏。参考图含用户个人订单内容，相关姓名、号码、地址、订单号、运单号和商品文案均未复制进仓库。

边界：这是本地 H5 实渲染和两端代码回归，不是微信真机、服务器发布或线上账号验收。
