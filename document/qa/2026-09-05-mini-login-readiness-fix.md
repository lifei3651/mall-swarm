# 小程序登录就绪状态与隐私页面修复记录

日期：2026-09-05（Asia/Shanghai）。本地候选1.0.126，分支codex/security-membership-payout；修改起点3d33f9fd。线上公开版本只读仍为1.0.115，构建20260904-2054-1.0.115，源码9efd8111964765e1db6fcfb920eb5a8af356dbc8。

## 结论与发布边界

本轮代码及模拟器可验证部分已修复，真实微信登录未恢复、未验收。用户已授权修复并按备份流程配套发布后端、核对启用登录注册，但当前生产缺少小程序凭据，不能把修改禁用提示当成登录成功。

本轮未部署、重启生产、修改数据库或运行开关，未上传小程序开发版、提审或正式发布；未触发真实短信、订阅消息、订单、支付、退款、提现。当前小程序验收副本使用真实公开接口，未注入虚假就绪状态或伪造微信授权。

## 修复内容

1. 隐私授权组件的标签选择器改成显式类名；按钮宽度、换行及默认边框在组件内约束。保留`agreePrivacyAuthorization`原生能力和`bindagreeprivacyauthorization`事件，没有用普通点击冒充授权。
2. 登录运行配置每次读取先关闭旧能力；仅布尔true表示就绪，网络失败、配置缺失和隐私版本不匹配均安全禁用。忽略过期或页面卸载后的响应，防止旧请求恢复已关闭能力。
3. 提示区分“商城登录服务暂未就绪”和“手机号快捷注册暂不可用”，提供重新检查和原生客服入口。未就绪返回页面时可复查；已就绪页面从微信手机号授权界面返回时不重新禁用按钮或丢弃该授权事件。重复提交与不具备手机号能力的事件不能发起登录。
4. 隐私说明补充小程序本地缓存、网页Cookie措辞的适用范围、实际账号资料入口和微信能力。商城后台通用政策保持原文及既有占位符替换；缺少的客服电话/邮箱明确显示暂未配置，另有独立缺失提示及在线客服按钮，不擅自填入客户资料。

## 自动验证

- 新增`login-readiness-and-privacy.test.mjs`，首批8项对旧实现失败、修复后通过，再补原生授权返回/卸载竞态回归，最终9/9通过。
- 小程序全量178/178；H5全量102/102；小程序工程检查与差异格式检查通过。
- 后端完整依赖链：共用模块17/17、商城模块663/663，合计680/680，失败0、错误0、跳过0。首次受限环境不允许Mockito附加JVM而失败；在获准的本机测试权限下完整重跑成功，不把首次失败当成业务缺陷或忽略。
- 当前微信开发者工具自带官方编译器：23个WXML、24个WXSS全部编译通过。
- 测试输出位于本机临时日志`/private/tmp/lingqi-126-mini-tests.log`、`/private/tmp/lingqi-126-h5-tests.log`、`/private/tmp/lingqi-126-backend-tests.log`；不是线上验收或持久归档的替代品。

## 原生模拟器实际验证

在独立副本`/private/tmp/lingqi-native-125-audit.P4Ttiv`同步1.0.126源码；保留该副本原项目配置和开启域名检查，项目名称仍带125，不代表源文件仍是旧版。AppID为wxd26e0a4e41df392b。

- 实点“我的”游客页→登录：看到准确的登录服务未就绪提示，登录按钮禁用、协议未勾选，重试及联系客服入口存在。
- 实点重试：进入加载状态，公开接口仍未就绪时恢复准确提示，没有触发微信授权或注册。
- 实点隐私说明并滚动：小程序补充说明、真实联系方式缺失提示、在线客服及后台通用隐私政策连续展示，没有丢弃后台政策。
- 修复后的控制台不再出现隐私组件标签选择器警告；仍有1项工具/基础库兼容性错误和4项预加载/工具能力/性能提示，不声称控制台零错误。
- 本轮截图通过原生UI工具即时查看，未宣称保存为本地图片归档；没有生成新的1.0.126手机预览二维码。上轮15:52预览不能算本轮真机证据。

真实隐私授权弹窗、微信手机号授权、登录后的头像/昵称/地址导入以及任何交易流程均尚未通过，不能用VM测试或游客页面截图替代。

## 生产只读核验与剩余阻断

- 通过已知专用SSH身份连接lingqimall.com，主机VM-4-6-rockylinux，商城服务active、本地健康接口UP。
- 服务加载alipay、data-encryption及sms环境文件；进程环境中小程序相关字段数量为0，外部application.yml没有微信小程序配置段。只检查配置键名/是否存在，没有输出密钥值。
- 公开小程序运行接口返回enabled=false、phoneAuthorizationEnabled=false、隐私版本MINI_PROGRAM_PRIVACY_V1。其paymentEnabled字段为固定展示值，不能单独拿来证明后台真实支付开关。
- 商城公开法律配置servicePhone及serviceEmail均为空。运营方仍需通过后台补齐真实有效联系信息及核对微信平台隐私指引；本轮只修复展示，没有替运营方承诺虚构联系方式。
- 已请用户把AppSecret填写至本机权限600的临时私有配置文件，不进入Git、不发送聊天、不读取回显。记录截至本轮核对时该字段仍为空。

## 恢复后的执行条件

拿到有效AppSecret后继续已有授权，不再次索要同范围授权：核验AppID配对、当前服务器版本/候选差异及配置绑定，按已有备份和隔离恢复验收流程准备配套后端候选。生产服务使用外部`spring.config.location`，不能假设只加入`WECHAT_MINI_PROGRAM_*`环境变量就一定被Spring属性读取，必须验证有效绑定且不改变支付/提现/订阅开关。

前序1.0.124/1.0.125包含共享后端与管理后台变化，不能宣称全部只是独立小程序UI或无任何H5影响。本轮新增修改没有触及H5页面源码和后端Java；后续发布仍按累积差异评估。配置就绪后再验收真机登录/注册、邀请关系及头像/昵称/地址授权拒绝与成功路径；正式小程序发布、真实资金流程保持本轮范围之外。
