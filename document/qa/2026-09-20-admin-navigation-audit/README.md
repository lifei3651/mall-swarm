# 后台设置归属与现有功能巡检

日期：2026-09-20。分支：`codex/security-membership-payout`。巡检开始前已确认唯一产品基座、发布报告、本地版本、工作区和远程分支；远程已同步，本轮未发布。

## 修复结果

- 设置编辑页改为只有一个导航归属：「设置中心」。`ERP订单对接`和`余额与提现规则`不再同时挂在「订单与售后」或「奖金与财务」菜单下。
- 保留旧地址兼容；直接打开`/tenant/erp`或`/withdraw/settings`仍进入原真实页面，但外层、面包屑和高亮都统一到「设置中心」。
- 归属规则从个别菜单删除改为通用过滤；以后新增的设置编辑页只要登记进设置索引，就不会再被业务菜单重复收纳。
- 工作台不再默认展开「商品与库存」；进入具体子页时仍由当前路由自动展开对应分组。
- 新增回归检查：设置索引的每一项必须存在真实路由，且页面权限必须与索引一致。

## 线上只读巡检步骤

截图是巡检当时线上1.0.151的修复前现状，不是本地修复冒充线上效果。因订单、会员和操作日志页含有生产账号、手机号、地址或订单号，证据PNG只保留在本机同目录，由`.gitignore`排除，不推送远程。

1. **设置中心→服务与对接：修复前失败，代码已修复。** 内容和面包屑已是设置中心，全局侧栏却高亮「ERP订单对接」，说明同一页同时被两个入口拥有。
2. **工作台：主功能正常，附加发现已修复。** 销售、利润、趋势、待办和风险区正常载入；页面无故预展开商品菜单，已取消。
3. **商品中心：通过。** 关键词、分类、状态、库存筛选、PV开关、商品表格和操作入口正常载入。
4. **订单管理：通过。** 状态筛选、搜索、导出入口、履约/售后/物流信息和「更多操作」正常载入；没有执行取消、发货、退款或导出。
5. **会员全景管理：通过。** 账号、身份、卡级筛选及会员列表正常载入；没有新增、编辑或禁用账号。
6. **财务总览：通过。** 当日统计、净收入、奖金、成本、分账、利润及风险指标正常载入；没有导出或修改资金数据。
7. **优惠券：通过。** 空数据状态、发行提醒和新建入口正常；没有创建或发行优惠券。
8. **后台操作日志：通过。** 模块/时间筛选、日志结果、分页和详情入口正常载入；已有发货、售后和配置变更成功记录。

## 自动验证

- 管理后台：44个测试文件、206项通过；生产构建通过。
- 微信小程序：510项通过；共用界面规范、H5同源生成物、工程结构、JSON、JavaScript和HTTPS配置检查通过。
- 商城H5：173项通过；公开商城、团队H5和一体化H5三种生产构建及各自边界检查通过。
- 后端：171个测试套件、747项通过，失败和异常均为0。
- 本轮共验证1636项自动测试；`git diff --check`通过。

## 限制与余留风险

- 本轮线上巡检是只读的，没有逐角色、逐权限穷举，也没有创建商品、订单、会员、优惠券，或进行真实付款、退款、发货、提现和微信授权。
- 自动测试和代表性页面巡检能证明已覆盖链路没有发现新的可重现阻断，不能证明整个系统绝对没有任何未知问题。
- H5构建仍有既有的大分块体积提醒（主要是直播媒体依赖）；不影响本次功能通过，但属于后续首屏性能优化项。
- 修复仍在源码和本地构建中，未部署管理后台、未上传小程序、未提审或正式发布。线上1.0.151仍会看到修复前的导航归属，需后续明确“发版”才会生效。
