# 1.0.157 商城收口 C/T 冻结证据（未放行 S/W）

记录日期：2026-09-23（Asia/Shanghai）

## 结论与边界

`1.0.157` 从已推送的不可变提交重跑完整跨端回归、五类生产构建、统一封包、本地递归准入、微信固定工程及 CLI 只读预检。正式主机只读发布前预检通过，仍显示线上基线 `1.0.154`。独立私有附件已上传，平台显示哈希与本机一致；**远端实际下载回读未完成，因此 P0-10 仍阻断服务器部署和微信上传。** 没有执行第 41 条正式迁移、修改服务器、上传微信、设置体验版、提审或正式发布；新版本真机 R 未验证。

`1.0.155` 和 `1.0.156` 均已在 S/W 前作废。此包是新的唯一 C/T 冻结对象，不能使用历史包、历史上传工程或旧回执替代。

| 阶段 | 结果 |
|---|---|
| C | 产品提交 `ef9da4bdfbe7bbce1d2903fe54e1edf28d119911`，源码树 `4acd081690e1fc83ad494add8ed0fd5abeffd854`，已推送 |
| T | 后端、管理后台、小程序 522 项、H5、发布脚本 26 项及生产构建的统一回归脚本退出 0；本地递归准入通过 |
| S | 只读预检通过；未部署、未执行迁移。在线仍为 1.0.154 |
| W | 232 文件固定工程只读预检通过；未上传，开发版仍为 1.0.154 |
| R | 未用 1.0.157 体验版做真机及真实交易验收 |

## 不可变产物

- 版本：`1.0.157`，构建号：`20260923-closure-1.0.157`。
- 统一包：`target/releases/lingqi-mall-1.0.157-ef9da4bd.tar.gz`，`146870042` 字节，SHA-256 `b065fe36c1a4c9a2fc4bc71a2c1a058687cca6e46da88322fee070210a75ed9a`。
- 后端 JAR SHA-256：`1d1c26720ef12350eb8974ca415d47c07a89c717715a78e325b869cbda7f92cd`。
- 后台/公开 H5/团队 H5/一体化 H5 构建文件数：`226/126/60/140`；小程序包内源码 `296` 文件；数据库迁移集合 `41` 条，仅第 41 条是本候选相对线上新增。
- 固定微信待上传工程：`232` 文件，聚合 SHA-256 `bd2ab42b75023501ab852e3ee57ab5f3d313f61c35c07bfec4495aa90fd7fe38`；工程清单 SHA-256 `944032c03c2d6061dba4c16f6cf75d9ec4294ecd8bed11234008df324d8d6af5`。AppID、正式 API、`urlCheck` 和物流插件均通过只读预检；端口 `32227` 未监听，没有执行 upload。旧 1.0.155 工程已原字节保留到独立命名的同主机目录，不被改写。
- 本机包可用 `shasum -a 256 target/releases/lingqi-mall-1.0.157-ef9da4bd.tar.gz` 复核；包内所有产物、逐文件哈希和迁移用 `bash scripts/release-readiness.sh --candidate target/releases/lingqi-mall-1.0.157-ef9da4bd.tar.gz --local-only --preflight-only` 递归复核。

## 本轮真机反馈和同类扫描

- 售后申请原因展开为窄浮层的真机反馈见 [`../2026-09-23-after-sale-choice/README.md`](../2026-09-23-after-sale-choice/README.md)。小程序售后原因、退货快递、客服业务选择及活动购买数量已共用底部通栏选择面板；系统省市区仍使用地区专用选择器。
- 自动回归覆盖取消保留原值、长选项滚动、非法索引、禁用、选择状态及业务页绑定。自动测试不等于真机弹层视觉验收，须由 1.0.157 实际体验版重验。

## 留存与下一门禁

机器可读留存状态见 `artifact-retention.json`：本机包已验签，并只追加复制到 `/Users/minmatemp/Documents/mall-swarm-app-h5-release-artifacts/1.0.157/`；同主机副本回读为 `146870042` 字节，SHA-256 仍为 `b065fe36c1a4c9a2fc4bc71a2c1a058687cca6e46da88322fee070210a75ed9a`。它只保护本机审计，不是独立耐久存储。独立私有附件已上传，平台显示 SHA-256 一致，但实际下载回读的字节数和本地 SHA-256 **未取得**；不得用平台显示哈希、本机副本、Git 源码或重新构建冒充合格回执。具体见 [`RETENTION_RECEIPT.md`](RETENTION_RECEIPT.md)。

在获得符合 [`../../RELEASE_ARTIFACT_RETENTION.md`](../../RELEASE_ARTIFACT_RETENTION.md) 的实际下载回读回执前，禁止 S/W。完成后仍须使用同一字节包通过正式准入、受控备份/迁移/发布与独立复核，并单独记录微信开发版/体验版及真机 R；P0 未清零前禁止提审和正式发布。
