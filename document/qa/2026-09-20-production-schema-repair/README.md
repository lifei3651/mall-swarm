# 2026-09-20 正式库余额结构补迁移与同类错误检查

## 结论

- 正式后端保持`1.0.149`，JAR SHA-256仍为`fbbea526aef6ed69144ccf64b14642240cb187d4595de9bbf8cd22bebaffa2d1`，没有替换程序、前端或客户配置。
- 本地迁移清单39条，修复前正式库成功记录38条；逐项对比后唯一缺失的是`V202609141100__balance_holder_withdrawal.sql`。正式库现为39/39，失败0，脚本名称和SHA-256与仓库一致。
- `dms_member_asset_account.withdrawable_balance`现为`DECIMAL(14,2) NOT NULL DEFAULT 0`，`dms_withdraw_record.agent_id`现允许为空，`idx_withdraw_user_time(user_id, create_time)`已存在。
- 余额Mapper实际读取的12个字段已在正式库直接执行通过；后端重新启动后保持`active`，`/actuator/health`为`UP`，未再出现余额字段SQL错误。
- 正式库当前会员资产账户与提现记录都是0条；迁移前后原余额、冻结额、累计收入和累计支出快照一致，没有修改订单、会员或已有金额。

## 执行与恢复证据

1. 修复前完整备份：`/opt/lingqimall/backups/full/20260920_155624`。
2. 由该备份恢复独立校验库，执行全部39条迁移；既有38条按相同校验和跳过，仅余额迁移执行。
3. 在独立库再次执行全部迁移，39条全部安全跳过；字段、可空约束、索引、回填边界和原账务字段快照通过。
4. 正式服务短暂停止后，通过统一迁移器执行同一文件并登记总账；服务退出保护负责重新拉起。
5. 第一轮正式验收查询误带了该表从来没有的通用`version`字段，因此验收脚本退出；迁移本身已成功，服务也已由退出保护恢复。随后已把查询改为Mapper真实列清单，复核通过。
6. 修复后完整备份：`/opt/lingqimall/backups/full/20260920_155817`。两份备份的数据库包、文件与配置包SHA-256均通过。

## 同类迁移遗漏检查

- 仓库39个`V*.sql`与正式库39条成功记录逐项校验通过，没有未登记迁移、失败记录或校验和冲突。
- 通用迁移器新增只读`verify`模式，后续发布可直接校验完整清单的数量、版本、脚本名、SHA-256和成功状态；发布规范同时明确禁止只把“本版本新增迁移”作为执行目录，从流程上拦截同类漏迁移。
- 9月14日至修复前共有4次`Unknown column 'withdrawable_balance'`，均来自余额摘要接口；未发现第二个仍在当前版本触发的缺列、缺表或迁移遗漏。
- 历史日志里2026-08-23出现过商户查询SQL空格错误；当前版本启动后的日志没有再次出现，不属于当前正式库迁移差异。

## 另外发现但本轮未擅自修改的现行问题

以下两项不是“数据库字段漏迁移”，但确实仍在当前正式环境发生，不能写成全部无错误：

1. **订单资金归集目标账号缺失**：`SYSTEM_PRODUCT_COST`和`SYSTEM_REMAINDER`在正式会员、推广身份和资产账户中均不存在。定时任务已经分别记录1087次失败，后端本次重启后仍立即复现；订单`L0FYBGR54XLRLS`已支付并发货，但两条归集记录尚未生成。仓库只有`document/sql/20260809_ensure_system_fund_accounts.sql`手工脚本，没有纳入迁移总账。修复会新建内部财务账户并补建资金归集记录，属于独立财务数据变更，未借本次余额字段授权顺带执行。
2. **财务风控默认规则并发初始化**：2026-09-20 15:32管理后台访问财务审计时，多请求同时执行“先查询、再新增”，其中一次命中`uk_rule_code`唯一键，页面返回系统错误。规则数据没有丢失，但初始化应改为数据库幂等写入或捕获并发重复键后重读。

另外，订单实时事件连接断开时会把正常的SSE断连记录为系统异常，并尝试向`text/event-stream`写普通JSON错误；这不会改变交易数据，但会制造错误日志，应在后续代码修复中一并降噪。

## 用户侧预期

刷新确认订单页后，`/shop/wallet/summary`不再因缺列失败，余额支付入口可正常读取状态。当前核对账号没有余额资产记录且未设置支付密码，因此会返回可用余额`0.00`；有余额并首次选择余额支付时，仍按原逻辑引导设置独立6位支付密码。
