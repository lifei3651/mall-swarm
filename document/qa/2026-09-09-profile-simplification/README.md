# 个人资料、消息和收货地址简化验收

基线 a1751bde；用户四张截图为修改前证据，不将原图中的真实手机号和地址复制进仓库。9月8日已在干净工作区拉取远程；中断后9月9日继续同一批改动。VERSION保持1.0.135，正式上传目录、腾讯版本和服务器未修改。续接前公网只读版本135/c6f72305。

| 项目 | 小程序 | H5 | 保留的规则 |
| --- | --- | --- | --- |
| 登录完成 | 授权弹层、独立微信登录、密码/短信登录不弹成功确认；继续原目标 | 未发现同类登录成功确认，不改认证 | 错误弹窗、隐私同意、邀请码和账号规则 |
| 我的头部 | 头像、昵称、实际商城账号；非独立账号显示尚未设置，真实会员等级保留 | 原有账号/等级展示保持 | 不根据余额推算等级、不暴露完整手机号 |
| 个人资料 | 头像/昵称/账号/手机号四行，昵称独立编辑模式 | 简化文字，移出密码入口及首次账号密码表单 | 微信头像授权及上传、昵称验证、新旧手机号验证码 |
| 账号与安全 | 常用服务独立入口，密码/支付/实名仍可进入 | 统一标题；首次账号设置收至独立模式 | 不关闭认证、限流或资金校验 |
| 消息中心 | 移除首页大横幅和短信卡；全部/订单/售后/资金/服务五等分短标签 | 同步移除大横幅、缩短分类，短信移到提醒设置 | 全部仍接服务端全量消息和未读数，未删除安全记录；已有短信同意可撤回 |
| 地址 | 白色分隔线列表，顶部微信导入、底部固定新增；编辑时不重复展示列表 | 同步改白色分隔线列表；原有编辑/选择功能保持 | 默认地址、删除确认、结算选择、导入后核对保存 |

## 验证

- 小程序403/403、H5 114/114测试通过；原生工程/同源规则检查、官方32 WXML/34 WXSS编译通过；公开/团队/一体化H5构建和边界检查通过，保留既有大包告警。
- 新增回归覆盖：登录完成不弹成功框、资料/安全入口分离、个人资料只含基本资料、会员等级保留、五项消息分类、设置显隐以及保存中不可切换、短信撤回入口仍保留。
- 隔离原生工程 `lingqi133-completion-ui-Awm4rl` 使用测试号，不使用云服务；伪造的本地验收会话无服务器权限，请求/上传/授权/支付等均被保护脚本拦截。未开启开发工具服务端口。
- iPhone 12/13模拟器：我的头部账号和VIP徽标、进入四行个人资料、进入昵称编辑、返回、消息五项分类无横向滚动、提醒设置/返回实点已核对。截图在本任务工具记录中；不是H5截图冒充原生。开发工具保留预加载、热重载、worker等警告，不声称零警告。
- H5本轮为源码、测试和构建验证，未进行登录态浏览器视觉验收。真机微信授权、头像上传/下载、短信发送及资金操作未验收，不将隔离页面等同于真实渠道通过。
- 地址原生实点：查看紧凑列表/微信导入行/固定底部新增，进入既有地址编辑，仅展示表单，取消返回原列表；无保存或删除真实地址。隔离工程保持打开可供查看，禁止上传。

## 发布边界

本批是135后的未发布源代码增量；无后端/数据库/客户配置变更。不上传、不提审、不正式发布。需后续重新构建小程序并上传、部署H5静态文件后，用户端才会出现本次界面。
