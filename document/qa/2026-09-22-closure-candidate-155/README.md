# 1.0.155 商城收口历史 C/T 候选（S/W 前已作废）

记录日期：2026-09-22（Asia/Shanghai）

## 结论与边界

`1.0.155` 曾从同一干净且已推送的产品提交完成完整回归、干净生产构建、统一封包、逐文件哈希、本地准入和微信固定上传工程预检；但在进入 S/W 前，同类权限扫描发现管理端快递选项、商户换货发出及订单动作展示存在权限合同缺口。由于产品代码必须变化，本候选已于 2026-09-22 标记为 **SUPERSEDED_BEFORE_SW**，由 `1.0.156` 替代，禁止部署或上传。

本目录只保存历史 C/T 证据和作废原因。它不代表服务器、数据库、微信平台或真机已经更新，也不得再通过补做耐久留存恢复其发布资格。

| 阶段 | 当前结论 |
|---|---|
| C（代码） | 历史字节身份已固定：产品提交 `930598092daa1ef5dd18d415af8d0364cb46b7c1`，源码树 `603da87084289512b2db569fed413d2ef934587f`；已被 `1.0.156` 替代 |
| T（测试） | 已通过：后端 common 17 + distribution 765、后台 208、小程序 516、H5 179，失败 0；五类生产构建、发布脚本合同、词汇/UI/敏感文件/边界门禁均通过 |
| S（服务器） | **未执行且永久禁止**：正式服务器仍为 `1.0.154`，正式库仍为 40/40；第 41 条迁移未进入正式库 |
| W（微信平台） | **未上传且永久禁止**：只曾生成并预检固定上传工程；开发版、体验版、审核版、正式版均未改变 |
| R（真实验收） | **不再作为验收目标**：当前手机只能继续排查 1.0.154 现状；后续真实验收必须绑定替代候选 |

P0 尚未清零，本候选不得称为已上线、已发布或统一发布候选，不得部署、上传、提审或正式发布。其本机包按未发布作废候选至少保留 30 天；是否留存只影响审计，不改变作废状态。

## 不可变身份

- 版本：`1.0.155`
- 构建号：`20260922-closure-1.0.155`
- 产品提交：`930598092daa1ef5dd18d415af8d0364cb46b7c1`
- 源码树：`603da87084289512b2db569fed413d2ef934587f`
- 统一候选包：`target/releases/lingqi-mall-1.0.155-93059809.tar.gz`
- 候选包大小：`146843119` 字节
- 候选包 SHA-256：`406ffc1549e29ba5ed048a1573cc8b4fbdb89e504bb5d49f2d00964e7a1ae1a7`
- 后端 JAR SHA-256：`2fe3a19b4970d4354350b295f5cec5abe0e5f07639096ae1c7ff609e362dc9ef`
- 候选清单：`REPOSITORY_ARTIFACTS.sha256`、`CANDIDATE_INNER.sha256`、`MIGRATIONS.sha256`、`EVIDENCE.sha256`

## 历史留存状态

- 历史候选包和同名 JSON 原位于本机被 Git 忽略的 `target/releases/`；为避免 `1.0.156` 干净构建清理 Maven `target/`，同一字节包已复制到 `/Users/minmatemp/Documents/mall-swarm-app-h5-release-artifacts/1.0.155/`，回读字节数和 SHA-256 与历史冻结值一致。该副本仍在同一主机，只是本机审计副本，不冒充独立耐久存储。固定微信工程及导出 JSON 仍只存在于被 Git 忽略的 `dist/`。
- Git 远程会保存产品提交、构建脚本、哈希和本目录证据，但这些材料不能恢复同一字节候选包。完整 146,843,119 字节候选不得放入普通 Git 对象。
- 独立耐久存储、对象标识、保留期限及远端回读 SHA-256 均为 **未完成**。本候选已作废，任何情况下都不得通过补做留存重新获得部署或上传资格；本机包仅按审计要求保留至少 30 天。
- 机器可读状态见 `artifact-retention.json`；`serverDeploymentAllowed` 和 `wechatDevelopmentUploadAllowed` 永久保持 `false`。
- 唯一规则见 [`../../RELEASE_ARTIFACT_RETENTION.md`](../../RELEASE_ARTIFACT_RETENTION.md)。`P0-10` 后续只针对替代候选 `1.0.156` 重新建立，不再由本包关闭。
- [`REAL_DEVICE_CHECKLIST.md`](REAL_DEVICE_CHECKLIST.md) 只保留历史现状排查说明；当前 1.0.154 截图不能关闭任何新候选的 R。

## 本轮收口修复

1. 订单列表、订单详情和商品服务标签快照维持既定紧凑规范；第 41 条迁移只为订单商品保存真实保障标签快照。
2. 进行中售后与“待评价”互斥：售后状态为 `0/4/5/6/7/8` 或异常空状态时，不进入待评价列表、角标或操作；只有售后终态 `1/2/3` 才按订单规则恢复评价。
3. “本人结算收入”按当前净额显示：已结算佣金扣除追回流水，最低为 0；待结算记录使用已被业务流程调整后的金额，不再二次扣减；关闭、退款或未知状态不计收入。
4. 候选构建剥离终端残留 `VITE_*`，避免版本号正确但接口或页面形态被外部环境污染。
5. 后端发布恢复只清理本轮确实创建的隔离数据库，避免创建失败时误删同名既有库。

## 全量回归

- 后端 Surefire：`mall-common` 17 项、`mall-distribution` 765 项，失败 0、错误 0、跳过 0。
- 管理后台 Vitest：45 个测试文件、208 项，失败 0；生产构建通过。
- 微信小程序：44 个测试文件、516 项，失败 0；工程、词汇、共用界面合同和同源生成物检查通过。
- 商城 H5：26 个测试文件、179 项，失败 0；公开、团队、一体化三种生产构建和边界检查通过。
- 发布脚本合同：7 项通过；统一候选只允许本地封包，小程序包装器只允许预检或开发版上传。
- 业务专项复审：58 项通过；本轮定向回归 8 项通过。

## 包内产物

| 产物 | 文件数 | SHA-256 | 当前状态 |
|---|---:|---|---|
| 后端 JAR | — | `2fe3a19b4970d4354350b295f5cec5abe0e5f07639096ae1c7ff609e362dc9ef` | 仅封包 |
| 管理后台 | 225 | `d4000da0352e134f063769208a8498bbeb8be96f4bc2564f97a06719f08d0245` | 仅封包 |
| 公开商城 H5 | 126 | `9f10f8105a5a881e1267c5bd50bd237fe463c3a899bea9d2860b1bb560f9c53b` | 仅封包 |
| 团队 H5 | 60 | `3b767e20c39363d670da2b489e0e5d50b55687fe61f002330701d2e93fb847c3` | 仅封包 |
| 一体化 H5 | 140 | `20792fbd22762357c307ffbfb1b248eee9ae4737a2654168322abacc1bc064a5` | 仅封包 |
| 小程序源码包 | 290 | `f983557069a7d967fd12a5cededc7d5316f29bde2a738c0850c5f0cc3c6d668e` | 仅封包 |
| 小程序源码清单 | 290 | `e387f6b52f4ce042e0b3c47473b14d61c5e93b671bbbdfd31dda4e928c26b3cc` | 已核对 |

小程序源码 290 文件聚合 SHA-256 为 `89d9a393e708732d2c5942a58aa7c09ebae26d46d274b2440a6c9b561b101e29`。排除测试、文档和源码专用文件后的固定待上传工程为 228 文件，聚合 SHA-256 为 `f2890663bdd3f1f22388892a8e6fea7ecf5c013a9c35d0e5bb3a3ee40fd33fd5`；两者用途不同，不得混用。
固定待上传工程的 228 个逐文件 SHA-256 已完整快照到 `wechat-upload-manifest.json`，其文件 SHA-256 为 `fc40122283fbd1e8e4ad08de0bc0cf358571fa0b9b4790fd69dc7d673bb7b031`；该清单可识别工程变化，但不代替 228 个实际文件或完整候选包的耐久副本。

## 迁移集合

- 共 41 条，集合清单聚合 SHA-256：`349a1296dea2a13e665b45b8cca4a9401143e91972861f153c738f033b3f9592`。
- 新增且唯一允许进入正式库的迁移：`V202609211530__order_item_service_tag_snapshot.sql`。
- 第 41 条迁移 SHA-256：`bb5f0dbf8942db2f2c56c28bd1c6c16fa185b1087690a750affd8ac523962526`。
- 本轮没有执行正式迁移；正式库仍应按 `40/40` 记录，直到 S 阶段实际回执确认。

## 本地准入与微信预检

- 统一候选、四个静态内包、小程序不可变源码、逐文件哈希、41 条迁移和 Nginx 模板递归校验通过。
- 固定微信工程 AppID：`wxd26e0a4e41df392b`；生产 API：`https://lingqimall.com/api`；`urlCheck=true`；物流插件 `wx9ad912bf20548d92@2.1.12`。
- 微信开发者工具 CLI `2.02.2608060` 只读预检通过；端口 `32227` 未监听，未开启服务端口，未执行 upload。
- `experienceVersionChanged=false`、`reviewSubmitted=false`、`formalVersionPublished=false`。

## 可重复校验

以下清单按根目录拆分，不再混用仓库路径和包内路径：

1. 从仓库根目录校验本机外层产物：`shasum -a 256 -c document/qa/2026-09-22-closure-candidate-155/REPOSITORY_ARTIFACTS.sha256`。
2. 将统一候选解压到独立临时目录后，进入解压根目录执行：`shasum -a 256 -c <仓库根>/document/qa/2026-09-22-closure-candidate-155/CANDIDATE_INNER.sha256`。
3. 从仓库根目录校验 41 条迁移：`shasum -a 256 -c document/qa/2026-09-22-closure-candidate-155/MIGRATIONS.sha256`。
4. 进入本 QA 目录校验证据文件：`shasum -a 256 -c EVIDENCE.sha256`。

清单通过只证明当前本机文件一致，不等于已完成独立耐久留存。

## 作废后的唯一顺序

1. 保留本目录、哈希和作废原因；本机二进制包至少保留到作废后 30 天，不把它上传到服务器或微信平台。
2. 权限合同修复必须使用新版本 `1.0.156`，重新执行完整 C/T、统一封包、耐久留存和后续 S/W/R。
3. 真机截图只有在“新候选上传回执 → 体验版绑定 → 体验二维码进入”的证据链完整时，才能登记为新候选 R。
