# 横向滚动条修复验收

日期：2026-09-12；基线ad92d648；分支codex/security-membership-payout；VERSION138不变。

## 问题与改动

用户要求修复横向滚动条；结合前述客服工单截图，灰色下横线是横滑容器滚动条，不是标签选中指示线。

- 原生客服工单、新品横滑容器原来仅有scroll-x，缺少enhanced/show-scrollbar=false；现补齐，并只在这两个容器添加WebKit样式回退。
- H5工单状态栏和分类视觉橱窗货架标签保留原overflow:auto/overflow-x:auto，补scrollbar-width:none及WebKit零尺寸/隐藏规则。按钮、绑定和筛选逻辑不改。
- 原生订单标签已有enhanced/show-scrollbar=false；H5订单、主图、活动首页分类已有隐藏规则；消息中心已固定五栏，不需要横向滚动。本轮不重复修改这些页面。
- 不全局隐藏所有滚动条，不关闭横滑，不裁剪最末选项；分类等页面的正常纵向滚动仍保留。Checkout中的saved-addresses目前仅残留样式无模板使用，不扩大本次修改。

## 验证结果

- 新增5项回归：遍历全部原生页横滑容器检查属性；局部回退样式；原生最末CLOSED筛选实函数；H5两处仍可滚动且隐藏轨道；H5末项筛选实函数。全部通过。
- 小程序全量453/453、H5全量142/142通过。
- 原生同源规则及工程检查通过；本次首页和客服工单WXML、首页/客服工单/共享工单WXSS经微信官方编译器通过。
- 公开/团队/一体化H5构建及边界检查通过，既有大包告警保留；无后台或后端修改。
- 差异空白检查通过。

## 实际浏览器点击与视觉

使用实际ServiceTicketsView和CategoryView组件、内存路由与本机4200独立预览，全部接口由本地假数据替代，非GET拒绝；不登录或访问生产接口、不提交真实工单、订单或资金请求。

- 320×720工单：面板可视宽300px，内容479px；computed scrollbar-width和WebKit display均为none，仍为overflow:auto，页面无横向溢出。
- 真实横滑从scrollLeft=0到179，再点击末项“已关闭”，标签选中、卡片显示已关闭和本地筛选CLOSED；截图确认标签下没有灰条。
- 320分类橱窗：货架标签可视宽289px、内容879px；真实横滑到590，点击“最后一个分类”，标题与选中项正常更新。标准/WebKit轨道隐藏，正常页面纵向滚动仍存在。
- 390×844检查工单截图及分类尺寸/样式，轨道隐藏、无横向页面溢出；本地浏览器错误日志为空。
- 截图在会话中直接查看，未持久化为图片文件。临时视口恢复、页签关闭，预览服务在结束前关闭。

## 未验与发布边界

尚未进行本次原生模拟器/微信真机视觉复核；H5截图不是原生验收证据。未开启微信服务端口。

开始前工作区干净且pull成功；公网只读公开H5138/87dace7f、后台137/ef47181a，与报告一致。本轮不部署、不导出固定正式工程、不上传腾讯或提审。下次发版才包含修复；既有未发布评价回复仍需配套数据库迁移及后端发布。

另外同步用户此前确认：三个服务器域名类别均已保存，头像真实上传/重进显示尚待用户复测。未代操作微信平台或上传用户照片。
