# 1.0.152 财务归集、风控初始化与SSE断连热修回执

## 发布范围

- 仅发布后端程序与`V202609202100__system_fund_accounts.sql`迁移；不发布管理后台、公开/团队H5或微信小程序。
- 冻结提交：`9016df160abcfb9d4e3538183291dff150c53cb1`。
- 正式JAR SHA-256：`5acbcd7b43c2b41a6aa6db685fbdbffa0fb39d17a01ae694fa3d5c29b4273893`。

## 修复结果

1. 两个内部资金归集账户通过正式迁移建立，客户会员、客户推广身份和既有账务快照保持不变。
2. 订单`L0FYBGR54XLRLS`生成`PRODUCT_COST`与`REMAINDER`两条唯一记录；金额分别为`0.00`和`0.01`，前者无需结算，后者待结算。系统余额和资产流水均为0。
3. 财务风控默认规则初始化改为数据库唯一键原子幂等写入，避免并发重复键；现有规则不覆盖。
4. SSE客户端断开与超时不再走通用500处理，也不向`text/event-stream`写JSON。

## 验证与恢复

- 后端完整测试732项通过，风控规则连续初始化后仍为4条，SSE两类结束路径测试通过。
- 最终发布前后完整备份：`/opt/lingqimall/backups/full/20260920_171658`、`/opt/lingqimall/backups/full/20260920_171813`。
- 最终回滚目录：`/opt/lingqimall/backups/financial-hotfix-152.IpysD5`。
- 正式库迁移40/40、失败0；四服务active、health UP，商城首页API返回200，临时隔离库0个。
- 发布后检查缺失归集账户、风控重复键、SSE事件流写JSON、启动失败等目标错误，命中0。

## 受控中止记录

- 第一次执行在隔离恢复库阶段因临时库名称使MySQL用户锁名超过64字符中止；正式服务、程序和数据库均未变更，仅产生完整备份`20260920_170503`，临时库已删除。
- 第二次执行的隔离迁移、重复执行和客户账务快照全部通过，正式迁移也成功；目标订单正确生成零成本“无需结算”和一分钱“待结算”两条记录。验收脚本错误地要求两条状态都为待结算，因此触发旧JAR自动恢复；迁移和新增记录按加法迁移策略保留，系统余额仍为0。该轮备份为`20260920_170948`。
- 验收口径修正后，脚本可识别39/39初始态和40/40恢复态，第三次从恢复态完整重验并成功发布。
