# 灵启商城 1.0.105 正式候选记录

候选日期：2026-08-30（Asia/Shanghai）

当前状态：已于2026-08-30受控发布并完成线上独立验收

## 候选身份

- 版本：`1.0.105`
- 版本形态：App / 小程序公开商城 + 团队 H5 拆分版
- 冻结业务 Git：`b51b27e21e09ee3cee33de85b95e1b599fd4b3ce`
- 构建号：`20260830-1533-1.0.105`
- 上一线上身份：`1.0.101` / `1b450eee20cce56e43fb7ff515a402407c41d924` / `20260830-1224-1.0.101`
- 后端 JAR：159,975,724 字节，SHA-256 `bd156a4cad83b6fd16bd985204f34d44d9e1f8c80d6c2653d686572d023b5d27`
- 正式候选包：`lingqimall-v1.0.105-20260830-1533.tar.gz`
- 候选包：147,245,275 字节，SHA-256 `8e3043d422a5bd6b1667a08008320be1f43e415da1ba80fb64dd9ac9326fa270`
- 发布脚本 SHA-256：`78c3ca4ba8ba8fe5e6109ca533423cb3f2b4eb044854b73a63f0685c3f72c42f`

## 本版范围

- `1.0.102`：余额支付先创建待支付订单，关闭、返回或密码错误后仍可从“待支付”继续；进行中售后订单由服务端统一排除出待收货列表和角标，并阻止人工或自动确认收货。
- `1.0.103`：补齐原生微信小程序结算和微信支付 API v3 程序框架，包括服务端预下单、查单、回调验签解密、关单竞态与退款闭环。基座默认`WECHAT_PAY_ENABLED=false`，没有客户商户号、证书或密钥，不在本次生产启用。
- `1.0.104`：把售后与待收货/确认收货互斥提升为数据库更新条件，未知扩展售后状态也默认阻断，不能只靠前端隐藏按钮。
- `1.0.105`：根治 iPhone 微信内置浏览器动态工具栏造成的底部导航错位；公开商城、团队 H5 和一体化 H5 获取短信验证码只校验合法手机号，图形验证码与短信验证码可任意顺序填写，最终提交时仍由服务端分别验证。
- 本版不增加数据库迁移、DDL、生产环境变量或客户业务配置；现有31项迁移原样复核，一体化 H5 只封包验签、不部署，XMind 不更新。

## 数据库与生产边界

- 发布前后迁移总账均须保持31/31、失败0，不执行新增迁移。
- 发布脚本保护当前真实测试基线：核心计数`3:2:2:4:4:199011.00:26:6:3`，业务状态`1:0:0:0:0:0:0:0`，待处理任务`0:0:0:0`，客服工单`0:0`。
- 必须保持唯一启用的`NEW_RETAIL_SIMPLE_DEFAULT`客户奖金程序、完整装修哈希`33bcd606...`、主题`#7c3aed`、品牌文化轮播`0:0`、字段加密配置以及外部通知、实名认证、微信小程序和微信支付运行开关关闭状态不变。
- 不清理或改写当前测试会员、订单、售后、商品、余额和客户装修配置；任何基线变化都在备份前停止。

## 自动化与构建

- 公共后端16/16、商城后端574/574（合计590/590）、商城前端100/100、小程序6/6通过；后端正式打包及管理后台、公开商城、团队 H5、一体化 H5生产构建通过。
- 私有部署完整安全流程通过；管理后台215项、公开商城113项、团队 H5 59项、一体化 H5 133项生产文件身份统一为候选版本、Git和构建号。
- 候选内部44/44项SHA-256通过；外层和四个内包不含危险路径、链接、macOS扩展属性、source map或私有配置。首次常规封包因macOS自动附加来源扩展属性被门禁拒绝，未上传或修改生产；改为明确禁用扩展属性后重新封包。首次服务器验签又在备份与停服务前发现迁移目录少一层`document/db`，线上未变化；修正为发布脚本固定目录后重新计算全部哈希并通过本地候选门禁。

## 发布顺序

1. 在干净且与远程一致的仓库上执行正式候选与生产联合门禁。
2. 上传后核对传输SHA-256、内部44/44哈希，并执行版本绑定的`--preflight-only`。
3. 完成完整备份、哈希校验和隔离数据库真实恢复；版本、数据或配置不一致立即停止。
4. 替换后端、管理后台、公开商城和团队 H5；一体化 H5 不部署，31项迁移只做校验。
5. 核对三端和后端身份、服务健康、Redis、严重日志、业务数据与所有默认关闭能力。
6. 完成发布后备份并清理服务器候选临时文件；正式备份保留。

## 正式发布结果

- 干净且与远程一致的仓库、最终候选包与生产联合门禁通过。服务器端传输SHA-256、内部44/44项哈希、31项迁移固定目录和脚本语法再次一致；版本绑定的只读预检确认线上仍为`1.0.101`且真实业务基线未变化后才执行正式发布。
- 发布前完整备份为`/opt/lingqimall/backups/full/20260830_155224`，通过文件哈希、数据库压缩校验并在隔离数据库真实恢复；发布后完整备份为`/opt/lingqimall/backups/full/20260830_155300`，再次通过内部哈希、数据库压缩包和文件配置包复验。
- 本版没有新增数据库迁移，31项迁移全部按既有校验和安全跳过；生产保持31/31、失败0。微信身份表保持1张表、16字段、5组索引、0记录；微信小程序登录、手机号授权、微信支付、外部通知与实名认证运行开关继续关闭。
- 线上公开商城、团队H5、管理后台与后端统一为`1.0.105` / `b51b27e21e09ee3cee33de85b95e1b599fd4b3ce` / `20260830-1533-1.0.105`；生产JAR SHA-256为`bd156a4cad83b6fd16bd985204f34d44d9e1f8c80d6c2653d686572d023b5d27`，三端文件数215/113/59，一体化H5未部署。
- 独立复核确认四项服务active、健康UP、Redis PONG、严重日志0；首页与三端入口200，商家/平台短地址分别跳转到各自登录页，接口正文与响应头请求编号一致，安全响应头完整。正式静态资源包含双验证码无顺序依赖和单滚动标识，旧的“先输入图形验证码”文案不存在。
- 核心计数`3:2:2:4:4:199011.00:26:6:3`、业务状态`1:0:0:0:0:0:0:0`、客服工单、外部待投递任务、唯一启用的`NEW_RETAIL_SIMPLE_DEFAULT`客户奖金程序、装修哈希`33bcd606...`、主题`#7c3aed`、品牌文化轮播`0:0`和字段加密配置均未变化。
- 发布成功且未触发恢复；服务器发布目录和上传候选包已删除，两份正式备份保留，XMind未更新。
