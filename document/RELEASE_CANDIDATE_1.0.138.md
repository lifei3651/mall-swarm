# 1.0.138 加购稳定与订单备注修复

日期：2026-09-11。业务起点 e3bcef4d，开始前工作区干净、pull成功。用户要求先修代码、由Codex发布服务器，小程序交给用户手动上传；本次禁止代为上传腾讯、提审或正式发布。

## 根因与边界

- 137已发布不等于缺陷已消除：生产公开H5、团队H5和后台仍为137/ef47181a，后端137/JAR 80663849f6f43f45180390f75f407f0bcf0fef2a60aa4df848be2fb9e6791c8f，四服务active/healthUP。不能从用户截图判断手机正在运行的包版本。
- 原生首页/分类虽然固定了按钮文案与颜色，仍将addingId绑定所有按钮的disabled；购物车quantityChecking也绑定加减/管理/删除/复选框。上轮测试只检查自定义样式，漏了微信组件自身的禁用状态。本轮请求锁留在逻辑层，成功仅更新角标或一次行字段补丁。售罄、数量下限和实际结算状态仍保留禁用；失败仍弹明确提示。
- 管理/勾选/删除不再整车请求；加号沿用checkAddition当前商品详情和累计限购结果，不再立即重复读取全车。减号仅重新核对同商品规格；入页/结算的服务端校验保留。原生管理勾选改成受控、带checkbox语义的控件，避免被忽略的在途点击触发原生自行勾选。旧库存警告在缓存/服务器刷新间不先清空再重显。
- H5相同列表/购物车按钮同样分离临时锁与disabled，保留aria-busy；同商品加减互斥。未修改交易API、库存/累计限购、会员、价格结算、支付和客户配置。
- 订单备注原为原生button，尽管WXSS写了width:100%，用户真机仍呈现居中窄按钮。本轮改成带按钮语义的通栏view，消除原生按钮默认宽度影响；已有草稿、取消/确定、500字与提交门禁保持。H5备注本身已是通栏结构。
- 头像截图命中微信uploadFile域名拦截，实际上传地址为 https://lingqimall.com/api/shop/media/member-avatar，预览使用同域downloadFile。公网GET到该仅支持上传的路径返回405，证明可达，不等于实际头像上传通过；没有上传真实头像。需要微信公众平台配置，不能通过部署服务器或关闭域名校验解决。

## 验证

小程序437项、H5 136项测试通过；原生同源/工程检查、官方33WXML/35WXSS编译通过；公开、团队、一体化三H5构建与边界检查通过。新增慢请求期间零可见状态补丁、重复加减/管理/勾选/删除/结算互斥、批准后一次补丁、失败数量不变、请求范围和警告稳定测试。测试中的商品/账号都是本地模拟。

微信开发者工具当前项目列表截图为空白，尚未完成原生慢请求点击/真机录屏验收；已请求临时服务端口许可，未自行开启。无新真机视频时不宣称所有视觉闪动已实际验收。原有构建大包警告保持。

## 发布范围

只部署本轮有修改的公开H5到138；后端、团队H5、后台137保持不动，无需重启后端。专用脚本绑定旧公开H5 137/ef47181a，前后完整备份、入口恢复字节检查、逐文件哈希、其它站点/后端/配置保护检查与失败回退。固定小程序目录由同一不可变提交生成138，旧137保留。

### 实际发布结果

- 发布源码`87dace7f6387266e4be12b637a28716ec69673d8`；首次GitHub SSH连接被远端关闭，重试推送成功，无改写历史。
- 公开H5共121文件，实际上传归档SHA256 `6eb9c3b773da2970fa3a46b7bcbe013d88f63081c3449085989f9d59e8faa7d1`；前后完整备份`/opt/lingqimall/backups/full/20260911_113434`、`/opt/lingqimall/backups/full/20260911_113445`，回退目录`/opt/lingqimall/backups/public-h5-138.sG1abf`。逐文件哈希、原入口从备份提取后的字节验证、公网版本/HTML/入口资源、后端/其余站点/配置保护检查通过；未触发回退，旧静态及旧哈希资源保留。
- 独立SSH再次确认公开H5为138/87dace7f、团队与后台为137/ef47181a、后端137及原JAR哈希未变，四服务active/healthUP。无后端重启或数据库变更。
- 固定正式小程序目录已生成138/同一源码，真实AppID与HTTPS API正确，269文件SHA256逐项匹配。旧137目录和清单保留于`/Users/minmatemp/Documents/mall-swarm-app-h5/dist/wechat-mini-program.previous-1789097480048`及同名`.release.json`。测试/脚本继续通过packOptions忽略。
- 没有代用户上传腾讯，没有开启服务端口或修改公众平台域名，没有进行真实头像/资金操作；原生点击/真机录屏验收仍待。用户手动上传结果见下方补记，上传及头像域名步骤保留供后续使用。

### 用户手动上传成功补记

2026-09-11，用户明确表示“上传成功了”，并提供截图`codex-clipboard-a9d61429-d053-4dc7-81b3-d14e7b4aa22b.png`，可见微信“代码上传成功”回执及代码质量扫描通过。结合此前已核对的正式工程、package版本和上传表单1.0.138，确认本次138代码上传完成；不再登记为“尚未上传”。回执提示51项文件按packOptions忽略，所示为构建/验收脚本。

此前上传流程出现覆盖体验版确认，但本次未独立进入公众平台核对体验版绑定，也未取得手机运行138的证据；未提审或正式发布。代码质量扫描与上传成功不代表实际闪动、订单备注、头像授权或资金操作已通过真机验收。工具与基础库兼容提示仍是独立事项，不将其等同上传失败。

本次仅补记文档：开始前e5f0b9d7、工作区干净，pull成功，公开H5只读仍138/87dace7f；不改业务代码、VERSION或正式产物，不重新部署服务器或上传腾讯。

## 用户手动上传小程序

1. 在微信开发者工具打开固定目录：`/Users/minmatemp/Documents/mall-swarm-app-h5/dist/wechat-mini-program`。不要打开临时QA目录，也不要上传H5的dist。
2. 核对项目为“灵启商城-正式上传工程”，AppID为`wxd26e0a4e41df392b`。如果此目录已在工具里打开，先“项目→重新打开此项目”，再编译。
3. 点击右上角“上传”；版本号填`1.0.138`，备注填：`修复首页、分类加购与购物车操作闪动；订单备注改为通栏信息行；保留库存、累计限购与结算校验。`
4. 等到“代码上传成功”。进入微信公众平台“版本管理”，确认开发版本确实为138，并检查该版本的体验版标记；若未设置则设为体验版，出现覆盖提示时按需确认。不要把点击上传或关闭上传窗口当成成功。
5. 手机完全退出小程序后，重新扫描该版本的体验二维码测试。普通线上入口可能仍打开未更新的正式版；此次不要求提交审核/正式发布。

## 头像域名配置

在微信公众平台选择同一个AppID的小程序，进入“开发管理/开发设置→服务器域名”。保留既有配置，分别检查request、uploadFile、downloadFile均包含`https://lingqimall.com`（不带`/api`路径）。如需管理员扫码，由用户本人完成。保存后在开发者工具重新打开工程获取配置，并用体验版重试头像上传及预览。仅填写request不等于允许uploadFile/downloadFile；不要关闭合法域名校验。若仍失败，保留微信原始错误及当前版本再定位，不能假定服务器保存成功。
