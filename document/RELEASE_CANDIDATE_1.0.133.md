# 1.0.133 小程序发版记录

2026-09-07；用户明确要求“发版”；分支`codex/security-membership-payout`。业务源码基线`36e65caa`，上传源码`53f72269deef0c679c993d7288a217d7d157a7bd`，本轮只更新VERSION与小程序package/lock及发布记录。

## 内容与范围

- 首页搜索、热门词与历史带词跳到分类，清除旧分类/排序，失败显眼提示。
- 包含上一批已提交的H5购物主链对齐：首个可售SKU加购、库存/限购、购物车管理、商品评价、地址解析、余额支付、合并续付、账号分层/安全表单、工单和售后等。
- 包含邀请返回同账号保持布局、授权期间按钮稳定和登录完成顺序修复；微信原生验证码窗口未自动完成的具体根因仍未证实，必须真机复测，不能宣称全部解决。
- 与服务器已发布业务`91e5a6d8`对比，H5/后台/后端代码无变化。无需部署服务器，不改客户资料、开关、资金规则或既有数据库。

## 本轮核验

- 开始时工作区干净，远程pull成功，版本未高于拟上传133。
- 357/357小程序自动测试，工程结构/JSON/JavaScript/HTTPS检查、官方30 WXML与32 WXSS编译以及差异检查通过。
- 只读正式主机`VM-4-6-rockylinux`：VERSION132，JAR SHA-256 `e23979000f84f4636f8844b04972723e20139306bcd560e9485b150ee7768975`，服务active、健康UP；公开H5静态115，后台132，均与既有报告一致。
- 默认SSH身份首次连接失败；改用已有商城专用密钥后只读核验成功，未变更密钥/服务器设置。

## 上传工程

固定目录：`/Users/minmatemp/Documents/mall-swarm-app-h5/dist/wechat-mini-program`。

由`scripts/prepare-lingqi-mini-release.mjs`从提交导出；原132工程及清单完整改名保留，可追溯回退。正式AppID`wxd26e0a4e41df392b`，API为`https://lingqimall.com/api`且合法域名校验开启；测试和脚本由packOptions排除。不得上传`/private/tmp/lingqi-parity133-native.qOtqz8`等假数据验收副本。

实际上传备注：

> 优化首页搜索与分类跳转；完善商品加购、限购及购物车操作；优化登录、邀请返回与个人中心；补充订单、售后及账号安全功能，改善异常提示。

## 上传结果（2026-09-07 15:38–15:39）

- 固定正式工程由已提交并推送的53f72269导出，236文件逐一SHA-256校验一致；正式目录再次运行工程检查及官方30WXML/32WXSS编译通过。导出清单为同级`wechat-mini-program.release.json`，无私有配置或QA请求覆盖。
- 原132工程完整保留在`/Users/minmatemp/Documents/mall-swarm-app-h5/dist/wechat-mini-program.previous-1788766068047`，配套清单为该路径加`.release.json`；未删除任何原发布工程或历史备份。
- 打开固定路径时，既有窗口仍显示上次132成功弹窗，同时缓存提示新增`pages/account-settings/index.wxml`不存在。磁盘内容和全量编译验证完整；从“项目→重新打开此项目”后正常加载，缺文件提示消失。Mac期间锁屏，用户明确解锁后继续GUI操作，未绕过锁屏或开启CLI安全端口。
- 在正式AppID与真实HTTPS API工程中实际输入“复购补充装”并点击搜索，跳到分类，保留关键词并显示1件对应商品。只访问公开商品，没有登录授权/注册/下单/付款/提现。
- GUI提示“上次提交已被选为体验版，本次上传将会覆盖体验版”，按用户发版授权确认。随后填写版本1.0.133及上述备注、点击上传，最终返回“代码上传成功”；39个scripts/tests文件按packOptions忽略，15:38代码质量扫描为“未发现代码质量问题”。非旧132弹窗，已保留本次版本填写及成功证据。
- 上传后再次核验清单236文件无变化，正式AppID、API及合法域名校验保持正确；H5/后端/后台没有部署，生产数据与配置未改。
- 工具仍有基础库版本兼容提示（1条错误）及预加载等3条警告；官方编译、实际页面运行与上传通过，不等于控制台零告警。真实手机号授权和资金全流程仍需真机独立验收。

证据目录：[本次发布截图](qa/2026-09-07-mini-133-release/README.md)。未另行操作微信公众平台，不宣称已提审、审核通过或正式版生效。

此轮以开发/体验版上传为范围，不自动提交审核或正式发布。真实手机号/隐私授权、邀请双账号、头像地址、支付退款到账/订阅/直播，以及64项对照清单剩余项仍待验收；不以上传成功代替商业全流程验收。
