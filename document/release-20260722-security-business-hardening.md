# 2026-07-22 安全与奖金业务修复发布记录

## 发布范围

- `mall-distribution`
- `mall-shop-web`
- `mall-distribution-admin`
- Nginx 站点与限速配置
- 生产数据库连接参数：由 `useSSL=false` 改为 `sslMode=REQUIRED`

旧版 mall-swarm 业务模块没有上传或启动。

## 服务器备份

正式发布前完整备份：

`/opt/lingqimall/backups/full/20260722_164547`

临时上传目录和临时回滚目录已在发布成功后清理。

## 线上文件哈希

- 后端 JAR：`3657fd609a8cd65d26ccdf6af3d48e20e9ae52025b081595af40cf7f34124ea9`
- Nginx 站点配置：`92b1709ed423bcc714f562f6f2858c38596cc0a324c707db874051ec912b687e`
- Nginx 安全配置：`afd5e39f6b16ff7a96665349798a141d59a9a15af80803b305b41b88f935b237`

## 验证结果

- 根 Maven 构建和打包成功。
- 后端 55 项测试全部执行并通过。
- 两个前端生产构建成功。
- 两套 npm 依赖扫描均为 0 项已知漏洞。
- 后端、Nginx、MySQL、Redis 服务均为 active。
- MySQL TLS 1.2/1.3 可用，应用启动前后 TLS 接受计数增加。
- HTTP 自动 301 跳转 HTTPS。
- TLS 1.2、TLS 1.3 和证书验证通过，TLS 1.1 被拒绝。
- HSTS、CSP、安全响应头生效。
- 公网 Actuator 返回 404。
- 未登录余额流水返回 401 且带 `Cache-Control: no-store`。
- 非法跨域预检返回 403。
- 商城首页、管理后台控制台和佣金结算页面实际渲染成功，无浏览器控制台错误或破图。
- 佣金结算页面显示“订单确认收货 T+7 自动结算”。
- 服务器只运行当前 `mall-distribution`，8086 只监听本机，没有旧版服务进程和旧服务端口。

## 非阻断待确认事项

生产数据库中“复购补充装”的展示文案仍包含“积分消费场景”，与当前只保留余额的业务口径不一致。源码初始化数据已是“适合日常复购场景”，但本次没有擅自覆盖线上商品资料，需由业务方确认后单独修改。

服务器存在 Ubuntu 常规系统更新，其中包含 CPU 微码安全更新。应用发布未安装系统更新，建议另设维护窗口处理。
