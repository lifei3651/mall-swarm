# 灵启商城 1.0.82 正式候选发布准备

更新时间：2026-08-24（Asia/Shanghai）

状态：正式候选已准备、未发布

生产现状：三个公开版本清单仍为 `1.0.81`；本轮没有生产 SSH 身份，未连接生产主机或数据库，线上迁移 21/21 仅采用 `1.0.81` 发布后的已验收基线，不伪称本轮数据库直查。

## 冻结身份

- 产品基座：`/Users/minmatemp/Documents/mall-swarm-app-h5`
- 分支：`codex/app-h5-split`
- 业务冻结提交：`73fab35b2cef0f35507a3b35aaa67951c5845e38`
- 版本：`1.0.82`
- 构建号：`20260824-1233-1.0.82`
- 上一正式版本：`1.0.81`
- 迁移边界：生产已验收基线 21/21；本候选新增且只新增第 22 项 `V202608241530__unified_member_message_center.sql`。
- 生产拓扑：公开商城、团队 H5、管理后台拆分部署；一体化 H5 仅作为同源可选产物封包和验签，不在拆分发布中启用。

业务产物只认以上冻结提交。后续候选准备文档和发布脚本提交不改变业务构建身份。

## 正式候选产物

候选目录：

`/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.82-20260824-1233`

| 组件 | 绝对路径 | 字节数 | SHA-256 | 版本 / Git / 构建号 |
| --- | --- | ---: | --- | --- |
| 后端 | `/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.82-20260824-1233/mall-distribution.jar` | 156,574,012 | `7a0a86479588c0f5c870f0a692b6ec61d4627b849ce18528cb95e811af30b55c` | `1.0.82` / `73fab35b...` / `20260824-1233-1.0.82` |
| 管理后台 | `/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.82-20260824-1233/admin.tar.gz` | 2,536,018 | `66e3d55a0e5a9b43758623516f4ff87a975202b7e43dc356051346c5cc28b999` | `1.0.82` / `73fab35b...` / `20260824-1233-1.0.82` |
| 公开商城 | `/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.82-20260824-1233/shop.tar.gz` | 857,557 | `af1c11775bde0cbcf0dc520f8ed71f698241656c159c4b2380c1c051ce3fd2c9` | `1.0.82` / `73fab35b...` / `20260824-1233-1.0.82` |
| 团队 H5 | `/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.82-20260824-1233/team.tar.gz` | 573,266 | `d25bda9fbc9c9bd71207e9ad2176e2c49e932262a009a56bbbfa1a0551cd6483` | `1.0.82` / `73fab35b...` / `20260824-1233-1.0.82` |
| 一体化 H5（可选、不启用） | `/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.82-20260824-1233/integrated.tar.gz` | 894,873 | `0a22838f7b42f50fb6c34ace82d60fc7eee9a61f8201aa6450d186256d24811c` | `1.0.82` / `73fab35b...` / `20260824-1233-1.0.82` |

完整传输包：

- 路径：`/Users/minmatemp/Documents/mall-swarm-app-h5/target/release-candidates/lingqimall-v1.0.82-20260824-1233.tar.gz`
- 字节数：144,208,992
- SHA-256：`b889ef5323b8321bb241eb42cda32a1a739311641d0520e00cd8b3f0c8f75750`
- 内含五类业务产物、`VERSION`、`RELEASE_MANIFEST.json`、34 项 `SHA256SUMS`、22 项迁移总账、生产全量备份工具、迁移工具、拆分 Nginx 配置和 1.0.82 专用 `release.sh`。
- `release.sh`：20,198 字节，SHA-256 `d2a85530330af1286d363a443b41e11a42208d8fa8dadd7e2951b63e5a4b9f6f`。

候选产物位于 Git 忽略目录，不进入准备记录提交。完整包必须以本节路径、字节数和 SHA-256 三项同时匹配为准。

## 已完成门禁

- 准备开始时本地和远程均为冻结提交，工作区干净；三份线上公开清单只读核验为 `1.0.81`、Git `3ce3e11fa679786723be1054fafc0906248a198b`、构建号 `20260824-1110-1.0.81`。
- 后端从冻结提交执行干净生产打包；JAR ZIP 完整性通过。公开商城、团队 H5、一体化 H5、管理后台使用统一构建身份重新执行生产构建，公开商城扫描 66 个文件、一体化扫描 123 个文件，两个边界门禁均通过。
- 四个前端包内 `version.json` 均为 `1.0.82`、`app-h5-split`、冻结 Git 和统一构建号；应用标识分别为 `storefront-public`、`team-h5`、`integrated-h5`、`admin`。
- 候选生成目录与完整压缩包重新解出的独立副本均通过 34/34 SHA-256；JAR、四个前端 tar.gz 和完整包压缩完整性全部通过。
- 四个前端包均不含 source map；候选文件名扫描未发现 `.env`、`.git`、私钥、证书密钥库或 source map，文本扫描未发现常见私钥、访问令牌特征。
- 迁移总账准确包含 22 项，迁移计划可完整列出 22/22；第 22 项脚本与开发阶段在 MySQL 8.4.10 连续执行两次的已通过版本一致，结果为四张表、15 个模板、15 个渠道配置、五个分类、7 个必要索引，三类外部通道启用数均为 0。
- 公共后端 14/14、分销后端 454/454（合计 468/468）、商城 85/85、后台 123/123、私有部署结构校验 9/9 和完整安全流程均为冻结提交的全量通过结果。
- 1.0.82 专用发布脚本、备份工具和迁移工具均通过 Shell 语法检查；脚本固定校验上一版本 1.0.81、上一迁移 21/21、新版本、冻结 Git、构建号、JAR 哈希和 22 项迁移，不满足即停止。

## 发布脚本的数据库边界

发布脚本必须由明确的生产发布授权触发；本次准备没有执行该脚本。

1. 发布前确认消息四表尚不存在、第 22 项迁移尚未登记，迁移历史必须恰好 21/21 且失败 0。
2. 先记录核心业务计数、明文敏感字段计数、数据库字符集/排序规则和字段加密配置哈希，再生成新的全量备份。
3. 全量备份必须通过 SHA-256、数据库 gzip、文件 tar 清单和关键对象检查；随后完整导入固定名称的临时验证库，确认恢复后的迁移为 21/21、失败 0、消息四表不存在，再删除验证库。恢复演练或核心业务计数任一异常都必须停止。
4. 停止后端后设置数据库变更边界，只运行包内迁移总账；前 21 项必须按原校验和跳过，只执行 `V202608241530__unified_member_message_center.sql`。
5. 迁移后必须为 22/22、失败 0，并核对消息四表、15 个模板、15 个渠道配置、五类、7 个索引和外部三通道关闭。任一条件不满足都视为迁移失败。
6. 迁移失败或结构验收失败时，保持应用停止，使用刚刚复验的 `database.sql.gz` 删除并按原字符集重建整个 `mall_distribution`，整体导回发布前快照；恢复后重新确认 21/21、消息四表不存在、核心计数和敏感字段状态一致。禁止手工删消息表或局部修改迁移记录。
7. 迁移已完整成功但后端、静态资源或 Nginx 验收失败时，按短期回滚点恢复旧 JAR、旧版本文件和三端旧静态目录；第 22 项为加法迁移，可保留到故障排除后重试，不以手工删表方式降级。

## 发布前全量备份对象

每次正式发布必须生成新的备份，不能复用历史备份。对象包括：

1. MySQL `mall_distribution`：单事务导出，包含 routines、triggers、events 和二进制安全表示，生成 `database.sql.gz`。
2. `/opt/lingqimall/uploads`、`/opt/lingqimall/config`、当前后端 JAR。
3. 当前管理后台、公开商城、团队 H5 三个静态目录。
4. 后端 systemd 服务、备份 service/timer 和当前备份程序。
5. 实际存在的 systemd drop-in、Nginx 主配置/站点配置、安全限制、TLS 配置和证书目录。
6. 字段加密配置 `/opt/lingqimall/config/data-encryption.env` 及对应 systemd drop-in，发布前后 SHA-256 必须不变。
7. 短期回滚目录另存旧 JAR、旧 `VERSION`、三端静态目录、两份 Nginx 配置和旧备份工具。

恢复性验收必须同时满足：`SHA256SUMS` 全部通过、数据库 gzip 可解压、文件包 tar 清单可读取、关键对象确实存在、备份后核心业务计数不变。

## 受控发布顺序

1. 获得用户明确发布授权及生产 SSH 身份后，再只读确认三端仍为 `1.0.81`，后端/Nginx/MySQL/Redis active、Redis `PONG`、健康 `UP`、严重日志为 0。
2. 生产数据库直查 21/21、失败 0、第 22 项未登记、消息四表不存在；核对核心计数、明文敏感字段、字段加密配置哈希和无待处理奖金/ERP/移线/超时订单。任一异常停止。
3. 上传完整候选到固定临时目录，先复验完整包 SHA-256、包内 34 项哈希、版本、Git、构建号、JAR 和四个前端清单。一体化 H5 只验签。
4. 生成并复验发布前全量备份；备份缺对象、不可解压、哈希失败或计数变化时停止。
5. 在后端停止期间执行第 22 项迁移和结构/种子/索引/渠道验收；失败按上一节整体恢复数据库。
6. 切换后端 JAR 并等待健康 `UP`，核对核心计数、加密状态和配置哈希。
7. 原子切换管理后台、公开商城、团队 H5 三端目录；一体化 H5 不切换。安装冻结 Nginx 配置前先校验，成功后才 reload。
8. 完成线上验收后生成发布后全量备份并复验，再次核对服务、日志、JAR、迁移、消息结构、核心计数和加密配置。

候选包不包含 `application.yml`、客户 `.env`、数据库密码、字段加密密钥、支付密钥、短信密钥、直播密钥或 TLS 私钥。正式流程保留服务器现有字段加密、支付、短信、直播、数据库和 TLS 配置，不从包内生成或覆盖。

## 线上验收清单

- [ ] 三份线上 `version.json` 均为 `1.0.82`、冻结 Git 和统一构建号；一体化 H5 未启用。
- [ ] 后端、Nginx、MySQL、Redis active，Redis `PONG`，健康 `UP`，启动和业务日志无新增严重错误。
- [ ] 迁移为 22/22、失败 0；第 22 项只登记一次，四张消息表、15 个模板、15 个渠道配置、五类和 7 个索引正确。
- [ ] 短信、App 推送、小程序订阅三个外部通道仍全部关闭，没有客户密钥被生成、覆盖或输出。
- [ ] 核心业务计数发布前后一致；待处理奖金、ERP、移线、超时订单为 0；明文敏感字段为 0；字段加密配置哈希不变。
- [ ] 公开商城、团队 H5 的消息入口和 `99+` 红点可见；一体化候选只在隔离环境校验，不替代拆分拓扑。
- [ ] 五个分类未读、列表、详情、单条已读、分类全部已读、全部已读正常；列表曝光不自动已读，跨端已读回源同步。
- [ ] 商城公告仍只在公告系统显示，不进入个人未读数。
- [ ] 使用两个不同会员验证消息归属；猜测其他会员消息编号、业务目标编号均被拒绝，跨租户实时订阅不泄漏。
- [ ] 受控跳转只进入订单、售后、钱包、提现、账户安全枚举页面，目标业务接口再次校验归属，不执行任意 URL。
- [ ] 后台“商城设置 → 消息运营”可读取模板、渠道和脱敏发送记录；普通管理员无资金消息重发入口。
- [ ] 公开商城验证码登录仍使用登录短信且不显示图形验证码；注册与密码登录继续要求图形验证码，不发送无验收目的的真实短信。
- [ ] 首页、商品、购物车、下单、支付、订单、发货/收货、售后退款、钱包和提现核心链路按发布验收账号回归；不得为验收伪造生产业务事实。
- [ ] 未登录后台和消息运营接口为 401；Actuator、Swagger、`.env`、`.git` 等敏感路径为 404；CSP、Permissions-Policy、HSTS 等安全响应头存在。
- [ ] 发布前和发布后两份全量备份均通过哈希、压缩与关键对象复验。

## 当前结论

本地正式候选的构建、封包和发布工具门禁均已完成，状态为“正式候选已准备、未发布”。正式执行前仍必须取得生产 SSH 身份，并以生产只读查询确认 `1.0.81`、迁移 21/21 和全部数据门禁；无法取得身份或任一门禁不一致时必须停止，不能仅凭公开版本清单发布。
