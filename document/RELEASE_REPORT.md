# 灵启商城发版报告（按版本迭代）

更新时间：2026-08-23（Asia/Shanghai）
当前线上版本标记：`1.0.75`
当前本地版本：`1.0.76`（`app-h5-split`，待发布）
线上环境：公开商城 https://lingqimall.com ；团队 H5 https://www.lingqimall.com ；管理后台 https://lingqimall.com/admin/

### App / 小程序公开商城 + 团队 H5 拆分研发记录（本地 1.0.76 待发布）

- 2026-08-23 放大登录图形验证码字符：服务端栅格图保持 `140×48`，粗体字号由 28 调整为 34，并收紧随机基线范围，提升后台、公开商城及团队 H5 共用验证码的可读性，同时避免旋转字符上下裁切。验证码仍为四位、一次性使用、两分钟失效，安全规则不变。
- 验证码生成专项 1/1 通过；本轮未连接或修改线上，线上继续为 `1.0.75`。

### App / 小程序公开商城 + 团队 H5 拆分研发记录（线上 1.0.75 已发布）

- 2026-08-23 正式发布 `1.0.75`：公开商城、团队 H5、管理后台和分销后端统一切换。业务构建 Git 为 `a56c12cb9f83f66c9c68ca293a0569b8ad371a54`，构建号 `20260823-0910-1.0.75`，后端包 SHA-256 为 `d9b88e681174c5b0b533489dac420a1f8151a451f0a8e8914d3ef5f6403e1589`。
- 发布前完整备份 `/opt/lingqimall/backups/full/20260823_091633` 已通过数据库压缩包、程序配置包、哈希及内容清单复验；发布后含新加密密钥配置的配套备份 `/opt/lingqimall/backups/full/20260823_091724` 再次复验。密钥仅保存在服务器受限配置与备份中，未进入 Git、日志或发布回执。
- 数据库迁移总账由 18/18 升为 19/19、失败 0。敏感字段按兼容读取和加密写入两阶段切换；现有数据库待迁移敏感值为 0，新写入已启用 AES-256-GCM 加密。Nginx 同步保留公开商城/团队 H5 双域名映射，并收紧未知域名、访问日志和 CSP。
- 发布前后核心计数均为 `45:2:18:27:58:13450.00:5:6:3`，待处理奖金、ERP、移线和超时订单均为 0。三端版本清单与入口均为 1.0.75；四项服务、Redis 和健康检查正常，新增错误 0，未登录后台 401，Actuator、Swagger、敏感文件和未知域名 404，安全响应头及公开商品敏感字段门禁通过。

- 2026-08-23 完成 `1.0.75` 稳定性验收、复杂度收敛和正式交付门禁：新增独立 H2 全链路用例，按真实服务顺序验证注册、团队绑定、余额下单、发货、客服备注、收货、奖金冷静期结算、货款归集、提现审核打款、退货退款、余额退回、库存回补和资金冲销。
- 验收准确复现并修复全额退货退款时关闭订单的退款数量状态口径；定时任务增加统一测试开关，避免测试并发和 Redis 噪声；新验收用例发现共享 H2 污染后改为独立数据库并重新执行全量。私有部署模板同步删除运行代码已不再使用的 Sa-Token JWT/超时死配置，未按理论性建议扩张架构。
- 浏览器实测公开首页、注册、个人中心、商品详情、结算、团队 H5 和后台工作台通过。后端 442/442、并发/幂等专项 53/53、商城 74/74、后台 110/110；公开商城、团队 H5、一体化 H5、后台生产构建、私有部署校验器 9/9 和安全工作流通过。详细见 `document/audits/2026-08-23-delivery-stability-acceptance/REPORT.md`。该验收阶段未访问线上；随后已按本节上述正式发布流程上线。
- 2026-08-22 完成第十九轮 IDOR、客户范围、并发、会话和业务逻辑复核。报告 38 项按实际“每客户独立部署、平台管理域全局可见、部署内商户按 merchant_id 隔离”重新定界，确认并修复 6 组真实问题：会员与管理员会话禁止数据库令牌哈希反向充当 Bearer 凭证，奖金任务全状态按当前客户隔离，会员错误密码锁定 15 分钟自动解除，旧佣金账户提现统一行锁，移除未使用的 Sa-Token 运行依赖，并删除订单列表两个无效身份参数。
- 第十九轮其余 31 项分别由后台权限矩阵、财务详情权限、商户父单归属、数据库原子更新、分布式锁、状态机或独立部署数据边界覆盖，不为理论报告重排资金锁或破坏平台运营能力。逐项证据见 `document/audits/2026-08-22-security-round-19-product-review/REPORT.md`。验证通过：后端 441/441、商城 74/74、后台 110/110，后端正式打包、三种 H5 和后台生产构建、两项商城边界门禁及 Sa-Token 依赖清除检查成功；无数据库迁移、未访问线上、未发布。
- 2026-08-22 完成第十八轮 Cookie、API、输入校验、XSS 与 SQL 注入复核。修复后台取消佣金前后端参数协议不一致和原因无长度边界；奖金模拟及禁用的人工结算入口补齐输入约束，禁用的财务手填退款入口不再解析业务 DTO；Sa-Token 基础配置明确只读请求头、不读 Cookie。逐项证据见 `document/audits/2026-08-22-security-round-18-product-review/REPORT.md`。
- 商城 HTTPS Cookie 的 `SameSite=None` 为原生 App 跨站会话兼容要求，现有精确 CORS、自定义请求头预检及服务端缺头 403 已验证有效；不以破坏 App 登录来消除扫描提示。批量导入继续按服务层逐行校验并输出失败明细。验证通过：专项 20/20、后端完整回归 437/437、后台 110/110 及生产构建；无数据库迁移、未访问线上、未发布。
- 2026-08-22 完成第十七轮依赖、日志、异常与 Docker 部署安全复核：ELK 参考日志改为统一脱敏编码，默认日志级别与磁盘总量收紧；生产依赖移除 Hutool 全量包、Admin Client、Knife4j、代码生成器传递和旧 Maven Docker 构建链；支付宝第三方错误、导入失败与奖金失败原因在响应或落库前脱敏，后台 401/403 改为类型化错误码。
- 客户私有部署模板默认启用数据库 TLS，MySQL、Redis、后端和 Nginx 增加可调 CPU/内存/进程限制，MySQL/Redis/Nginx 增加只读根文件系统和最小权限校验；未被发布流程引用的旧 `external_links` Compose 文件已移除。逐项证据见 `document/audits/2026-08-22-security-round-17-product-review/REPORT.md`。
- 当前 Mac 已安装并登录官方 Docker Desktop 4.87.0（Engine 29.7.2、Compose 5.4.0），并完成真实空数据卷和同卷升级两轮验收。运行验证发现并修复 MySQL 初始化入口不可执行、基础库与历史迁移边界冲突/前置表缺失、Nginx 仍绑定被原子替换的旧静态目录三项交付缺陷；新装与升级路径现被明确分离。
- 空库最终形成 70 张表，迁移总账 19/19、失败 0、默认管理员 0；公开商城、团队 H5、管理后台、后端健康、HTTPS、安全响应头、敏感路径和非公开端口检查均通过。同卷升级先生成并验证数据库备份，19 项迁移全部按校验和跳过，仅重建后端与 Nginx，MySQL/Redis 不重启；四份构建清单自动带 Git 身份和构建批次。
- 验证通过：后端完整回归 434/434，商城 74/74，后台 110/110，三种 H5、后台与后端生产构建、运行 Jar 依赖核验、私有部署校验器 9/9、安全工作流、脚本语法及差异检查成功。测试容器、网络、数据卷与临时密钥证书已清理；无新增数据库迁移，未连接线上、未发布，线上继续为 `1.0.74`。
- 2026-08-22 完成第十六轮 23 项报告复核。商城登录回跳统一为规范站内路径；支付宝异步通知在原有 RSA2 验签、应用、金额、状态、行锁与幂等保护上增加收款商户 `seller_id` 校验，生产启用时必须配置 `ALIPAY_SELLER_ID`；支付宝和阿里云短信 SDK 增加连接/读取超时。调度器改为可配置 4 线程并优雅停机，ERP 与奖金扫描周期改为部署变量，会员浏览器快照不再保存登录账号。
- Cookie 写请求已有自定义请求头、严格 CORS、HttpOnly/SameSite 的 CSRF 防护；History 路由、公开 404、CSP meta、监控实例采样和未使用 `@Async` 不构成漏洞。ERP 适配器当前没有出站请求且禁止启用，真实对接时须按客户内网/专线设计 endpoint allowlist。逐项证据见 `document/audits/2026-08-22-security-round-16-product-review/REPORT.md`。
- 验证通过：后端完整回归 426/426、商城 74/74、后台 110/110、支付/超时专项 24/24；后端正式打包、公开商城/团队 H5/一体化 H5、后台生产构建、私有部署校验器 8/8 和安全工作流成功。无新增数据库迁移，未连接线上、未发布，线上继续为 `1.0.74`。
- 2026-08-22 完成第十五轮文件、配置、短信和批量导入复核：导入文件增加真实签名/UTF-8 内容与业务 5MB 校验、独立限流和 DTO 服务端约束；导入原始明细使用既有 AES-256-GCM 机制加密落库并兼容历史分批迁移；售后临时凭证增加每日清理；Sa-Token 与 Nginx 默认日志减少敏感信息暴露。逐项结论见 `document/audits/2026-08-22-security-round-15-product-review/REPORT.md`。
- 第十五轮无新增数据库结构迁移。后端完整回归 421/421、更新专项 27/27、编译打包及私有部署安全校验通过；未连接线上、未执行历史导入明细加密、未发布，线上继续为 `1.0.74`。
- 2026-08-22 完成第十四轮报告的产品与代码复核。商户资料、保证金、提现状态和财务风控等高影响操作补齐服务端强制二次验证，前端统一请求两分钟、一次性、绑定管理员/方法/路径的凭证；读取、客服和普通履约操作不增加无意义密码弹窗。
- 身份证、代理/会员/商户收款账号、商户提现账号快照、ERP 应用密钥及回调令牌改为 AES-256-GCM 随机密文落库，读取兼容历史明文，历史数据按 200 条独立事务可重试迁移。生产环境强制独立 64 位十六进制密钥，密钥不进入 Git、日志或前端。
- 新增迁移 `V202608222130__sensitive_data_encryption_capacity.sql`，本地迁移计划由 18 项增至 19 项；线上仍为 18/18，未执行本轮迁移。既有环境发布采用两阶段开关：先以兼容只读模式验证新程序，再启用加密写入并迁移；迁移后禁止直接回退到不识别密文的旧包。
- Nginx 收紧未知域名/IP、CSP 外联和行内样式策略，私有部署上传上限与后端统一为 6MB；客户模板强制启用字段加密且将密钥与数据库备份成对管理。逐项结论见 `document/audits/2026-08-22-security-round-14-product-review/REPORT.md`。
- 验证通过：后端完整回归 409/409，新增安全/持久化专项 14/14，商城 72/72、后台 110/110；后端正式打包、公开商城/团队 H5/一体化 H5、后台生产构建、私有部署校验器 8/8、安全工作流、迁移计划与差异格式检查均成功。本轮未连接线上、未执行线上迁移、未发布，线上继续为 `1.0.74`。

### App / 小程序公开商城 + 团队 H5 拆分研发记录（线上 1.0.74 已发布）

- 2026-08-22 正式发布 `1.0.74`：公开商城、团队 H5、管理后台及分销后端统一切换，累计纳入 1.0.63～1.0.74 的安全、资金、定时任务、商户结算、权限审计和产品链路修复。
- 发布构建身份为业务 Git `21ef651c82035a62ddfbfd94f55af1822c2435e1`、`20260822-1901-1.0.74`；后端包 SHA-256 为 `76c5a7e20966ff691d8d25884eeea1310a950cd2b1a1a836034ccd2590866f37`。发布前完整备份 `/opt/lingqimall/backups/full/20260822_190710` 的数据库、程序、配置、上传文件和三端静态资源已通过哈希、压缩包及内容清单复验。
- 停服后按迁移总账执行 5 条待发布迁移：支付宝迟到支付退款标记、佣金财务租户隔离、业绩排行索引、操作日志请求上下文、管理员初始密码生命周期；迁移记录由 13/13 升为 18/18，失败记录为 0。
- 发布前后核心计数均为 `45:2:18:27:58:13450.00:5:6:3`，待处理奖金/ERP/移线/超时订单均为 0。三份线上版本清单、入口和资源均为 1.0.74；服务与 Redis 正常、健康检查 `UP`、新增错误为 0，未登录后台与二次验证接口 401，Actuator/Swagger/敏感扫描路径 404，安全响应头和公开商品敏感字段门禁通过。

- 2026-08-22 完成第十三轮代理层、后台敏感操作、SSE 与数据库脚本安全复核。私有部署补齐 CSP、Permissions-Policy、显式目录禁列和常见扫描路径拦截；遗留微服务调试端口仅绑定本机，部署后验收开始真实核对安全头与扫描入口。
- 后台账号控制、会员状态/调级/移线、商户冻结、订单退款、售后退款、提现审核、佣金结算、外部团队迁入及配置恢复升级为服务端强制二次验证：当前管理员密码通过现有加密挑战传输，签发绑定管理员/方法/路径的两分钟一次性 Redis 凭证，原子消费、不可挪用或重放，并有独立限流。
- 删除旧 `mall.sql` 的 1653 条账号与业务数据，当前基线和历史升级 SQL 不再创建固定管理员；首次部署改为交互式 `bootstrap-admin`，仅空管理员表可运行且首次登录强制改密。代码生成数据库凭证移出 Git，旧升级脚本字符集拼写完成修正。逐项结论见 `document/audits/2026-08-22-security-round-13-product-review/REPORT.md`。
- SSE 最长连接由 30 分钟收紧为默认 10 分钟，到期重新认证；统一 CORS、后台权限、有限队列和查询接口权威状态继续保留。第十三轮代码本身无新增数据库迁移，开发验收阶段未连接线上；随后已通过本节上述正式发布流程上线。
- 完整验证通过：后端 402/402、商城 72/72、后台 110/110；公开商城/团队 H5/一体化 H5、后台生产构建、私有部署校验器 8/8、安全工作流及脚本语法检查均成功，构建版本统一为 `1.0.74`。

- 2026-08-22 完成第十二轮管理员、Redis、接口暴露与后台高风险操作复核：后台新建/代重置账号强制首次改密，密码使用 10～64 位及至少三类字符策略并拒绝复用；生产启动门禁要求 32 位以上 Redis 强密码，私有模板禁用危险管理命令，短信缓存键不再含手机号明文。
- 接口文档默认关闭、基础服务默认只监听本机、健康端点最小披露、测试 H2 控制台关闭；客户端错误采集和 ERP 回调增加独立限流，ERP 启用前强制合格回调令牌。ERP、财务风控和商户提现高风险操作增加确认、处理中保护，打款密码在所有退出路径清空；操作日志在服务端统一脱敏。
- 报告正文实际 31 项中，17 项真实缺口已修复、5 项完成条件风险加固、9 项按当前私有部署架构和合法公开协议不误改，逐项证据见 `document/audits/2026-08-22-security-round-12-product-review/REPORT.md`。新增迁移 `V202608221830__admin_initial_password_lifecycle.sql`，本地迁移计划 18 项，线上仍登记 13/13。
- 完整验证通过：后端 406/406（公共 8、分销 398）、商城 72/72、后台 108/108；后端完整回归、公开商城/团队 H5/一体化 H5 三种构建及边界门禁、后台生产构建、私有部署校验器 8/8 与安全工作流均成功。本轮未连接线上、未执行迁移、未发布，线上仍为 `1.0.62`。

- 2026-08-22 完成第十一轮定时任务、商户结算、异常响应和 SSE 复核：超时订单、商户货款及旧版到期移线按单据独立事务处理；奖金、平台归集和商户结算三个子任务异常互不阻塞；ERP 单条适配器异常不再中断整批。
- 商户资料与八维控制状态先锁定商户行，兼容启停和经营/收款资料补齐脱敏业务审计；待付款支付与停业/清退竞争同一商户锁；商户结算候选 SQL 显式限定当前客户。所有环境隐藏异常详情，未登录使用明确 401 错误码，文件解析和 SSE 4xx 重试完成收口。
- 报告 37 条中确认 14 条缺陷并已修复，6 条完成预防性加固，3 条为正确设计，14 条为误报、风格建议或当前独立部署架构下无可利用路径；逐项证据见 `document/audits/2026-08-22-security-round-11-product-review/REPORT.md`。本轮无数据库迁移、未连接线上、未发布，线上仍为 `1.0.62`。
- 完整验证通过：后端 400/400（公共 8、分销 392）、商城 72/72、后台 105/105；后端正式打包、公开商城/团队 H5/一体化 H5 三种生产构建、公开与一体化边界门禁、后台生产构建和私有部署安全工作流全部成功，四份构建清单均为 `1.0.72`。

- 2026-08-22 用户确认本目录为后续唯一商城基座：只维护 App / 小程序公开商城 + 团队 H5 拆分版及同源可选一体化 H5；原 `/Users/minmatemp/Documents/mall-swarm` 一体版仅保留历史备份，不再开发、修复或发布。仓库规则已加入根目录门禁，避免再次把补丁落到旧产品线。
- 将上一轮资金补丁落实到当前拆分基座：S07 余额流水和 S08 看板提现脱敏原已存在并继续保留；新增团队 H5 提现自然日/月次数及累计金额上限，同一会员通过行锁串行核对；底层余额入账/扣款使用稳定业务请求号复用原流水，不同业务或金额复用同一编号时拒绝执行。
- 提现默认每日 5 次、每月 50 次、每日 10 万元、每月 100 万元，客户交付时通过私有部署变量调整；审核拒绝仍计申请次数以抑制反复提交，被拒绝金额不占累计金额。无需数据库迁移，本轮尚未发布，线上继续为 `1.0.62`。
- 验证通过：资金与脱敏专项 42/42，后端完整回归 395/395（公共 8、分销 387），商城 71/71、后台 104/104；后端正式打包、公开商城/团队 H5/一体化 H5 三种生产构建、公开与一体化构建门禁、部署安全工作流及策略测试 7/7 均成功。

- 2026-08-22 完成第十轮商品、资产、前端状态和数据库迁移复核：商品售价/市场价及 SKU 价格变化新增业务审计；商品上下架和会员启停使用目标行锁；商品封面、图集、详情和规格图片统一只允许 HTTPS 或站内路径；订单详情余额支付补齐稳定幂等键，登录、找回和密码修改阻止请求中重复提交。
- 尚未发布的操作日志迁移改为可安全重试的条件 DDL。余额流水前后金额、固定租户 1、已有资金幂等和代理树锁等候选项经源码与测试确认符合当前“每客户独立部署、部署内 merchant_id 隔离”的业务边界，未按误报改坏。完整结论见 `document/audits/2026-08-22-security-round-10-product-review/REPORT.md`。
- 最新完整验证为后端 393/393（公共 8、分销 385）、商城 71/71、后台 104/104；公开商城/团队 H5/一体化 H5 生产构建、构建边界检查和后台生产构建通过。本轮未访问线上、未执行迁移、未发布；线上仍为 `1.0.62`。
- 2026-08-22 独立复核 SKU 与团队性能清单：SKU 写入服务已按 JSON 对象限制最多 20 项并拒绝嵌套，现有 21 项拒绝测试证明报告为误报，因此未按错误的 JSONArray 建议改变数据格式。
- 成立的大团队问题已修复：整线移线及取消资格后的子树提升批量读取代理资料并复用事务快照；旧、新祖先链团队人数由逐项查询/更新改为批量聚合与批量写入，关系数据缺失时先失败后变更。奖金、历史业绩、上下级与人数口径不变。
- 后端完整回归 389/389（公共 8、分销 381），专项覆盖固定批量查询次数、空团队归零、整棵关系重建和历史业绩不迁移；商城 70/70、后台 104/104、后端正式打包及四份前端生产构建通过。本版无数据库迁移，不连接线上、不修改线上数据、不发布；线上仍为 `1.0.62`。

- 2026-08-22 完成第九轮路由、运费、配置权限与操作审计复核：运费模板读写和结算按服务端当前客户隔离，关联商品模板禁止误停用，地区名称规范化，后台增加真实物流轨迹入口；商品与商城资料长表单增加未保存确认，401、403、404 与公开登录路由行为明确。
- 商城资料、奖金规则和 ERP/第三方集成拆为 `config:shop`、`config:bonus`、`config:integration` 三类权限，旧 `config:manage` 自动兼容；PV、财务、可见范围等关键 DTO 增加服务端约束，奖金配置变更同步清理缓存并记录前后摘要。
- 登录、退出、管理员账号、会员提现、奖金与商城配置补充业务级审计；所有请求建立关联编号，操作日志增加 IP、客户端信息、时间范围查询和清理批次摘要。新增迁移 `V202608221600__operation_log_request_context.sql`，本地迁移计划共 17 项，线上仍登记 13/13。
- 本版不连接线上、不修改线上数据、不发布；逐项产品结论见 `document/audits/2026-08-22-security-round-9-product-review/REPORT.md`。完整验证为后端 388/388、商城 70/70、后台 104/104；后端打包、公开商城/团队 H5/一体化 H5 三种生产构建、两项构建门禁和后台生产构建通过。线上仍为 `1.0.62`。

- 2026-08-22 完成第八轮商品、导入、代理关系和内容治理复核：关系树写操作统一数据库串行并固定锁顺序；导入增加格式、容量和同客户单任务保护；分类限制总量并改为平台专属维护；SKU 规格限制结构和数量；团队列表改为批量查询。
- 评价与公告增加客户可配置内容词库，评价和商品图片增加独立限流；公开商品图片缓存由 365 天缩短为 1 天，私密文件权限降级会输出明确告警。旧版 PMS 代码经引用核查未进入当前业务，不做无收益的遗留表改造。
- 本版无新增数据库迁移，不连接线上、不修改线上数据、不发布；完整结论见 `document/audits/2026-08-22-security-round-8-product-review/REPORT.md`。线上仍为 `1.0.62`。
- 完整验证为后端分销 378/378、公共模块 8/8、商城 70/70、后台 104/104；后端正式打包、公开商城/团队 H5/一体化 H5 三种生产构建、公开包扫描、一体化完整性门禁和后台生产构建均通过，四份本地产物版本清单均为 `1.0.68`。

- 2026-08-22 新增可选一体化 H5 构建模式：同一套源码现可生成 `storefront-public`、`team-h5` 与 `integrated-h5`，一体化入口包含普通商城、秒杀、按客户开关显示的复购区、邀请、团队业绩、钱包、转账、提现、订单与售后；现有公开 App / 小程序包和团队 H5 的隔离边界保持不变。
- 结算业务改为构建级策略：公开包继续只允许普通商品和秒杀商品，一体化包额外允许复购商品，三类商品禁止混合下单；一体化会话使用完整会员身份恢复，公开包继续使用普通商城资料接口。新增一体化构建身份、独立输出目录和完整性门禁，私有交付构建包同步保留该可选产物。
- 本版无新增数据库迁移，不连接线上、不修改线上数据、不发布；后端 375/375、商城 70/70、管理后台 104/104 回归均通过，后端正式打包、公开商城/团队 H5/一体化 H5 三种商城生产构建、两项构建边界门禁和后台生产构建全部成功。线上仍为 `1.0.62`。

- 2026-08-21 完成 `1.0.66` 本地安全与性能候选：余额流水、经营看板提现和提现普通列表统一脱敏登录账号、手机号、实名与银行卡号；完整收款资料仅允许具有 `finance:manage` 的财务处理人员通过单笔详情读取，普通财务只读权限不再获得完整资料。
- 业绩排行榜由全量会员逐项查询及内存分页改为数据库一次聚合、SQL 排序与数据库分页，新增两项幂等查询索引迁移 `V202608211730__performance_ranking_indexes.sql`；统计口径继续保留个人/团队/新增会员、区间/累计/当月三个维度。
- 购物车打开及结算前均从服务端刷新商品价格、PV、库存、限购和商户信息，本地缓存只作为购物清单，不再作为可信展示价格来源；完整复核结论见 `document/audits/2026-08-21-security-round-7-followup/REPORT.md`。
- S05、S06 经后端权限矩阵和商户工作区隔离复核后确认是平台管理员的合法按会员查询能力，不改成当前登录管理员本人；C01 订单号使用完整雪花随机尾段并有订单归属校验，移除时间前缀不增加实质安全收益，均不按误报破坏现有业务。本版未连接线上、未执行迁移、未发布，线上仍为 `1.0.62`。
- 最新完整验证为后端 375/375、商城 70/70、管理后台 104/104；公开商城/团队 H5 双生产构建、公开包边界扫描、后台生产构建及差异格式检查通过。新增迁移后本地迁移计划共 16 项，线上仍登记 13/13，发布时须先备份再按迁移总账执行待发布项。

- 2026-08-21 完成 `1.0.65` 本地安全候选：逐项复核第七轮 22 项报告，7 个报告条目含实际修复（其中 S01/S08 为同一根因）、2 项进一步加固、1 项性能技术债，其余 12 项在当前“每客户独立数据库、部署内按商户隔离”架构及现有权限/资金保护下不构成可利用漏洞，完整证据见 `document/audits/2026-08-21-security-round-7/REPORT.md`。
- 看板佣金金额、数量及最新记录增加显式客户租户范围；佣金和资产模糊搜索增加服务端长度限制；经营报表导出增加每来源每分钟 5 次的独立限流；收货人、详细地址和订单备注增加前后端一致的长度校验。
- 结算页跳转地址管理时不再将姓名、电话和完整地址写入浏览器临时存储；旧版商城 Token 启动后立即移除本地持久副本，只在当前页面内存保留到换发 HttpOnly Cookie 完成；支付密码在成功、失败和关闭弹窗时统一清空。
- 最新完整验证为后端 374/374、商城 69/69、管理后台 103/103；后端正式打包、公开商城/团队 H5 双生产构建、公开包边界扫描、后台生产构建和差异格式检查通过。本版无新增数据库迁移，未连接线上、未执行迁移、未发布；线上继续为 `1.0.62` 和 13/13 迁移。

- 2026-08-21 完成 `1.0.64` 本地候选：发货与退货物流公司名称统一为最多 50 字；商城多个并发 401 只提示并跳转登录一次；后台解除会员登录锁定增加影响说明和二次确认；商品编辑、上架和下架按钮增加 `shop:product` 权限可见性保护。
- 佣金追回、结算批次和结算明细三张表补齐 `tenant_id`，所有查询、汇总、锁定、更新及插入均以服务端当前租户为准，禁止调用方写入其他租户；佣金待结算排除查询同时校验批次和明细租户。新增迁移 `V202608211700__commission_finance_tenant_isolation.sql`，待发布迁移总数增至 15 项；历史单客户数据归入默认租户 1。
- 后端完整回归 372/372、商城 68/68、管理后台 103/103 通过，后端正式打包、公开商城与团队 H5 双生产构建、公开包边界扫描、后台生产构建及迁移计划校验通过；未连接线上、未执行迁移、未发布，线上继续为 `1.0.62` 和 13/13 迁移。

- 2026-08-21 完成 `1.0.63` 本地候选：待结算佣金扣减在余额判断前锁定代理账户；支付宝迟到支付退款成功后，在交易父单或历史订单持久保存独立退款标记，异步重复通知与同步补单均直接确认已有结果，不再重复调用退款或形成渠道重试循环。
- 新增迁移 `V202608211430__late_payment_refund_marker.sql`，上线时须在后端切换前备份、执行并登记，迁移总账将由 13 项增至 14 项。后端完整回归 371/371、专项回归 19/19 和迁移计划校验通过；未连接线上、未执行线上迁移、未发布，线上继续为 `1.0.62`。

- 2026-08-21 正式发布 `1.0.62`：将 1.0.57～1.0.62 累计的多商户财务闭环、租户隔离、支付退款并发、售后状态机、秒杀库存、后台高风险操作确认、接口校验及安全加固上线；公开商城、团队 H5、后台继续使用三个独立入口和同一后端。
- 发布构建身份为业务 Git `403981408a7638c16e8dc483720beba93133a5e9`、`20260821-1308-1.0.62`，发布及备份工具最终提交为 `cf5dfa3`；后端包 SHA-256 为 `8c7334159755b459016046530f2c9457b21576f97dec0a0244d8bf9e825af0c7`。
- 发布前完整备份 `/opt/lingqimall/backups/full/20260821_131643` 的数据库、程序、配置、上传文件、公开商城、团队 H5、后台和系统服务配置均已通过 SHA-256、压缩包及内容清单复验；本版无新增数据库迁移，迁移总账保持 13/13 成功。
- 发布前后核心计数保持 `45:2:18:27:58:13450.00:5:6:3`。线上三份版本清单、入口与资源均为 1.0.62；公开商城、团队 H5、后台和首页接口返回 200，未登录后台接口 401，公网 Actuator 404，公开商品敏感字段门禁通过，四项服务与 Redis 正常、后端健康 `UP`、当前进程新增错误为 0。

- 2026-08-19 正式发布 `1.0.56`：公开商城与团队 H5 已按域名使用两个独立静态根目录，公开商城为 `lingqimall.com`，团队 H5 为 `www.lingqimall.com`，后台仍为 `lingqimall.com/admin/`；两个前端共用后端账号和接口，但公开构建不包含团队奖金、邀请、转账或提现页面。
- 发布构建身份为 Git `6c530268205e6d73399e978d31b1b3e783b8af0c`、`20260819-1436-1.0.56`；后端 324/324、商城 66/66、后台 93/93 通过，后端打包、公开商城、团队 H5 与后台生产构建全部成功。后端包 SHA-256 为 `9c84d76a8e41afb2e46d349d1dffdd3f9b57e4df682a9e2b59a72d97167627ec`。
- 发布前完整备份 `/opt/lingqimall/backups/full/20260819_144536` 已通过哈希、数据库压缩包和文件清单校验；8 个新增迁移按顺序执行，迁移总账现为 13/13 成功。发布前后核心计数保持 `45:2:18:27:58:13450.00:5:6:3`，未删除、清空或改写现有会员、订单、奖金、资产、商品、分类及管理员数据。
- 线上三份版本清单分别确认 `storefront-public`、`team-h5`、`admin` 均为 1.0.56；公开商城、团队 H5、后台、两域名首页接口及三端入口资源均返回 200，后端健康为 `UP`，Nginx、MySQL、Redis 和分销服务均为 active。后台未登录接口为 401，公网 Actuator 为 404，公开商品经营敏感字段门禁通过，发布进程新增错误为 0。

- 在新目录 `/Users/minmatemp/Documents/mall-swarm-app-h5` 和分支 `codex/app-h5-split` 建立独立产品副本；原商城 `main` 目录和线上 1.0.55 未修改、未覆盖、未重复发布。
- 同一前端源码生成 `storefront-public` 与 `team-h5` 两个独立构建。公开包只保留商品、购物车、订单、支付、物流、售后和普通账号；团队 H5 单独承载邀请、关系、业绩、奖金、转账与提现。公开构建新增强制边界扫描，发现团队文字、路由或接口即失败。
- 后端新增公开注册和公开个人中心，公开注册即使伪造邀请码也不建立关系；新增一次性团队关系确认。数据库增加 `team_opt_in`，公开账号支付不会激活代理、关系快照、业绩或佣金，只有主动进入团队业务的账号才进入奖金链路；历史一体化会员迁移时保持原资格和历史数据。
- 私有部署模板改为公开商城、团队 H5、后台三个独立静态目录和至少两个不同域名，公开域名无法猜路径读取团队包；证书、CORS、预检和部署后验收同步覆盖团队域名。
- 拆分基线验证曾完成后端 278/278、注册/首单边界专项 10/10、商城前端 64/64、公开/团队双生产构建、公开包边界扫描、部署策略 7/7 与隔离工作流。加入多商户、结算成本治理及统一售后结算后，最新完整验证为后端 293/293、商城前端 66/66、管理后台 82/82，并通过公开商城、团队H5、后台三端生产构建；新增数据库迁移尚未执行，真实支付、短信、物流及资金链路尚未在隔离环境验收，因此本候选不得直接替换线上版本。
- 在同一拆分核心上新增可选多商户基座：商户商品按订单成本快照乘有效数量结算，平台服务费为0；货款经过待结算、可提现、提现冻结、发票审核和人工打款，退款可冲回或形成欠款。商品新增普通商城、复购区、报单区及团队奖金模式边界；现有商品保持平台自营，客户定制奖金未配置时拒绝下单。
- 商户结算成本改为受控财务条款：历史订单始终使用下单快照，后续改价不回写旧订单；新增或修改商户归属、商品成本及SKU成本必须同时具有商品与财务管理权限、填写原因，并记录前后值和操作人。提现回归覆盖待收发票禁打款、驳回先抵退款欠款、剩余余额退回、重复驳回/重复打款及已打款后驳回均被拒绝。
- 商户商品上架改为完整审核闭环：商户账号只能维护本商户商品，新增或编辑后保持下架草稿；提交后进入待审核，平台在独立审核页核对商品及各SKU的销售价、结算价，通过后自动上架，驳回时必须填写原因并保持下架。已上架商品必须先下架才能编辑，任何编辑都会重新进入草稿并再次审核；旧的单SKU写接口禁止改动商户商品，结算价高于销售价或商户已停用时不能提交/通过；历史订单保留原下单结算价快照，新订单使用最新审核通过的结算价。新增迁移 `V202608171930__merchant_product_review_workflow.sql`，尚未执行。本轮最新完整验证为后端295/295、商城66/66、管理后台84/84，公开商城、团队H5和后台生产构建通过；未连接线上、未修改线上数据、未发布，线上仍为1.0.55。
- 拆分版浏览器验收覆盖公开首页、注册、商品、购物车、结算、个人中心，以及团队H5首页、邀请、业绩和钱包；修复HttpOnly会话恢复误跳登录和公开测试基线内部文案。完整证据见 `document/audits/2026-08-17-app-h5-acceptance/REPORT.md`。多商户迁移 `V202608171600__multi_merchant_foundation.sql` 尚未执行，独立商户门户、真实支付/退款/物流和后台真实账号写入仍未验收，本版未发布。
- 2026-08-18 补齐商户结算周期与保证金：商户默认等待和商品覆盖均为0～365天，订单锁定当时规则，确认收货时固化“售后截止后再等待”的预计可结算时间；到期任务直接筛选真正可释放记录，避免长期风险商品或在途售后阻塞后续订单。商户工作台可查看本商户账户、结算、保证金、提现并自行申请提现；平台可冻结/解冻保证金，保证金与提现冻结分账，解冻先抵退款欠款。全量验证为后端299/299、商城66/66、管理后台86/86，公开商城、团队H5和后台生产构建通过；新增迁移 `V202608181030__merchant_settlement_cycle_and_deposit.sql` 尚未执行，本次不连接线上、不发布，线上仍为1.0.55。
- 2026-08-18 完成多商户 P0 收口：移除商户对平台经营看板的访问，商户商品、服务地址、货款和提现全部按认证会话归属隔离；平台地址仅在显式共享时供商户选用。停用商户事务内下架商品，并在公开列表、详情、库存和下单继续校验；默认结算周期变化只退回依赖默认周期的商品审核。补齐商户主体、合同、银行/开票资料、应缴保证金和资料版本；保证金支持货款转入及平台登记线下到账，未缴足禁止商品审核和提现。提现以数据库唯一请求号持久防重并固化收款资料快照，网络重试不会重复冻结；退款按可提现、保证金、欠款顺序冲回。新增迁移 `V202608181730__merchant_security_and_finance_closure.sql`，尚未执行。最终验证后端304/304（公共8、分销296）、商城66/66、后台89/89，三端生产构建、后端打包、私有部署工作流和Compose校验7/7全部通过；未连接线上、未修改线上数据、未发布，线上仍为1.0.55。
- 2026-08-18 将商户状态完善为账号、经营、履约、提现、结算、保证金、审核和退出八个独立控制维度；平台“全面冻结”会禁止新销售、商品修改、提现和新货款结算，但允许商户继续登录处理历史订单与售后。订单、售后、服务地址、资金、提现事件和经营统计全部使用认证会话绑定的 `merchant_id` 进入服务端 SQL 条件，前端传入其他商户编号不会扩大查询范围；商户后台改为独立工作台和专属菜单，不再出现平台总看板、会员等级或团队奖金。
- 2026-08-18 新增商户资金总账和提现事件链，每次订单待结算、到期释放、退款冲回、提现冻结/退回/打款和保证金变化都保留不可覆盖的流水及变化后余额，可从首笔流水核对当前账户；订单、售后和售后私密凭证同时增加租户与会话商户归属校验，猜测其他商户文件路径也会被拒绝。新增迁移 `V202608181930__merchant_controls_ledger_and_workbench.sql`，尚未执行。当前继续阻止跨商户商品一次合并结算；“父订单+商户子订单”已固化为下一阶段架构边界，必须与支付、退款、发货、售后和结算状态机一起实施，不能只删除现有限制。浏览器验收证据见 `document/audits/2026-08-18-multi-merchant-flow/REPORT.md`，设计见 `document/MULTI_MERCHANT_ARCHITECTURE.md`。最新完整验证为后端308/308、商城66/66、后台89/89，公开商城、团队H5和后台生产构建通过；未连接线上、未执行迁移、未发布，线上仍为1.0.55。
- 2026-08-18 补齐商户清退、提现异常和资金核对闭环：平台确认“已退出”前，后端重新聚合在售商品、未完成订单、开放售后、待结算、非终态提现和全部资金余额，任一未清即拒绝退出；提现新增付款处理中、付款失败重试、商户撤回、风控冻结/恢复、已打款归档，冻结金额只允许释放一次。新商户账户从期初流水开始，后台“账本对账”比较六类账户余额与末笔资金总账，并以业务唯一索引阻止重复入账。新增迁移 `V202608182130__merchant_exit_withdrawal_reconciliation.sql`，尚未执行。后端312/312（公共8、分销304）、后台91/91和后台生产构建通过；未连接线上、未修改线上数据、未发布，线上仍为1.0.55。父订单+商户子订单仍为下一阶段，当前跨商户商品继续分开结算。
- 2026-08-18 完成跨商户一次支付的父子单闭环：新增 `dms_shop_trade` 支付父交易，现有商城订单作为单商户履约子单；平台自营和多个商户商品可一次提交、一次余额扣款或一次支付宝支付，各子单独立运费、库存、发货、售后、退款、货款和奖金。支付先完整校验全部子单再统一入账；父子金额或支付号不一致整笔拒绝，重复回调不重复处理；待付款取消任一子单关闭全部联合子单并释放库存，支付宝退款按原父交易支付号执行。商城和后台明确展示联合支付号，后台搜索、商户会话及SQL仍只返回本商户子单。
- 同轮全量回归定位并修复旧单商户余额支付的并发返回旧状态问题：第二个并发请求等待行锁后不再命中加锁前的订单快照，两个请求都返回已支付且资金只扣一次；支付宝跳转失败会保留并重试原待付款交易，不再生成重复订单。新增迁移 `V202608182245__multi_merchant_parent_trade.sql`，当前仅通过本地迁移命名和校验和计划检查，尚未执行。最终验证后端320/320（公共8、分销312）、商城66/66、后台92/92，公开商城、团队H5和后台生产构建通过；未连接线上、未修改线上数据、未发布，线上仍为1.0.55。
- 2026-08-18 继续完善多商户履约：平台订单页新增联合支付聚合详情，集中核对父交易、子单金额、已完成退款和全部商户子单；商户账号只保留本商户子单的正常发货、售后和客服备注，父交易汇总、平台取消、人工退款及团队奖金保持平台专属。商户履约切换为平台接管或冻结后，页面与后端同时关闭发货和售后处理；修复Excel批量发货未逐行校验商户归属的问题，防止猜测订单号操作其他商户订单。无新增迁移，完整验证后端324/324（公共8、分销316）、商城66/66、后台93/93及后台生产构建通过；未连接线上、未执行迁移、未发布，线上仍为1.0.55。

### 1.0.65（2026-08-21，待发布）

- 第七轮 22 项安全报告已逐项回到当前源码与产品架构复核。当前客户隔离模型为每客户独立部署、独立数据库和密钥，部署内入驻商户由认证会话绑定的 `merchant_id` 隔离；默认租户 1 不接受客户端切换，因此不能把未来多客户共享数据库 SaaS 的要求直接判定为当前高危漏洞。未来若改共享数据库，必须整体迁移所有业务表、约束、索引和测试。
- 修复成立项：看板佣金统计和最新记录增加显式租户范围；佣金、资产流水查询关键词增加服务端长度校验；经营报表导出增加每分钟 5 次独立限流；收货人最多 30 字、详细地址最多 200 字、订单备注最多 500 字，并由前后端共同校验。
- 完成浏览器隐私加固：结算草稿不再保存姓名、手机号和完整地址；旧版 Token 加载后立即从 `localStorage` 删除，只在换发 HttpOnly Cookie 前短暂保留于页面内存；支付密码和设置密码表单在成功、失败或关闭弹窗时立即清空。
- 支付宝回调已确认具备官方 SDK RSA 验签、`app_id`/金额/状态核对、行锁和幂等；支付验证使用后端重算权威金额；模拟支付默认及生产配置均关闭且有启动门禁，未按误报重复改写。订单号还受完整随机雪花尾号和会员归属校验保护。
- 完整验证为后端 374/374、商城 69/69、管理后台 103/103，失败、错误和跳过均为 0；后端正式打包、公开商城与团队 H5 双生产构建、公开包边界扫描、后台生产构建和差异格式检查通过。无新增数据库迁移，未访问线上、未修改线上数据、未发布，线上仍为 `1.0.62`。
- S09 业绩排行的全量加载与重复聚合保留为规模化性能技术债；需要客户真实代理规模和统计频率后改为数据库聚合或按日增量快照，不在安全补丁中擅自改变奖金统计口径。

### 1.0.64（2026-08-21，随 1.0.65 待发布）

- 统一物流公司名称边界：后台发货、ERP 发货与会员退货均最多 50 字，退货 DTO、业务服务及商城输入框采用同一规则，避免前端允许提交而后端或数据库拒绝。
- 商城请求层增加并发 401 跳转锁，同一批失效请求只清理一次展示会话并发出一次登录提示；路由完成后释放锁，后续真实会话过期仍可正常跳转。后台解除会员登录锁定增加二次确认；商品编辑及上架/下架按钮增加 `shop:product` 权限判断，路由和后端原有权限校验继续保留。
- 给 `dms_commission_clawback`、`dms_commission_settlement_batch`、`dms_commission_settlement_item` 增加租户字段、组合索引和显式作用域 DAO；覆盖读取、列表、汇总、待抵扣行锁、状态更新及新增写入，跨租户读取返回空、跨租户更新影响 0 行、伪造其他租户写入直接拒绝。新增迁移 `V202608211700__commission_finance_tenant_isolation.sql`，旧数据按现有单客户部署边界归入租户 1。
- 完整验证为后端 372/372、商城 68/68、管理后台 103/103，失败、错误和跳过均为 0；后端正式打包、公开商城与团队 H5 双生产构建、公开包边界扫描、后台生产构建、迁移命名/顺序/校验和计划及差异格式检查通过。本版尚未发布，线上仍为 `1.0.62`，线上迁移仍为 13/13。

### 1.0.63（2026-08-21，随 1.0.64 待发布）

- 修复待结算佣金并发扣减检查：`subtractUnsettledCommission` 先使用 `SELECT ... FOR UPDATE` 锁定账户，再执行余额判断和带下限条件的原子扣减，与已结算佣金追回路径保持一致。
- 修复支付宝在本地超时关单后才成功扣款的本地幂等闭环：交易父单和历史订单增加 `late_refund_flag`；退款成功后必须写入标记，后续异步通知和同步查询直接返回成功。渠道退款失败或本地标记保存失败继续返回失败，使用稳定退款号安全重试。
- 未复写 `pay_type`，支付渠道统计和后续退款判断保持原语义；售后 `audit` 的可选订单级锁未加入，因为当前产品禁止同订单存在多个处理中售后，售后单锁和订单状态条件已覆盖并发，额外锁只会扩大锁竞争。
- 新增迁移 `V202608211430__late_payment_refund_marker.sql`。后端完整回归 371/371、佣金锁/支付宝重复通知/同步补单/租户范围专项 19/19、正式打包、迁移计划和差异格式校验通过；本版尚未发布，线上仍为 `1.0.62`，迁移仍为 13/13。

### 1.0.62（2026-08-21，已发布）

- 逐项复核 C15～C17、H27～H35、M30～M45 与 L6～L11 报告并修复成立项：佣金和商品全部直接读写增加显式租户条件；秒杀 Redis 库存键缺失时回退数据库权威扣减，不再把未初始化误判售罄或用调用方旧库存懒初始化；秒杀预约、未付款释放和退款回补增加业务审计记录。
- 完善后台高风险操作与售后体验：会员启停、售后审核增加二次确认；代理提现和商户提现最终打款均要求当前管理员密码；人工退款前端限制不超过剩余可退商品金额；多退货地址订单要求按地址拆分售后，确认退货支持填写实际验收备注。
- 完善输入、配置和前端容错：地址上限为每会员 20 个并通过会员行锁防并发突破；物流公司统一 50 字、运单号统一 4～64 位格式；Redis 超时和连接池可按环境配置；MyBatis 关闭二级缓存并将本地缓存收紧为单语句；商品、购物车、订单、结算及轮播图片加载失败统一显示占位图；未评价订单查询改用 `NOT EXISTS`。
- 复核确认报告 C15 的双重回补在当前代码不成立：已付款待发货秒杀取消只回补一次活动库存，重复取消被状态机拒绝，并增加精确数量回归。提现请求号唯一约束、发货与结算/售后并发锁、分页上限、后台商品权限、手机号唯一性、移线防环及购物车会话保持均已有后端或现有实现保护，不重复改写。
- 当前产品采用“每个客户独立数据库、单部署内多商户”边界，因此 C16 所述把客户租户混放同库不属于现有部署模型；若未来选择共享数据库 SaaS，必须作为完整架构迁移为所有业务表补充租户键、约束、索引和数据迁移，不能只给少数表补字段。`COALESCE` 历史空租户兼容和模糊搜索性能优化同样需结合正式数据清洗及规模指标单独实施。
- 完整后端回归 366/366、商城 67/67、管理后台 101/101，失败、错误和跳过均为 0；后端正式打包、公开商城与团队 H5 双生产构建、公开包边界扫描及管理后台生产构建全部通过。
- 已发布构建身份为 Git `403981408a7638c16e8dc483720beba93133a5e9`、`20260821-1308-1.0.62`；完整备份 `/opt/lingqimall/backups/full/20260821_131643` 已复验并新增覆盖团队 H5，后端包 SHA-256 为 `8c7334159755b459016046530f2c9457b21576f97dec0a0244d8bf9e825af0c7`。本版无数据库迁移，迁移保持 13/13；Redis 生产超时/连接池及 MyBatis 缓存策略同步写入外部配置并随整版回滚。发布前后核心数据完全一致，三端版本、页面、资源、接口、权限、敏感字段、服务健康和启动日志门禁全部通过。

### 1.0.61（2026-08-21，随 1.0.62 发布）

- 复核 C13～C14、H16～H22、M10～M25 与 L5 报告并修复成立项：订单全部直接读取增加显式租户条件，删除可绕过业务状态机的通用订单状态更新；支付、取消、确认收货增加订单行锁和幂等状态判断，售后审核、退货物流、确认收货增加合法来源状态条件，防止并发请求跨状态推进。
- 完善支付与退款闭环：订单超时关闭后支付宝迟到的成功付款会使用稳定退款号自动原路退回，退款失败则明确让网关重试；外部退款本地入账前重新锁定并读取最新订单；佣金追回锁定代理资金账户，并同时校验总佣金、已结算佣金和可用余额下限，任一扣减失败整笔事务回滚。
- 完善运行安全与可用性：实时订单 SSE 增加全局及单账号连接上限、有限异步发送队列和退出清理，避免慢连接占满请求线程；登录口令、支付密码、转账、余额支付、提现和后台人工资产操作使用更严格的独立限流；后台会话恢复使用单次并发请求，避免多个路由同时重复调用身份接口。
- ERP 基座继续保留订单推送、失败记录、告警和人工重试，但在尚未实现真实供应商协议映射时禁止启用对应适配器，不再出现“界面显示已启用、实际只能失败”的假可用状态。真实对接仍须客户确定 ERP/物流厂商并提供正式接口文档、账号、签名规则和测试环境。
- 复核确认无需按报告改写的条目：售后完成与库存回补在同一事务内已先锁订单并有状态条件；订单与售后超时均已有 0～365 天配置；订单号不可仅凭猜测越权；上传文件已有魔数识别、图片解码、尺寸限制、重编码和安全路径；OAuth 配置类不存在；外部退款失败状态可使用相同业务号人工幂等重试。盲目后台自动重试外部退款需结合客户支付渠道的订单查询与重试规则设计，不作为本轮安全修复直接启用。
- 完整后端回归 357/357、商城 66/66、管理后台 97/97，失败、错误和跳过均为 0；后端正式打包、公开商城与团队 H5 双生产构建、公开包边界扫描及管理后台生产构建全部通过。无数据库迁移，未连接或修改线上，线上仍为 `1.0.56`。

### 1.0.60（2026-08-21，随 1.0.62 发布）

- 修复支付校验配置接口匿名可读：`GET /payment/checkVerify` 现在必须是已登录商城会员，HttpOnly 商城会话继续自动生效；匿名请求在进入配置服务前即被拒绝，并增加独立限流。
- 代理注册/资料修改/调级/移线/旧记录审批、后台会员手机号修改/登录密码重置/调级、人工余额增减等高风险写接口统一启用服务端 Bean Validation；补齐手机号、身份证号、等级、审批状态、原因、管理员密码、金额两位小数、幂等 UUID 和目标会员等边界，原有服务层资金及业务校验继续保留。
- 全局分页参数 `pageSize` 统一限制为 1～100，避免遗漏单个控制器造成无界查询；部署变量 `CORS_ORIGINS` 正式接入后端，仅接受明确 HTTPS 来源（本机开发地址除外），跨域携带 HttpOnly Cookie 时拒绝通配来源，并保留标准预检请求。
- 加密挑战端点保持登录前公开，因为登录和注册必须先取得一次性公钥挑战，但增加每 IP 120 次/分钟限流；复核确认外部直连请求不能用 `X-Real-IP`/`X-Forwarded-For` 改写限流身份，只有本机反向代理来源才采信代理头，并补齐正反向回归。
- 完整后端回归 350/350，商城 66/66、管理后台 96/96，失败、错误和跳过均为 0；专项与邻近安全回归 58/58、后端正式打包、公开商城与团队 H5 双生产构建、公开包边界扫描及管理后台生产构建全部通过。无数据库迁移，未连接或修改线上，线上仍为 `1.0.56`。

### 1.0.59（2026-08-21，随 1.0.62 发布）

- 复核 C1～C12 遗留清单并修复成立项：父交易 `dms_shop_trade` 的按编号、按交易号、加锁读取、支付更新、关闭与新增全部绑定当前租户；订单列表不再固定租户 1；订单新增和全部状态写入同时执行显式租户条件，不再只依赖全局拦截器；售后凭证引用统一兼容历史空租户数据。
- 新订单触发的会员卡级刷新从“重新计算所有会员”缩小为订单关系快照涉及的本人及上级，奖金仍按支付前等级和订单关系快照计算。补充批量佣金第 N 笔失败整批回滚、同一佣金重复结算不二次入账、跨租户父交易和订单写入拒绝、合法本租户读写成功等回归。
- C3 属于未来“多个客户共用同一数据库”的 SaaS 架构迁移，当前产品是每客户独立部署/独立数据库，部署内多商户使用 `merchant_id` 隔离，未单独给全局代理账户补一个不完整的租户字段；C7 实际订单号为时间戳加完整雪花编号并有归属校验，C8 当前无优惠券模块，C9 历史佣金必须保留下单快照，C11 比例为不可编辑的固定策略且总比例受审计上限保护，C12 已有状态行锁和数据库唯一约束，因此未按误报改变正确业务。
- 本地全量后端回归 338/338，失败、错误和跳过均为 0；专项验证修复前可稳定复现跨租户父交易读取及订单列表固定租户 1，修复后同一用例 3/3 通过。无数据库迁移，未连接或修改线上，线上仍为 `1.0.56`。

### 1.0.58（2026-08-19，随 1.0.62 发布）

- 对用户提供的系统性审查报告逐项回到当前源码核验：当前客户隔离模型是“每客户独立部署、数据库和密钥”，部署内多商户再以认证会话绑定的 `merchant_id` 隔离，并不是共享数据库的多客户 SaaS；报告引用的多项类名、表名和状态含义不属于当前版本。现有余额扣减、佣金入账、库存、父子订单、支付回调和外部退款继续使用原子条件更新、事务、行锁、稳定业务号及唯一约束，未按误报重复改写。
- 修复仍然成立的边界：ERP 发货回调必须提交租户编号，先按租户与供应商读取启用配置并恒定时间校验回调令牌，鉴权后才在该租户上下文发货，不再固定租户 1；后台登录跳转只允许规范站内路径，拒绝绝对、协议相对、反斜杠、控制字符及编码绕过。
- 后台会话完全以 HttpOnly Cookie 和服务端 `/me` 为准，启动时主动删除旧版 `token`、用户和权限本地缓存；权限及商户身份只保存在当前内存并在路由放行前重新取自服务端。提现输入拒绝科学计数法，余额转账预览与提交统一使用同一规范化手机号。
- 补齐退货售后恢复路径：平台同意退货后，客户在寄回前仍可取消；已提交退货物流可修改；物流单号前后端限制为 4～64 位字母、数字、下划线或短横线。新增默认 7 天、可设 0～365 天的待寄回超时任务，0 表示关闭自动超时，超期申请自动关闭并释放订单继续履约。
- 本地全量验证：后端 333/333、商城 66/66、管理后台 96/96，失败和跳过均为 0；完整 Maven 打包、公开商城与团队 H5 双构建、公开包边界扫描、管理后台生产构建及 `git diff --check` 全部通过。无新增数据库迁移，未连接线上、未修改线上数据、未发布；线上仍为 `1.0.56`。

### 1.0.57（2026-08-19，随 1.0.62 发布）

- 完成多商户基座审查报告的逐项证据复核，修复批量佣金结算吞异常、待结算佣金并发负数、业绩贡献误读实时关系、移线祖先团队人数过期、冻结邀请人仍可绑定、邀请码无失败上限和交易号取模碰撞风险。
- 财务、退款、售后和公司分账的 DAO 读写全部以当前租户和归属订单为服务端强制条件，并增加跨租户反向验证；售后退款使用售后单号作为稳定退款号，手工业绩刷新与定时任务共用分布式锁。
- 报告中关于支付宝回调竞争/空指针、余额支付、限购、提现、秒杀、关系环、定时关单等结论经当前代码验证已有行锁、条件更新、关系快照或分布式锁保护，未重复修改。架构建议不伪装成已验证漏洞。
- 本地验证：重点链路 46/46，完整后端工程 329/329（公共模块 8、分销模块 321），完整 Maven 打包和差异格式检查通过。无新增数据库迁移，未连接线上、未修改线上数据、未发布；线上仍为 `1.0.56`。

### 1.0.56（2026-08-19，已发布）

- 完成 Daybreak Blue 本地防御性审计后的代码修复：短信验证码改为 Redis Lua 原子校验并消费；支付密码成功校验遇到并发锁定时重新确认并拒绝资金操作；同一会员并发下单先锁定会员行，防止限购快照竞争；后台新增会员时，开通分销或指定等级必须额外具有 `distribution:manage`。
- 导入代理和订单的审计操作人只取后台认证会话，不再信任客户端传入身份；后台不存在账号及锁定账号也执行等价密码校验，缩小登录响应时序差异；幂等指纹优先绑定稳定会员编号，避免登录令牌刷新改变幂等身份；数据库账号密码取消基础默认值，生产启动拒绝 root 和已知弱口令，并删除已被安全客户部署入口替代的旧 Compose 环境文件。
- 升级 Spring Boot 3.5.16、Spring Cloud 2025.0.3、Spring Data Commons 3.5.13、Jackson 2.21.6、Netty 4.1.137.Final、HttpClient 5.6.4、Log4j 2.26.1、POI 5.5.1、dom4j 2.2.0 和 Bouncy Castle 1.85.2，排除支付宝 SDK 带入的旧 Bouncy Castle、dom4j 和 commons-logging；两端 npm 官方审计均为 0，后端 208 个运行依赖经 OSV 复核未命中已知漏洞。
- 当前测试服务器已独立完成两项运行环境加固：后端切换为专用非 root 用户和严格 systemd 沙箱；每日完整备份定时器已启用，新备份通过 SHA-256、压缩包和内容清单校验。Nginx 精确屏蔽 Actuator 已更新并复验，线上程序包、静态资源、数据库和业务数据均未替换或改写。
- 页面复验修复注册页进入时未主动加载图形验证码及电脑端表单过度分散，首页、分类、搜索、商品详情、未登录拦截和手机端无横向溢出均通过；截图与完整边界见 `document/audits/2026-08-16-full-chain/REPORT.md`。
- 上述安全基线随后叠加 App/团队 H5 拆分与多商户闭环，最终发布验证为后端 324/324、商城 66/66、管理后台 93/93、后端打包和三端生产构建通过；8 个新增迁移已执行并登记。真实支付宝、真实短信、真实物流/ERP、隔离秒杀压测、客户复购结算制度、隔离恢复演练和可选微信支付仍需对应外部条件后验收，不影响本次基座程序发布结论。

### 1.0.55（2026-08-14，已发布）

- 完成第一阶段品牌与版权整理：根项目和管理后台 README 全面改为灵启商城当前真实架构、能力和维护入口，删除原开源项目公众号、社群、演示地址、教程、旧架构宣传、三份无人引用的上游参考文档及 51 张已无引用的旧项目展示图片，不影响历史发版记录和实际业务文档；删除内容仍可从 Git 历史恢复。
- 增加长沙灵启软件开发有限公司版权体系：新增 `COPYRIGHT.md` 和根目录 `NOTICE`，明确本公司对自主新增及实质性修改内容享有相应权利，同时保留 Apache License 2.0、相关上游归属和第三方许可证边界；法律声明只进入源码仓库和技术交付包，不要求在商城消费者页面或页脚展示。
- 商城前端和管理后台 npm 包改为 `lingqi-mall-storefront`、`lingqi-mall-admin` 并标记为私有包，增加公司作者信息；Maven 根父工程坐标改为 `lingqi-mall`，项目名称、容器维护者、结构化日志项目标识和历史 SQL 文件说明改为灵启品牌。Java 包名、数据库、接口和业务流程本版不改，避免品牌整理引入运行风险。
- 本地验证：后端完整回归 275/275、商城 63/63、管理后台 80/80 通过，后端打包及商城、后台生产构建通过；本版无新增数据库迁移。
- 已发布构建身份为 Git `2d450627c097ceb19fb66da4332391de565870ed`、`20260814-2009-1.0.55`；后端包 SHA-256 为 `fa24789bee007d7b7380bf984340f30f9e9410e500db43ecdf3c8760ce409a29`，商城资源 `/assets/index-BB2RucRD.js`，后台资源 `/admin/assets/index-DaWNRMMD.js`。发布前完整备份 `/opt/lingqimall/backups/full/20260814_201509` 的数据库、程序、上传文件和关键配置已通过哈希及内容复验；历史5项数据库迁移均正确识别为已执行，没有新增或重复迁移。线上版本清单、商城、后台、首页接口及资源均正常，后端健康为 `UP`，Nginx、MySQL、Redis和分销服务均为active，后台未登录接口按预期为401，公网Actuator为404，公开商品响应经营敏感字段命中为0，当前进程新增错误为0。发布前后核心计数保持 `45:2:18:27:58:13450.00:5:6:3`，未删除、清空或改写现有测试会员、订单、奖金及资金数据。

### 1.0.54（2026-08-14，已发布）

- 修复后台管理员纵向越权：账号新增、编辑和密码重置必须用当前登录管理员密码二次确认；普通管理员只能授予自己已有的已登记权限，不能创建超级管理员、注入未知权限、修改同级/更高权限账号或重置其密码，权限变化后立即撤销目标账号旧会话。
- 将订单发货与退款权限彻底拆分：发货继续使用 `shop:order`，后台退款和待发货“取消并退款”改为 `shop:aftersale`；售后审核、确认退货和人工退款的操作人只取服务端认证上下文，不再信任客户端传入的姓名或ID。
- 管理员连续输错密码的锁定改为15分钟自动恢复，锁定、账号不存在和密码错误统一返回相同提示，避免账号状态枚举，同时保留失败次数防暴力保护。
- 短信注册、验证码登录和找回密码发送前增加一次性图形验证码；发送冷却改为Redis原子占位，并补充发送/验证双层接口限流。转账等敏感验证码强制发送到当前会员绑定手机号；第三方错误尝试达到上限后只锁住错误尝试，不再删除受害人的正确验证码。
- 售后凭证上传必须绑定当前会员仍可申请售后的真实订单；待提交图片按会员限制为最多12张、合计30MB并在24小时后自动清理，售后单创建后转入正式私密目录，兼容读取历史凭证，防止无订单账号和反复放弃申请造成无限文件堆积。
- 轮播图外链只允许完整HTTPS地址，拒绝脚本协议、HTTP和带用户信息的地址；新窗口强制 `noopener,noreferrer`，阻断反向控制原商城页面。
- 新增管理员层级、售后操作人、短信验证码、短信限流、售后图片配额、轮播外链及订单权限矩阵回归。后端完整回归275/275（公共7、分销268）、商城63/63、后台80/80通过，后端打包及商城、后台生产构建通过；本版无新增数据库迁移。
- 已发布构建身份为 Git `d250b6801dd44044e9ada8e62882bd0649f70536`、`20260814-0907-1.0.54`；后端包 SHA-256 为 `384027cff19234a1c19948fdc53893d4903809cd95bf618959c11a50e5255676`，商城资源 `/assets/index-BB2RucRD.js`，后台资源 `/admin/assets/index-DaWNRMMD.js`。发布前完整备份 `/opt/lingqimall/backups/full/20260814_091813` 的数据库、程序、上传文件和关键配置已通过校验；线上版本清单、商城、后台及资源均正常，后端健康为 `UP`，Nginx、MySQL、Redis和分销服务均为active，后台未登录接口按预期为401，公开商品响应未出现成本、BV、安全库存、发货地址、内部地址编号或复购配置等经营字段，当前进程启动后新增错误为0。发布前后核心计数保持 `45:2:18:27:58:13450.00:5:6:3`，未删除、清空或改写现有测试会员、订单、奖金及资金数据。

### 1.0.53（2026-08-13，已发布）

- 用户确认自本版起只使用当前 Mac 作为唯一开发与发布主机；仓库规则取消家里电脑和18:00双机交接限制，远程仓库保留为备份。远程网络不可用时允许在工作区干净且版本已核对的唯一主机继续开发，但必须保留提交、重试推送并明确记录领先提交数，仍禁止强推、覆盖和回退。
- 将未来客户私有部署从人工提醒升级为不可绕过的统一入口 `document/private-deploy/scripts/deploy.sh`，固定执行 `build → prepare → firewall → check → apply → verify`。自动生成每客户独立的MySQL、Redis和会话强密钥，拒绝覆盖已有 `.env`，强制权限600、真实域名、HTTPS证书、客户名称、非全网SSH来源及可追溯云安全组证据。
- 私有部署模板收敛为当前仓库真实可运行的Nginx、商城后端、MySQL和Redis四个必要服务；移除不存在Dockerfile或当前基座不需要的网关、搜索、监控、MongoDB、RabbitMQ、Elasticsearch和Nacos。公网只发布80/443，MySQL、Redis和8086不映射宿主机端口，数据网络为Docker内部网络；数据库维护改为SSH进入服务器后使用容器命令，不再临时开放3306。
- 后端容器强制prod、关闭模拟支付和固定短信验证码，使用非root用户、只读根文件系统与 `no-new-privileges`；Nginx统一承载前后台、API分级限流并屏蔽Actuator、Swagger和接口文档。展开后的Compose会检查端口、网络、特权模式、host网络、Docker socket和支付/短信/数据库密钥的最小容器边界，任一不合格直接停止。
- 统一部署入口在已有数据库升级前自动生成并校验备份，发现历史数据卷但数据库不可用时拒绝发布；首次安装只执行当前数据库基线，不再自动运行包含旧示例数据或清库逻辑的历史SQL。增量结构统一按版本、SHA-256和单机部署锁登记，新增长期幂等表迁移 `V202608131430__durable_idempotency.sql`；客户名称、品牌和主题使用十六进制参数化方式安全写入，不拼接SQL文本。
- 部署后自动核验所有容器、宿主机真实监听端口、内部后端健康、客户HTTPS证书以及公网Actuator/Swagger 404，并生成权限600且不含密钥的客户验收报告。备份改为容器内部执行；恢复必须显式 `--confirm` 且覆盖前自动再备份当前数据库。
- 本地验证：后端完整回归265/265（公共模块7、分销模块258）、商城62/62、后台80/80和三端生产构建通过；部署安全策略单元测试7/7、完整隔离工作流测试通过，覆盖强密钥、权限、云安全组关卡、source map拒绝、公开Redis端口拒绝、漂移镜像拒绝、密钥跨容器泄露拒绝、模拟支付/固定验证码拒绝、host网络和Docker socket拒绝；Shell语法、YAML解析、四服务集合、内部网络、仅80/443端口、环境变量完整性及数据库迁移计划均通过。当前Mac没有Docker客户端，客户私有部署时由统一入口在客户服务器强制执行真实Compose展开和容器验收，未通过时不允许部署。
- 已发布构建身份为 Git `58d5c9046be6404151c23b59c3a89952e7e57d23`、`20260813-1540-1.0.53`；后端包 SHA-256 为 `fe341fd4b4c33362afc56012aaaad4a49d351be5c227e16d03662ca66403f817`，商城资源 `/assets/index-xJMygNTb.js`，后台资源 `/admin/assets/index-Ca4ku0mY.js`。执行并登记持久化幂等迁移 `V202608131430__durable_idempotency.sql`，迁移版本、校验和和长期幂等表均复验通过。
- 首次切换在完整备份 `/opt/lingqimall/backups/full/20260813_153653` 后，被公开商品字段安全门禁发现响应仍序列化成本/BV等空字段名；发布脚本立即自动恢复1.0.50，业务数据未变化。随后将公开商品和SKU响应改为不序列化空内部字段，并补齐发货地址字段清理及2项回归测试，确认商品名称、售价和库存等合法字段保持正常后重新构建。
- 第二次发布前完整备份 `/opt/lingqimall/backups/full/20260813_154501` 已完成并复验。独立线上验收确认版本1.0.53、商城/后台及新资源200、后台未登录接口401、后端健康 `UP`，Nginx、MySQL、Redis和后端服务均为active；商品列表、首页和商品详情的成本、BV、安全库存、发货地址、内部地址编号及复购配置字段命中均为0，当前进程启动后新增错误为0。发布前后核心计数保持 `45:2:18:27:58:13450.00:5:6:3`，未删除、清空或改写现有测试会员、订单、奖金及资金数据。

### 1.0.52（2026-08-13，随1.0.53发布）

> 本节保留开发完成当时“未发布”的原始边界记录；该代码最终已包含在1.0.53线上构建中，长期幂等迁移也已在1.0.53发布时执行并校验。

- 完成仅针对本地仓库的防御性安全修复，不连接或攻击线上、不读取真实凭证、不修改线上数据：后台权限路由改为规范路径白名单并拒绝矩阵参数绕过；登录失败统一口径并执行等时密码校验；注册先验证短信再检查账号，首次账号设置增加会员行锁、仅限未初始化账号且成功后撤销旧会话。
- 商城公开商品、SKU、订单和秒杀响应统一移除成本、内部统计及资金拆分字段；公开商品详情只能读取已上架且启用商品，后台 SKU 改走独立授权接口。支付宝同步回跳增加本地待支付订单前置校验和独立限流，不能用任意订单号无限触发渠道查询。
- 资金写操作由 30 秒 Redis 占位升级为数据库持久化幂等，请求键与身份、方法、路径绑定，处理成功或提交结果不明时不会因缓存过期、服务重启或响应丢失而重复扣款；上线前必须执行版本化迁移 `document/db/migrations/V202608131430__durable_idempotency.sql`，`document/sql/20260813_add_durable_idempotency.sql` 只保留为兼容旧发布流程的同内容副本，禁止重复执行。
- 修复同一代理多笔订单并发抵扣历史欠款的竞态，欠款行进入事务后锁定；佣金和公司资金结算在锁定订单后重新核对确认收货、租户实际售后截止时间和在途售后，不再固定按 T+7 提前释放资金。
- 第三方退款改为事务后调用与可恢复状态：本地账务只冲减一次，支付宝调用使用稳定售后编号；渠道失败保留“渠道退款处理中”，后台只重试外部退款，不重复冲减余额、奖金、业绩或库存，渠道成功后再完成订单收口。
- 私有化部署模板移除 MySQL、Redis、Nacos、RabbitMQ、MongoDB、Elasticsearch、监控和后端服务的公网端口，只保留 Nginx 80/443，MySQL 如需本机维护仅绑定 `127.0.0.1`；取消所有应用共享整份 `.env`，改为按服务注入最少变量，支付和短信密钥只进入实际使用它们的分销服务。
- 生产默认关闭模拟支付和固定短信验证码；只有显式 `local`/`test` 环境允许测试开关，其他环境启动时执行安全阻断。同步更新幂等与技术维护手册，明确数据库持久化、退款中间状态和后台 SKU 接口。
- 本地验证：后端完整回归 263/263，商城 62/62，管理后台 80/80，两端生产构建通过；两端 `npm audit --offline` 共检查 589 项依赖，已知漏洞均为 0。Docker 客户端在本机不可用，私有化 Compose 已通过 YAML 解析、服务数量、端口和环境变量静态断言，但仍需在正式交付机执行 `docker compose config`。本版未发布、未执行数据库迁移、未修改线上程序或业务数据，线上继续为 1.0.50。

### 1.0.51（2026-08-13，随1.0.53发布）

> 本节保留功能完成、造数和沙盘执行当时的原始版本边界；1.0.51程序能力最终已包含在1.0.53线上构建中，按用户要求保留的奖金测试数据未在发版时清理。

- 按用户要求在当前测试商城基座长期保留奖金全景核对数据：44 个 `bonus_` 会员、18 张订单、27 条奖金、5 条追回/抵欠流水、4 笔退款，覆盖 1～8 级、四档董事、同级压缩、隔层无限代、无推荐人、部分/全额退款、T+7 自动结算、已消费奖金欠款及未来奖金抵欠款。全部场景断言与净资金拆分校验通过，详细清单见 `document/audits/2026-08-13-persistent-bonus-fixtures.md`；造数前后数据库快照 `/opt/lingqimall/backups/bonus-fixtures/20260813_111211`、`/opt/lingqimall/backups/bonus-fixtures/20260813_111611` 均通过 SHA-256。本次按用户要求保留全部测试业务数据，只为触发真实 T+7 任务重启后端一次，没有替换程序包、静态资源或版本标记，线上程序仍为 1.0.50，本地 1.0.51 仍待发布。
- 经明确授权完成测试商城基座清库与奖金沙盘验收：清理前完整备份 `/opt/lingqimall/backups/full/20260813_102957` 和最终零业务数据备份 `/opt/lingqimall/backups/full/20260813_105135` 均通过数据库包、文件配置包和 SHA-256 复验。最终客户会员、启用代理、订单、售后、退款、奖金、追回、余额流水、提现、会员/后台会话及操作日志全部为 0，总余额、冻结、累计入账和累计出账全部为 0；保留 2 个全零禁用系统资金账户、3 个后台管理员、5 个商品、6 个分类、4 条迁移和 6 条配置版本记录，商品库存还原、销量归零，秒杀与复购保持默认关闭。
- 使用隔离测试会员和后台人工加余额跑通真实接口沙盘：3 张订单、2 笔余额支付、4 笔分次退款、3 条奖金和 7 条追回记录，覆盖支付前级别冻结、30%直推奖、5%董事团队分红、禁止自奖、成本/奖金/剩余商品款守恒、重复支付、双请求并发、待付款取消、30%+30%+40%累计退款、全额退会和业绩冲回；所有断言通过后已二次清库。详细证据见 `document/audits/2026-08-13-foundation-bonus-validation.md`。
- 真实沙盘发现公共模块 `IdempotentAspect` 未进入正式应用装配：相同请求号当时仍由订单行锁和状态机保证只扣款一次、只发一组奖金，但 Redis 第一层没有立即拒绝。现改为排在 `RedisAutoConfiguration` 之后的正式自动配置并登记清单，新增真实配置顺序测试；修复后的临时待发布后端重新跑完整沙盘，确认相同请求号被拒绝、Redis 幂等键落地。该修复仅进入本地待发布代码，线上后端仍保持 1.0.50 原包及哈希，未静默覆盖。
- 售后申请新增图片凭证：会员最多上传 6 张、单张不超过 5MB，服务端按真实图片内容校验并转换，随机文件名按会员目录隔离；凭证不进入公开商品图片目录，会员只能读取本人凭证，具有售后权限的后台人员可在审核弹窗查看。服务器私密目录默认为 `/opt/lingqimall/uploads/private`，文件权限不向系统组和其他用户开放。
- 后台“商城资料与客服”新增客户交付预检：集中检查正式品牌与 Logo、经营主体、信用代码、经营地址、客服渠道、营业执照公开、协议与售后规则、发退货地址、商品分类、测试内容、备案、支付、短信以及秒杀/复购客户定制规则；ERP和真实物流轨迹作为可选集成单独提示，不把测试商城基座误判为线上故障。
- 新增可插拔真实物流轨迹框架和会员订单轨迹接口。未配置服务商时继续只展示系统真实持有的物流公司、运单号及外部查询入口，不虚构运输节点；后续接入快递100、快递鸟或承运商直连时只需新增供应商适配器并配置 `SHOP_LOGISTICS_TRACKING_PROVIDER`，不改订单主流程。
- 新增秒杀并发写入验收工具及操作指南：支持多会员同时起跑、幂等键、重复下单、订单唯一性、库存超卖和延迟指标检查；工具强制 HTTPS、独立测试环境及显式写入确认，不输出会员会话。因当前未提供隔离环境的多会员会话和活动下单参数，本版只交付工具，尚未执行真实写入压测。
- 同步完善《用户操作手册》《技术维护/接口说明手册》和客户交付清单，并更正旧手册中的售后起算及特殊奖金模式枚举。真实支付宝/微信资金、提现继续由用户后续验收；本版代码无数据库结构迁移，但已按本次授权完成测试业务数据清理与余额沙盘，并在验收后恢复零业务数据基座。
- 本地验证：后端完整回归 248/248（分销 243、公共模块 5）、奖金/钱包定向回归 59/59、幂等与自动配置专项 5/5、商城回归 62/62、管理后台 80/80，两端生产构建和秒杀工具帮助命令均通过。当前代码只提交推送为待发布版本，线上程序继续为已验收的 1.0.50；线上数据库已经是本次验收后的零业务数据基座。

### 1.0.50（2026-08-13，已发布）

- 新增完整《用户操作手册》和《技术维护/接口说明手册》：前者按会员端与运营后台整理 67 项实际操作流程，不写代码；后者按当前 1.0.50 代码整理系统结构、接口地址、主要入参/出参、核心业务逻辑、配置、测试、发布和故障定位，并在仓库 README 增加入口。
- 更正旧版 API 指南中管理后台仍使用浏览器本地 Bearer Token 的过时说明，统一为当前 `admin_session` HttpOnly Cookie、`X-Admin-Client` 写请求来源校验及旧 Token 一次性迁移口径；仅修改文档，不改变会话实现。
- 将后台订单发货弹窗的“物流公司”由自由输入框改为可搜索、可清空的标准下拉选择，覆盖顺丰、京东、中通、圆通、申通、韵达、极兔、邮政、EMS、德邦等常用承运商，避免同一物流公司出现多种写法。
- 删除商城订单详情中“售后入口期限及具体截止时间以规则为准”的笼统提示，已发货等订单不再额外展示这段文案；实际“申请售后”入口仍由服务端配置、订单状态和截止时间准确控制，售后规则本身不变。
- 物流单号、发货数量、拆包/合箱、批量 Excel 发货及后端保存接口保持不变；已有订单和历史物流名称不改写，不执行数据库迁移。
- 管理后台完整回归 79/79、商城完整回归 62/62，两端生产构建均通过。发布构建身份为 Git `bdc6324f42c0de2ef178511e817d8c87122c7e50`、`20260813-0950-1.0.50`；商城资源 `/assets/index-B-SWRqPM.js`，后台资源 `/admin/assets/index-Dx1AQSbs.js`。
- 本版只原子替换商城与后台静态资源和版本标记，不重启后端、不执行数据库迁移。发布前完整备份 `/opt/lingqimall/backups/full/20260813_094816` 的数据库、程序、上传文件和关键配置已通过哈希及内容复验；发布前后核心计数均为 `8:2:31:6:15:79532.00:5:6:3`。
- 线上二次验收确认商城、后台、首页接口均为 200，后台未登录身份接口按预期为 401，分销服务健康 `UP`，Nginx、MySQL、Redis 和后端服务均为 active；后端包哈希继续与 1.0.49 一致，未删除、清空或改写业务数据。

### 1.0.49（2026-08-13，已发布）

- 将 `RedisLock` 从普通配置类的条件 Bean 调整为正式 Spring Boot 自动配置，并明确排在 `RedisAutoConfiguration` 之后加载，解决 1.0.48 在真实启动顺序中条件判断过早、Bean 仍未注册的问题。
- 新增与生产一致的自动配置顺序测试：先由 Spring Boot 基于 `RedisConnectionFactory` 创建 `StringRedisTemplate`，再断言 `RedisLock` 唯一存在；同时验证测试环境排除 Redis 自动配置时应用上下文仍可正常启动。
- 修正 1.0.48 发布脚本日志验收的管道误判：先完整提取新进程日志，再匹配锁组件、Redis、启动及通用错误；发现任一错误必须触发回滚，不再用可能被 `pipefail` 反转的快速匹配。
- 定向上下文与锁测试 6/6、后端完整回归 241/241（分销 236、公共模块 5）及生产构建通过。补丁未执行数据库迁移，不改变售后、奖金或订单规则。
- 发布构建身份为 Git `30007b69710e0a8b5234b464ce607a8f2f876c99`、`20260813-0843-1.0.49`；后端包 SHA-256 为 `c5f199a7bba9ebdf9b56d9ac3fc97ae5da900aff6a3b5e8c9102a029e0a9963a`，商城资源 `/assets/index-DcroI4nh.js`、后台资源 `/admin/assets/index-DNP3TKL5.js`。
- 首次执行在备份 `/opt/lingqimall/backups/full/20260813_084819` 后、停服前因脚本仍检查 1.0.48 清单而退出；修正后首次切换因日志字节起点记录过早，将旧 1.0.48 停止前最后一次定时任务错误误算为新版本错误，脚本按设计自动恢复 1.0.48，备份 `/opt/lingqimall/backups/full/20260813_084914` 保留。两次均未修改数据库数据。
- 调整为旧进程完全退出后记录日志起点，再次发布成功。最终发布前完整备份 `/opt/lingqimall/backups/full/20260813_085215` 的数据库、程序、上传文件和关键配置已通过哈希与内容复验；商城、后台、首页、商品接口均为 200，后台未登录身份接口按预期为 401，分销服务健康 `UP`，Nginx、MySQL、Redis 均为 active。
- 独立二次验收以当前进程 PID 对应的启动日志为边界：分布式锁相关错误 0、全部新增 ERROR 0；待处理奖金、ERP、团队变更和超时订单队列均为 0，到期佣金与资金分配结算均为 0。核心计数保持 `8:2:31:6:15:79532.00:5:6:3`，秒杀、复购及活动数保持 `0:0:0`，4 条数据库迁移登记及校验和完整，未删除、清空或重置任何数据。

### 1.0.48（2026-08-13，已发布但锁修复未生效）

- 修复多实例定时任务分布式锁组件未进入应用扫描范围的问题。Redis 服务虽然正常，但奖金计算、超时待支付订单关闭、冷静期结算、ERP 重试、团队关系变更和汇总任务此前会为防重复而安全跳过；本补丁显式注册 `RedisLock`，继续保留 Redis 异常时宁可跳过、绝不重复结算的保护。
- 发布前只读核对：线上没有待处理奖金任务、ERP 任务、到期未支付订单或待执行团队关系变更；补丁启用后不会补跑这四类历史任务。4 条待结算佣金继续遵守确认收货满 7 天且无在途售后的既有规则，不改变售后期限或奖金业务口径。
- 新增 Redis 配置上下文回归测试，并保留锁获取成功、已有其他实例持锁及 Redis 异常安全跳过测试。定向测试 5/5、后端完整回归 241/241（分销 236、公共模块 5）及生产构建通过。
- 构建身份为 Git `b0c563a5bc03e8deaf600cafb84a0d0113a8e8fb`、`20260813-0833-1.0.48`；发布前备份 `/opt/lingqimall/backups/full/20260813_083744` 已完整校验。版本清单、商城、后台、核心接口、数据库和四项服务均正常，核心计数保持 `8:2:31:6:15:79532.00:5:6:3`。
- 独立验收发现真实应用启动后仍持续记录“分布式锁组件不可用”：普通配置类的 `@ConditionalOnBean` 在 Redis 自动配置之前判断，导致修复未生效。任务继续安全跳过，没有重复结算或误处理；该版本不作为分布式锁缺陷的最终验收版本，由 1.0.49 修正。

### 1.0.47（2026-08-13，已发布）

- 完成商城基座全链路产品与测试审计并保存截图证据：修复未接入真实物流轨迹时错误显示“运输中”的误导文案，订单只展示已发货和系统实际持有的发货时间，真实轨迹明确以承运商查询结果为准；APP 下载页移除固定 `LQ` 图标，改为读取后台客户 Logo；后台人工退款统一使用服务端售后截止时间及 0 天关闭开关，不再自行按下单后 7 天判断。审计报告见 `document/audits/2026-08-12-full-commerce/REPORT.md`。
- 新增默认关闭的限时秒杀基座：后台维护活动商品/SKU、价格、PV、库存、限购和时间；前台提供独立秒杀入口与结算，订单明确标记为秒杀订单。
- 秒杀并发链路采用应用限流、Redis Lua原子防重复与扣库存、数据库活动库存二次原子校验、商品/SKU实物库存最终扣减及数据库唯一约束；Redis异常时降级为本机限流和数据库原子扣减，不以缓存作为最终库存真相。
- 新增默认关闭的会员复购商城：支持按完成首单会员、代理及以上或全部注册会员设置准入；商品可分别进入普通/复购渠道，复购价、复购PV、复购限购及SKU覆盖配置与普通订单隔离，禁止混单。
- 秒杀与复购奖金分别可选“不参与奖金”“沿用普通奖金”“客户定制”。基座默认不参与；客户定制规则未开发验收时禁止对应下单，避免误发奖金。
- 待付款秒杀订单取消/超时会释放活动和实物库存；已支付退货按实际退回数量回补库存但不开放同场重复参与。普通商城流程默认保持不变。
- 新增幂等迁移 `V202608121635__flash_sale_and_repurchase_foundation.sql` 及设计说明 `document/FLASH_SALE_AND_REPURCHASE_DESIGN.md`。秒杀与复购开关发布后保持默认关闭，活动数为 0，不改变普通商城现有订单流程。
- 本地验证：后端全量 240/240（分销 235、公共模块 5）、商城回归 61/61、管理后台 77/77；后端打包、迁移计划校验及两端生产构建均通过。
- 修复管理后台浏览器标签页仍使用旧蓝紫色图标的问题：后台默认图标与商城前台统一为灵启品牌标志，登录页和后台主界面加载客户商城资料后会同步切换为客户配置的 Logo。
- 后台订单末尾新增“客服备注”：与客户下单留言独立，支持添加、修改、清空、备注关键词搜索和订单导出；列表只显示单行摘要，完整内容在弹窗编辑。客服备注不返回会员端，每次实际修改均记录修改前后内容和管理员身份。新增幂等迁移 `V202608121715__shop_order_service_remark.sql`，本版本发布前必须先完成字段迁移。
- 发布构建身份：Git 提交 `69f319e17c4f6bd87cfe5407aac46d93f667e41b`，构建编号 `20260813-0811-1.0.47`；商城资源 `/assets/index-DcroI4nh.js`，管理后台资源 `/admin/assets/index-DNP3TKL5.js`。
- 发布前完整备份 `/opt/lingqimall/backups/full/20260813_082525` 的数据库、程序、上传文件、应用配置、systemd、Nginx 和证书配置已通过哈希及内容清单复验；预检备份 `/opt/lingqimall/backups/full/20260813_082407` 也完整保留。
- 两项新增迁移已执行并登记，迁移校验和与仓库一致；发布前后核心计数均为 `8:2:31:6:15:79532.00:5:6:3`。商城、后台、首页、商品接口、版本清单和四项系统服务验收通过，未删除、清空或重置测试及业务数据。
- 独立二次验收发现 Redis 分布式锁组件自 8 月 12 日起未进入应用扫描，定时任务因此安全跳过。该问题不是 1.0.47 迁移引入，已单列为 1.0.48 补丁，避免在同一版本号下静默覆盖。

### 1.0.46（2026-08-12，已发布）

- 调整管理后台“全部订单”展示：不再在商品名称栏展示已取消、已拒绝的售后卡片及申请原因；历史售后记录继续保留在服务端，不删除、不改写，仅不进入订单主表摘要。
- 将“履约状态”与“售后 / 退款”拆分：订单继续准确显示待付款、待发货、已发货、已完成或已关闭；仅展示仍在处理的售后和已完成退款，退款结果明确区分全额退款与部分退款，避免部分退款覆盖整单状态。
- 调整操作层级：处理售后和发货保持为当前业务主操作，奖金去向、取消订单、后台退款收纳到“更多操作”，减少表格横向拥挤。
- 本版不修改售后期限、起算方式或客户业务规则，不执行数据库迁移，不删除或清空测试与业务数据。管理后台回归 70/70、两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260812_162633`，数据库包和文件配置包哈希复验通过。发布前后核心计数均为 `8:2:31:6:15:79532.00:5:6:3`。
- 线上验收：版本标记 1.0.46；Git 提交 `8c93af6e79c882847ee619e9ecbb49449f9b5144`；商城资源 `/assets/index-ZGvOC9Go.js`、后台资源 `/admin/assets/index-53kDn82Z.js`；两端版本清单、首页、商品接口、服务健康和四项系统服务均通过，未发现新增致命日志。
- 首次发布的页面验收请求遇到 HTTP/2 流异常，脚本自动恢复 1.0.45 且回滚后版本、服务、核心数据和备份哈希全部通过；随后将验收连接固定为 HTTP/1.1，第二次发布成功。

### 2026-08-12 线上程序基线更正（状态核验，不是新发布）

- 2026-08-12 只读核验确认：线上商城入口为 `/assets/index-DpLltb-G.js`，管理后台入口为 `/admin/assets/index-OLCrXQqr.js`，两份文件的 SHA-256 与 Git 提交 `2917c62` 工作区内 2026-08-11 构建产物完全一致。
- 因此，`document/DAILY_LOG.md` 中 2026-08-11 各开发条目末尾的“未发布线上”只能表示该条记录写入当时未执行发布，不能继续代表当前线上程序状态；当前线上程序代码应以本节核验结果为准。
- 1.0.45 发布前已连接服务器核验：8 月 11 日迁移对应的表和索引已存在，但迁移登记表尚未初始化；本次通过幂等迁移补齐登记，未重复创建已有结构。
- 从 `1.0.45` 起，商城构建自动生成 `/version.json`，后台构建自动生成 `/admin/version.json`。发布时通过 `RELEASE_GIT_COMMIT` 和 `RELEASE_BUILD_ID` 写入构建身份，后续不再只依赖易遗漏的人工资源文件名判断版本。

### 1.0.45（2026-08-12，已发布）

- 修复售后期限口径：后台可明确选择“签收后起算（推荐）”或“下单后起算（兼容）”，并设置 0～365 天；0 天关闭客户自助申请入口但保留后台人工售后，服务端统一计算订单截止时间，前端不再自行固定按下单后 7 天判断。
- 修复佣金只读查询污染后台操作日志、商城表单无障碍、经营资质首页入口、Android 测试包公开展示、后台会话长期保存在浏览器本地存储等问题。
- 管理后台会话调整为默认 12 小时并迁移到 HttpOnly Cookie；ECharts 改为图表页面按需异步加载；测试中的废弃 `MockBean` 替换为当前 API。
- 本地验证：后端全量测试 233/233（分销 228、公共模块 5）、商城回归 58/58、管理后台 Vitest 68/68，后端冷启动构建及两端生产构建通过；ECharts 约 566KB 的包已隔离为图表页面异步资源，不再进入后台首屏主包。
- 数据库迁移 `V202608111205__tenant_config_versions_and_operation_log_index.sql` 和 `V202608121235__tenant_after_sale_window.sql` 均已成功执行并登记；售后字段默认采用“签收后 7 天”，支持后台改为 0～365 天或切换起算方式。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260812_155457`，数据库包与文件配置包哈希复验通过。发布前后核心计数均为 `8:2:31:6:15:79532.00:5:6:3`，未删除、清空或重置测试及业务数据。
- 线上验收：版本标记 1.0.45；Git 提交 `c13029b6dcd4fd9a458aeeaf063678bdb582ff5a`；商城资源 `/assets/index-ZGvOC9Go.js`、后台资源 `/admin/assets/index-M4tmy3JQ.js`；商城、后台、首页和商品接口正常，未登录后台身份接口按预期返回 401，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active，未发现新增致命错误日志。

### 1.0.44（2026-08-10，已发布）

- 奖金任务成功落库继续严格限定为仅允许从“处理中”状态转换，禁止失败或其他终态被重新标记为成功；本版未重复修改已生效的状态机规则。
- 为会员支付密码设置与校验、余额转账、余额支付补齐关键业务日志，日志只记录业务编号、会员/订单标识和金额，不记录密码、手机号或密钥。
- 后台“会员全景”新增支付密码锁定状态和人工解除入口；解除只清空错误次数与锁定时间，不修改原支付密码，并写入后台操作审计。后台管理员登录锁定继续使用“后台账号管理”的原有解除入口。
- 本版仅替换分销后端、管理后台和版本标记；不执行数据库迁移，不删除、重置或批量改写业务数据。发布前后核心计数保持 `8:2:31:6:15:79532.00:5:6:3`。
- 本地验证：后端全量测试 192/192、最终数据库兼容定向测试 21/21、管理后台 Vitest 60/60，后端打包和管理后台生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260810_155236`，数据库和文件配置包哈希、数据库压缩包及文件包均校验通过。
- 线上验收：版本标记 1.0.44；管理后台资源入口 `/admin/assets/index-y1eOYSEn.js`，商城继续使用已验收资源 `/assets/index-CiYsUk_s.js`；商城、后台、首页和商品接口均返回 200，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active，发布后无新增错误日志。

### 1.0.43（2026-08-10，已发布）

- 商城购物车、首页、分类和商品详情统一按最新库存拦截超量加购/立即购买，并用居中短时提示准确告知最多可购买数量；订单取消、确认收货、取消售后、地址删除、购物车清理和退出登录统一使用商城二次确认组件。
- 会员全景仅统计已支付后的有效订单，未支付及未支付关闭订单不再计入；订单状态准确区分已关闭与已售后。商城电脑网页补齐与移动端一致的配置化导航和登录入口。
- 后台移除“订单与售后”父级汇总大数字，将待发货和待售后数量放入对应筛选项；工作台待处理事项同步前置。发货/退货地址强制完整省市区，会员余额调整默认显示 0 元。
- 强化资金与售后并发安全：提现金额限制两位小数和数据库精度上限，支付密码成功校验不再清除并发错误次数，售后/退款锁定订单防止重复冲销，奖金任务增加同订单幂等与数据库唯一约束。
- 发布前确认奖金任务不存在历史重复记录后执行 `20260810_bonus_task_concurrency_guard.sql`，仅新增唯一键 `uk_bonus_task_order`；未删除、合并、重置或批量改写任何业务数据。发布前后核心计数保持 `8:2:31:6:15:79532.00:5:6:3`。
- 本地验证：后端全量测试 190/190、管理后台 Vitest 59/59、商城回归 49/49，后端打包及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260810_125848`，数据库压缩包及备份哈希校验通过。
- 线上验收：版本标记 1.0.43；商城资源入口 `/assets/index-CiYsUk_s.js`、管理后台资源入口 `/admin/assets/index-VkSOELRX.js`，商城、后台、首页及商品接口均返回 200；分销服务健康 `UP`，Nginx、MySQL、Redis 均 active，发布后无新增错误日志。

### 1.0.42（2026-08-09，已发布）

- 修复商城首页、分类页、商品详情和购物车的限购校验时机：加入购物车或立即购买前即核对当前购物车数量，登录会员同时核对历史有效订单占用；快速连续点击增加商品级锁，超限时在屏幕中央显示短时准确提示，正式提交订单仍保留服务端最终校验。
- 修复“立即购买”错误合并购物车的问题：商品详情立即购买使用独立结算清单，不再加入、合并或在支付后删除原购物车商品。
- 完善后台待发货与售后工作队列：待发货、售后中及菜单分别显示待办数量；售后处理中订单从待发货队列排除并禁止人工、ERP 或表格发货，取消/驳回售后后按订单实际状态恢复，部分退款后仅处理剩余商品。
- 本版未执行数据库迁移，未删除、重置或批量改写业务数据；发布前后核心计数保持 `8:2:27:5:14:79621.00:5:6:3`。
- 本地验证：后端全量测试 173/173（公共模块 3/3）、商城回归 46/46、管理后台 Vitest 56/56，后端打包及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260809_232517`。
- 线上验收：版本标记 1.0.42；商城资源入口 `/assets/index-BQ8VQi4Z.js`、管理后台资源入口 `/admin/assets/index-t5zl8j_S.js`，商城、后台及商品接口均返回 200；分销服务健康 `UP`，Nginx、MySQL、Redis 均 active，发布后无新增错误日志。

### 1.0.41（2026-08-09，已发布）

- 后台新增“商城设置 → 商城公告”正式管理页面，支持按状态筛选、新增、编辑、公告类型、展示顺序、启用/隐藏和二次确认删除，发布后直接联动客户首页与公告中心。
- 公告标题与正文增加前后端双重必填及长度校验；删除接口执行租户隔离校验，避免越权删除其他商城数据；兼容保留历史公告的有效期字段。
- 本版未执行数据库迁移，未删除、重置或批量改写任何业务数据；发布前后核心计数保持 `8:2:24:2:8:69699.00:5:6:3`。
- 本地验证：后端全量测试 170/170、管理后台 Vitest 54/54，后端打包、管理后台与商城生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260809_220308`。
- 线上验收：版本标记 1.0.41；商城资源入口 `/assets/index-CwcGYqqW.js`、管理后台资源入口 `/admin/assets/index-BkNnx8uP.js`、商城首页、商品及公告接口均返回 200；分销服务健康 `UP`，Nginx、MySQL、Redis 均 active，发布后无新增错误日志。

### 1.0.40（2026-08-09，已发布）

- 修复商城账号成为代理前已经存在余额账户时重复建账导致的余额接口异常：正式会员继续复用原账户并自动补齐代理关联，转正前后的余额与流水保持连续。
- 购物车增加数量、选中结算和全部结算统一提前调用服务端限购校验，避免进入支付密码阶段才发现没有购买额度；提交订单仍保留服务端最终校验。
- 商城新上传图片统一补齐 Web 可读权限，并修复存量异常图片权限；服务器 `/tmp` 权限恢复为标准安全模式，避免数据库临时文件操作异常。
- 管理后台统一装修开关布尔值和 Element Plus 单选组件写法，清除相关控制台兼容警告。
- 发布前已安全补齐两个系统资金归集账户，历史订单缺失资金分配记录已由定时任务补齐为 0 条；本次发布不再执行数据库迁移，不删除或重置业务数据。
- 本地验证：后端全量测试 169/169（公共模块 3/3）、商城回归 42/42、管理后台 Vitest 51/51，后端打包及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260809_211521`。
- 线上验收：版本标记 1.0.40；商城资源入口 `/assets/index-CwcGYqqW.js`、管理后台资源入口 `/admin/assets/index-BcLNwBkE.js`、商城首页和商品接口均返回 200；分销服务健康 `UP`，Nginx、MySQL、Redis 均 active，发布后无新增错误日志，核心数据计数保持 `8:2:23:1:7:69798.00:5:6:3`。

### 1.0.39（2026-08-09，已发布）

- 将会员商品限购校验前移到加入购物车/立即购买：首页、分类页和商品详情页会在当前操作处立即核对会员历史未关闭订单占用量与购物车同商品数量（跨 SKU），超限时不再进入支付密码流程才提示。
- 结算和提交订单继续保留服务端最终限购校验，防止并发或绕过前端；未登录用户不会因预检被误拦。
- 本版仅替换分销服务、商城与管理后台静态资源及版本标记，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：商城回归 42/42、管理后台 Vitest 51/51、后端全量测试 167/167，后端打包及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260809_201536`。
- 线上验收：版本标记 1.0.39；商城资源入口 `/assets/index-DcK8SGST.js`、管理后台资源入口 `/admin/assets/index-BFE3nJiT.js` 返回 200，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active。

### 1.0.38（2026-08-09，已发布）

- 统一移动端提交表单的校验反馈：收货地址保存改为必填字段旁的准确错误提示并自动定位，结算、密码、余额转账、提现和账号设置改用短时顶部提示，离开页面时自动清理，避免错误长期滞留或隐藏在页面底部。
- 商城收货地址删除改为站内统一二次确认弹层；移除粘贴识别后的常驻提示；分类图片上传改为浏览器正确生成 multipart 边界并补齐失败重试提示。
- 本版仅替换商城与管理后台静态资源及版本标记，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：商城回归 41/41、管理后台 Vitest 51/51、后端打包及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260809_141835`。
- 线上验收：版本标记 1.0.38；后台资源入口 `/admin/assets/index-HeWoyvrs.js`、商城资源入口 `/assets/index-BxfqH3CP.js` 返回 200，商城首页与商品接口返回 200，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active。

### 1.0.37（2026-08-09，已发布）

- 修复“发货与退货地址”新增/编辑：省、市、区/县完整选择后可正常保存；联系电话前端过滤文字和非法字符，服务端同步校验手机号、座机及 400/800 服务号码。
- 发布前发现线上数据库缺少商城资料协议字段，已先执行幂等迁移 `document/sql/20260809_add_tenant_legal_profile.sql`，仅补齐 `unified_social_credit_code`、`service_hours`、`third_party_services` 三个字段，不删除或改写业务数据。
- 发布前后会员、订单、奖金、流水、余额、商品和分类核心计数一致：`6:20:0:4:59899.00:5:5`；数据库迁移与发布均在全量备份后完成。
- 本地验证：后端全量测试 170/170、管理后台 Vitest 50/50、商城回归 41/41，后端编译及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260809_101105`；线上版本标记 1.0.37，商城/后台入口、加密公钥接口、登录保护接口和分销服务健康检查通过，Nginx、MySQL、Redis 均 active。

### 1.0.36（2026-08-09，已发布）

- 商城注册提交改为居中短时反馈：5 位验证码明确提示应为 6 位，验证码错误、过期分别准确提示，成功后显示“账号注册成功”；移除容易被误认为报错的红色“已有账号，返回登录”。
- 同步上线 1.0.35 后已完成的商城订单详情、个人中心、轮播图总开关、分类删除、全站时间显示和会员全景信息优化。
- 商城会员物理字段 `username` 迁移为 `login_account`，保留全部 8 条会员及原账号值；管理员账号表不变。发布前后会员、订单、奖金、流水、余额、商品、分类和管理员核心计数一致。
- 本地验证：后端全量测试 161/161、商城前端 40/40、管理后台 45/45，后端打包及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260809_084034`；未删除或重置任何业务数据。
- 线上验收：版本标记 1.0.36；管理后台资源入口 `/admin/assets/index-BYbcVmaX.js`、商城资源入口 `/assets/index-DaXHnP64.js`、商城首页和商品接口均返回 200；分销服务健康 `UP`，Nginx、MySQL、Redis 均 active。

### 1.0.35（2026-08-08，已发布）

- 注册页移除昵称输入，登录账号统一为 4～20 位、以英文字母开头且仅支持字母、数字和下划线；手机号、账号、邀请码和验证码等错误在对应位置短时提示。
- 个人中心新增“账号设置”：昵称按 2～20 字规则维护；会员自行更换手机号需验证当前手机号与新手机号，成功后撤销旧登录会话；管理员核实客户身份后可直接代改手机号并记录原因。
- 余额转账收款人核对只展示昵称、会员编号、脱敏登录账号和脱敏手机号，服务端不再返回完整账号与手机号。
- 本版不执行数据库迁移、不删除或改写业务数据。
- 本地验证：后端全量测试 160/160、商城前端 35/35、管理后台 37/37，后端打包及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260808_211310`。
- 线上验收：版本标记 1.0.35；管理后台资源入口 `/admin/assets/index-B--KkiUP.js`、商城资源入口 `/assets/index-C2hKoEPG.js` 返回 200，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active。

### 1.0.34（2026-08-08，已发布）

- 首页轮播图管理统一按数字状态判断，避免接口返回字符串状态时启用开关显示错误。
- 当首页轮播图模块已打开但没有任何启用图片时，后台显示明确提示，避免只打开模块却没有前台内容。
- 本版不执行数据库迁移、不删除或改写业务数据；轮播图启用状态已在发布前单独备份并恢复为启用。
- 本地验证：管理后台 Vitest 37/37、后台生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260808_104141`。
- 线上验收：版本标记 1.0.34；管理后台资源入口 `/admin/assets/index-BsyoUHqy.js` 返回 200，首页接口返回 2 条启用轮播图，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active。

### 1.0.33（2026-08-08，已发布）

- 后台待发货订单新增“取消并退款”，执行原路全额退款、关闭订单、账务冲销并恢复预占库存；待付款订单继续使用普通取消。
- 本版不执行数据库迁移、不删除或改写业务数据；发布前已执行全量备份并保留回滚副本。
- 本地验证：后端全量测试 153/153、管理后台 Vitest 36/36、商城回归 26/26、后端编译和两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260808_103043`。
- 线上验收：版本标记 1.0.33；管理后台资源入口 `/admin/assets/index-DxbrUkj8.js`、商城资源入口 `/assets/index-COEMb539.js` 返回 200，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active。

### 1.0.32（2026-08-08，已发布）

- 后台订单管理新增待付款订单取消入口，取消后关闭订单并恢复预占库存。
- 客户退货物流展示物流公司、运单号，并提供外部物流轨迹查询入口。
- 修复商城视觉装修工作台主题预设/主题色单击选择不稳定，以及品牌 Logo 上传弹窗重复打开。
- 本版不执行数据库迁移、不删除或改写业务数据；发布前已执行全量备份并保留回滚副本。
- 本地验证：后端全量测试 153/153、管理后台 Vitest 35/35、商城回归 26/26、后端打包和两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260808_100650`。
- 线上验收：版本标记 1.0.32；管理后台资源入口 `/admin/assets/index-CJYdCBVj.js`、商城资源入口 `/assets/index-COEMb539.js` 返回 200，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active。

## 版本总览

### 1.0.31（2026-08-08，已发布）

- 商品与 SKU 增加安全库存阈值、低库存/缺货筛选和工作台库存风险预警；支持商品批量上架/下架及商品编辑分步导航。
- 新增发货地址与售后退货地址簿，商品可绑定地址并保存售后退货地址快照。
- 退货退款进入“待寄回→已寄回待收货→商家确认收货并退款”流程，防止重复申请、提前奖金结算和库存重复占用；保留后台超期按盒数/金额退款能力。
- 发布包含三个幂等数据库迁移：`20260808_add_product_safety_stock.sql`、`20260808_add_shop_service_addresses.sql`、`20260808_add_after_sale_return_workflow.sql`；只新增表和字段，不删除或改写业务数据。
- 本地验证：后端全量测试 153/153、管理后台 Vitest 35/35、商城回归 26/26、后端打包和两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260808_092714`。
- 线上验收：版本标记 1.0.31；管理后台资源入口 `/admin/assets/index-BDqPR6Mk.js`、商城资源入口 `/assets/index-B-jZv_Ea.js` 返回 200，分销服务健康 `UP`，Nginx、MySQL、Redis 均 active；三项迁移字段已核验。
- 首次迁移检测发现服务器此前未执行地址簿兼容迁移，应用文件尚未替换；补齐该迁移后重新发布成功，未删除或回退业务数据。

### 1.0.30（2026-08-07，已发布）

- 优化管理后台经营工作台视觉层级与信息密度：补充财务汇总关键指标、会员区域数据说明、月度新增会员标识及商品排行字段展示。
- 本版仅替换管理后台静态资源和版本标记，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：管理后台 Vitest 35/35、生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_172345`。
- 线上验收：版本标记 1.0.30，后台入口与资源入口返回 200，商城入口返回 200，分销服务、Nginx、MySQL、Redis 均 active。

> 1.0.29 已发布：补齐后台退款审计操作人，使用当前登录管理员记录操作人，不再写死为 admin；未执行数据库迁移、不删除业务数据。

### 1.0.29 发布后数据库补充（2026-08-07 16:57）

- 排查到商品列表和商城首页接口 500 的直接原因是后端已查询 `dms_shop_product.purchase_limit`，而线上数据库缺少该字段。
- 已先完成数据库备份 `/opt/lingqimall/backups/db/mall_distribution_20260807_165743.sql.gz`，再执行幂等迁移，仅新增默认值为 0 的限购字段，不删除或改写商品、订单、会员及余额数据。
- 线上 `/api/shop/products`、`/api/shop/home` 复验返回 200；本次未重新替换代码包，版本标记仍为 `1.0.29`。

## 1.0.29（2026-08-07，已发布）

- 后台退款和售后审核使用当前登录管理员的 ID、昵称/账号写入操作人字段，避免多人后台操作时日志无法区分。
- 本版仅更新管理后台静态资源和版本标记，后端及商城继续使用已验收版本；不执行数据库迁移、不删除或改写业务数据。
- 本地验证：管理后台 Vitest 35/35、生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_163808`。
- 线上验收：版本标记 1.0.29，后台资源入口 `/admin/assets/index-BJoyblCv.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

## 1.0.28（2026-08-07，已发布）

- 修正售后审核的支付宝退款调用顺序：先完成售后状态、财务退款、奖金冲销、资金归集、余额/库存处理，再调用支付宝原路退款；内部处理失败时不会提前触发外部退款。
- 本版仅更新分销服务 JAR 和版本标记，管理后台及商城静态资源继续使用已验收的 1.0.27 版本；不执行数据库迁移、不删除或改写业务数据。
- 本地验证：后端定向测试 55/55、后端打包通过；1.0.27 已验证的管理后台 35/35、商城 25/25 及两端生产构建保持不变。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_163313`。
- 线上验收：版本标记 1.0.28，后端健康 UP，商城/后台入口返回 200，Nginx、MySQL、Redis 均 active。

## 1.0.27（2026-08-07，已发布）

- 客户售后申请期限统一从下单时间开始计算 7 天；超过期限后，商城前台不再显示或受理售后申请，需由后台按实际情况处理退款。
- 后台订单管理新增“后台退款”：可选择具体商品盒数按实付商品金额比例退款，也可直接填写商品退款金额；系统校验剩余可退盒数和金额，防止重复或超额退款。
- 后台退款与普通售后审核共用售后、财务退款、奖金冲销和库存处理链路；支付宝实付订单在生产模式下会先调用退款接口，退款失败不会将售后误记为成功。
- 商品售后保障、商品默认售后说明和商城规则 FAQ 同步改为“下单后 7 天”，避免客户看到与实际规则不一致的“签收后”口径。
- 同步发布当前工作区已完成的管理后台 UI 优化；本版仅替换分销服务 JAR、管理后台和商城静态资源，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：后端定向测试 55/55、管理后台 Vitest 35/35、商城回归 25/25、后端打包及两端生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_162334`。
- 线上验收：版本标记 1.0.27；后台资源入口 `/admin/assets/index-NZbMC9It.js`、商城资源入口 `/assets/index-CHjFGkh7.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

| 版本 | 发布时间 | 核心内容 | 状态 |
| --- | --- | --- | --- |
| 1.0.37 | 2026-08-09 | 发货/退货地址校验与商城资料字段兼容迁移 | 已发布 |
| 1.0.36 | 2026-08-09 | 注册反馈、订单与个人中心体验、后台展示与会员登录账号字段统一 | 已发布 |
| 1.0.35 | 2026-08-08 | 账号注册规则、个人资料设置、手机号换绑与转账隐私核对 | 已发布 |
| 1.0.34 | 2026-08-08 | 首页轮播图启用状态提示与类型兼容 | 已发布 |
| 1.0.33 | 2026-08-08 | 待发货订单取消并原路退款 | 已发布 |
| 1.0.32 | 2026-08-08 | 订单取消入口、退货物流轨迹查询、视觉装修交互修复 | 已发布 |
| 1.0.31 | 2026-08-08 | 安全库存预警、发货/退货地址簿、退货物流确认退款流程 | 已发布 |
| 1.0.30 | 2026-08-07 | 经营工作台 UI 与经营数据展示优化 | 已发布 |
| 1.0.29 | 2026-08-07 | 后台退款审计记录当前登录管理员 | 已发布 |
| 1.0.28 | 2026-08-07 | 售后支付宝退款调用顺序安全补丁 | 已发布 |
| 1.0.27 | 2026-08-07 | 下单后7天售后期限与后台按盒数/金额退款 | 已发布 |
| 1.0.26 | 2026-08-07 | 风控与审计后台操作日志时间格式统一为空格分隔 | 已发布 |
| 1.0.25 | 2026-08-07 | 首页瞬断失败可重试、结算地址摘要化、购物车功能保留 | 已发布 |
| 1.0.1 | 2026-08-03 | 注册必填与手机号校验、协议返回、注册草稿、购物车与“我的”适配；后端注册规则同步 | 已发布 |
| 1.0.2 | 2026-08-03 | 后台登录页品牌居中、删除多余登录说明、修复加密登录重复解密误报 | 已发布 |
| 1.0.3 | 2026-08-03 | 加密挑战防缓存、未登录跳转、全入口手机号校验、重复注册提示、M会员编号清理 | 已发布 |
| 1.0.4 | 2026-08-03 | 商城与后台前端重建发布（后端与数据库不变） | 已发布 |
| 1.0.5 | 2026-08-03 | 余额钱包与奖金体系解耦，非会员也可增加/扣减余额 | 已发布 |
| 1.0.6 | 2026-08-03 | 短信验证码错误次数限制 + 每手机号每日发送上限 | 已发布 |
| 1.0.7 | 2026-08-03 | 会员可调整为非会员；退款后自动取消会员资格 | 已发布 |
| 1.0.8 | 2026-08-04 | 订单奖金链路验证、商品搜索/评分/物流可视化，以及商城交互回归修复 | 已发布 |
| 1.0.9 | 2026-08-04 | 余额钱包入口与转账流程、整数金额校验、团队业绩未开通提示 | 已发布 |
| 1.0.10 | 2026-08-04 | 商城 Logo、协议模板与 FAQ、营业执照展示开关、商品售后编辑 UI | 已发布 |
| 1.0.11 | 2026-08-05 | 支付宝支付安全接入代码（默认关闭）、余额流水审计字段兼容迁移 | 已发布 |
| 1.0.12 | 2026-08-06 | 商城设置与前台首页 UI 收口、协议模板与资料占位符、首页模块显隐配置 | 已发布 |
| 1.0.13 | 2026-08-06 | 后台订单管理列表布局收窄、订单状态前移、收货信息后移 | 已发布 |
| 1.0.14 | 2026-08-06 | 企业支付宝公钥更新、支付回调验签与同步查询兜底 | 已发布 |
| 1.0.15 | 2026-08-06 | 移动网络中断时订单查询自动重试、中文错误提示 | 已发布 |
| 1.0.16 | 2026-08-06 | 后台售后申请支持取消退款、取消状态展示与回归测试 | 已发布 |
| 1.0.17 | 2026-08-07 | 手机版首页装修工作台：模块实时预览、拖动排序、分类及底部导航显隐配置 | 已发布 |
| 1.0.18 | 2026-08-07 | 手机版首页装修工作台紧凑化，减少弹窗和预览高度，降低上下滚动 | 已发布 |
| 1.0.19 | 2026-08-07 | 客户独立部署基础配置：支付/短信参数隔离、媒体文件持久化、生产监听与密钥防误提交 | 已发布 |
| 1.0.20 | 2026-08-07 | 商城视觉工作台补齐首页、分类、购物车、我的四页手机版草稿预览 | 已发布 |
| 1.0.21 | 2026-08-07 | 商城视觉工作台六项装修能力收口，颜色微调完整作用于手机预览，清理官网与社群项目边界 | 已发布 |
| 1.0.22 | 2026-08-07 | 装修草稿未保存保护、拖动排序按钮兜底、拖动状态清理与交互细节收口 | 已发布 |
| 1.0.23 | 2026-08-07 | 商城视觉工作台模块分行独立编辑，移除购物车装修预览模板 | 已发布 |
| 1.0.24 | 2026-08-07 | 首页轮播图直达管理、客户可见文案中文化、移除重复服务说明配置 | 已发布 |

## 1.0.26（2026-08-07，已发布）

- 风控与审计的后台操作日志列表和详情统一将接口时间从 `YYYY-MM-DDTHH:mm:ss` 显示为 `YYYY-MM-DD HH:mm:ss`，仅调整前端显示，不修改历史日志数据。
- 本版仅替换管理后台静态资源和版本标记，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：管理后台 Vitest 33/33、管理后台生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_152133`。
- 线上验收：版本标记 1.0.26，管理后台入口与资源入口返回 200，商城入口返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

## 1.0.25（2026-08-07，已发布）

- 修复商城首页初始化请求在移动网络瞬断后失败时的假空页面：显示明确错误状态并提供“重新加载”按钮；外壳保留默认品牌，避免未处理异常影响页面启动。
- 修复确认订单页地址加载失败时误展示完整收货表单的问题：已保存地址只显示摘要和“更换地址”，首次没有地址时才显示完整填写表单；地址新增与管理入口保持独立。
- 购物车页面、底部购物车入口、商品加入购物车和结算流程均保留，本版未移除购物车功能。
- 本版仅替换商城静态资源和版本标记，不执行数据库迁移、不删除或改写业务数据；不修改奖金、会员或 PV 业务逻辑。
- 本地验证：商城回归测试 24/24、商城生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_150007`。
- 线上验收：版本标记 1.0.25，商城入口、购物车入口、资源入口和服务健康检查通过，Nginx、MySQL、Redis 均 active。

## 1.0.24（2026-08-07，已发布）

- “首页轮播图”模块行点击后直接进入轮播图管理页面，不再先打开装修工作台再二次点击。
- 首页轮播图管理页面统一使用客户易懂的中文文案，包括“首页轮播图”“点击后动作”“轮播图片”等。
- 移除商城视觉工作台中重复且无实际编辑价值的“服务说明”独立配置行及面板；旧服务保障配置和历史数据保留兼容，不删除业务数据。
- 本版仅替换管理后台静态资源和版本标记，不执行数据库迁移、不删除或改写业务数据；不修改奖金、会员或 PV 业务逻辑。
- 本地验证：管理后台 Vitest 30/30、管理后台生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_145003`。
- 线上验收：版本标记 1.0.24，管理后台入口 200，资源入口 `/admin/assets/index-DllHyBZr.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

## 1.0.23（2026-08-07，已发布）

- 将商城视觉工作台拆成品牌视觉、首页 Banner、首页模块、分类模块、服务说明、底部导航、颜色微调七个独立模块行，每项可单独进入编辑，右侧手机预览继续统一联动。
- 购物车保留商城实际功能和底部导航，但不再作为商城视觉工作台的可装修、可隐藏手机预览模板；预览页签收口为首页、分类和我的。
- 本版仅替换管理后台静态资源和版本标记，不执行数据库迁移、不删除或改写业务数据；不修改奖金、会员或 PV 业务逻辑。
- 本地验证：管理后台 Vitest 30/30、管理后台生产构建通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_142623`。
- 线上验收：版本标记 1.0.23，管理后台入口 200，资源入口 `/admin/assets/index-D6aJ5NIB.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

## 1.0.22（2026-08-07，已发布）

- 商城视觉工作台在关闭弹窗或点击取消时检测未保存草稿，明确提示“放弃修改”或继续编辑，避免颜色、模块、分类和底部导航调整被静默丢失。
- 首页模块与底部导航增加“上移/下移”按钮作为拖动排序的可访问兜底，小屏或触控设备无需依赖拖动手势也能调整顺序；拖动结束后清理状态，避免后续操作误判。
- 本版仅替换管理后台静态资源和版本标记，不执行数据库迁移、不删除或改写业务数据；不修改奖金、会员或 PV 业务逻辑。
- 本地验证：管理后台 Vitest 29/29、商城回归 23/23、管理后台与商城生产构建通过；后端保持既有全量测试通过结果。
- 发布范围：仅替换管理后台静态资源和版本标记；发布前全量备份 `/opt/lingqimall/backups/full/20260807_115413`，未执行数据库迁移、不删除或改写业务数据。
- 线上验收：版本标记 1.0.22，管理后台入口 200，资源入口 `/admin/assets/index-D4AK9RPU.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

## 1.0.21（2026-08-07，已发布）

- “商城视觉与页面”工作台完成六项装修能力验收：Banner 管理与开关、单个分类显隐、首页模块排序与显隐、四页手机版预览、底部导航隐藏/改名/排序、实时草稿与“保存发布”生效机制。
- 颜色微调的顶部背景、价格色、强调色、分割线和按钮背景均已接入手机预览，不再只是表单占位；恢复默认按钮继续保留。
- 核对项目边界：`official-site` 是独立完整的灵启软件官网项目；`/Users/minmatemp/wechat-group-tool` 是独立的灵启社群运营平台，未移动、覆盖或合并任何项目。
- 本版仅替换管理后台静态资源和版本标记，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：管理后台 Vitest 29/29、商城回归 23/23、管理后台与商城生产构建通过，后端 `./mvnw test` 通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_114616`。
- 线上验收：版本标记 1.0.21，管理后台入口 200，资源入口 `/admin/assets/index-BuU7Mcap.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active；首次验收因错误的本地压缩包目录层级自动回滚，修正打包后重新发布成功。

## 1.0.20（2026-08-07，已发布）

- 商城视觉与页面工作台的手机预览补齐首页、分类、购物车和我的四个页面，不再显示“下一阶段”占位。
- 四个页面共用当前草稿的品牌名、Logo、主题色、颜色微调、真实分类/商品数据和底部导航配置。
- 本版仅替换管理后台静态资源和版本标记，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：后台 Vitest 28/28、管理后台生产构建通过。
- 发布范围：仅替换管理后台静态资源和版本标记；不执行数据库迁移、不删除或改写业务数据。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_111943`。
- 线上验收：版本标记 1.0.20，管理后台入口 200，资源入口 `/admin/assets/index-CNH9rEFH.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

## 1.0.19（2026-08-07，已发布）

- 私有化交付模板补齐生产服务监听、上传目录持久化、支付宝手机网站支付和阿里云短信参数。
- 支付回调地址、应用密钥和短信模板均按客户独立环境注入，不写入前端或代码仓库。
- 本版不修改奖金模型、会员模型、PV 规则，不执行数据库迁移，不删除或改写业务数据。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_102923`。
- 线上仅替换分销服务 JAR 和版本标记；管理后台与商城静态资源不变。
- 线上验收：版本标记 1.0.19，分销服务健康 UP，商城/管理后台入口 200，Nginx、MySQL、Redis 均 active。

## 1.0.18（2026-08-07，已发布）

- 手机版首页装修工作台弹窗由 1180px 调整为 1040px，手机预览高度由 640px 调整为 560px，左侧配置区同步收紧，减少上下滚动。
- 本版仅替换管理后台静态资源，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：管理后台生产构建通过、Vitest 27/27 通过。
- 发布范围：仅替换管理后台静态资源和版本标记；不执行数据库迁移、不删除或改写业务数据。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_092629`。
- 线上验收：版本标记 1.0.18，管理后台入口 200，资源入口 `/admin/assets/index-RaUTlEqg.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

## 1.0.17（2026-08-07，已发布）

- 管理后台“商城视觉与页面”改为手机版首页装修工作台：左侧配置首页模块，右侧使用真实 Banner、分类和商品数据进行手机预览。
- 首页 Banner、公告、分类、服务说明和精选商品支持显隐与模块拖动排序；分类支持整体及单个分类显隐。
- 底部导航支持拖动排序、修改名称和单项显隐；分类、购物车、我的页面预览入口已预留，当前先接入首页真实预览。
- 配置切换只修改草稿，点击“保存发布”后才写入前台配置；本版仅替换管理后台静态资源，不执行数据库迁移、不删除或改写业务数据。
- 本地验证：管理后台生产构建通过、Vitest 27/27 通过。
- 发布范围：仅替换管理后台静态资源和版本标记；不执行数据库迁移、不删除或改写业务数据。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260807_084834`。
- 线上验收：版本标记 1.0.17，管理后台入口 200，装修工作台资源入口 `/admin/assets/index-DwQJ6lVQ.js` 返回 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

## 1.0.16（2026-08-06，已发布）

- 后台订单管理的待审核售后申请增加“取消退款”入口，取消前可填写审核备注并二次确认。
- 后端新增售后状态“已取消”（状态 3）；取消只关闭待处理申请，不创建退款流水、不改余额、不恢复库存、不关闭订单，客户仍可重新提交售后申请。
- 商城订单详情和订单列表同步展示“已取消”，避免客户看到模糊的“处理中”。
- 验证：后端全量测试 150/150、后台 Vitest 27/27、商城回归测试 22/22，后台与商城生产构建通过。
- 发布范围：后端 JAR、前后台静态资源和版本标记；不执行数据库迁移、不删除或改写业务数据。
- 线上验收：版本标记 1.0.16，后端健康 UP，商城/后台入口 200，Nginx、MySQL、Redis 均 active；发布备份 `/opt/lingqimall/backups/full/20260806_173559`。

## 1.0.15（2026-08-06，已发布）

- 线上日志显示用户截图时 `/api/shop/orders` 返回 200，接口和订单数据正常；前端因移动网络瞬时中断没有恢复，直接显示 Axios 的英文 `Network Error`。
- 商城查询类 GET 请求现在只在无响应的网络传输错误时自动重试一次；写入、支付等非查询请求不重试，避免重复业务操作。
- 重试仍失败时显示“网络暂时不可用，请检查网络后重试”，保留“重新加载”按钮。
- 商城回归测试 22/22、生产构建通过；不修改订单、支付、库存或数据库。
- 线上版本标记 1.0.15，商城入口 200，后端健康 UP，Nginx/MySQL/Redis 均 active；发布备份 `/opt/lingqimall/backups/full/20260806_172249`。

## 1.0.14（2026-08-06，已发布）

- 企业支付宝公钥已更新为 APPID `2021006180653344` 对应的接口加签公钥，最近一次支付宝同步回跳签名验证通过。
- 支付宝异步通知继续执行验签、应用、金额和交易状态校验；同步回跳增加服务端订单查询兜底，仅 `TRADE_SUCCESS` 才幂等更新商城订单。
- 后端测试 149/149 通过；线上后端健康 UP，商城/后台入口 200。
- 刚才的测试订单已通过支付宝服务端查询确认并标记为已支付；未手工改写订单支付结果。
- 发布范围：仅后端支付处理与版本标记；未执行数据库迁移，未删除或改写业务数据。
- 发布备份：`/opt/lingqimall/backups/full/20260806_164520`。

## 1.0.13（2026-08-06，已发布）

- 后台订单管理列表收窄布局：订单状态前移到订单编号之后，收货信息后移到物流和下单时间之后。
- 商品、规格、金额、物流及操作列宽度收窄，表格单元格内边距减少，降低横向滚动压力。
- 侧边菜单与面包屑中的“订单处理与售后”统一简化为“订单管理”，订单与售后功能入口、接口与权限保持不变。
- 验证：后台 Vitest 27/27、生产构建通过；线上后台入口 200，后端健康 UP，Nginx/MySQL/Redis active。
- 发布范围：仅管理后台静态资源及版本标记；未执行数据库迁移，未删除或改写业务数据。
- 发布备份：`/opt/lingqimall/backups/full/20260806_160140`。

## 1.0.12（2026-08-06，已发布）

- 商城设置与首页 UI 收口：Banner 归入“商城设置”，手机版首页预览按前台层级展示；首页搜索按钮恢复胶囊圆弧。
- 首页分类模块、底部分类入口支持独立显隐；首页服务说明条默认隐藏并提供可选开关。
- 协议、隐私、售后及 FAQ 默认模板补充平台规则、退款冲正和风控条款；经营主体与客服资料通过占位符自动带入前台正文。
- 本地验证已通过：商城回归 21/21、后台 Vitest 27/27、商城/后台生产构建通过。

发布范围：仅商城与管理后台前端静态资源及版本标记；不执行数据库迁移、不删除或改写业务数据。
发布备份：`/opt/lingqimall/backups/full/20260806_153906`。
线上验收：版本标记 1.0.12，商城/后台入口 200，后端健康 UP，Nginx、MySQL、Redis 均 active。

---

## 1.0.1（2026-08-03 12:49～12:55）

### 更新内容

- 商城前端：注册页必填标记与逐字段错误提示；中国大陆 11 位手机号统一校验；用户名改为注册必填（2～64字符）；协议页返回保留表单内容（密码与验证码不落盘）；购物车名称两行展示、图片裁切、件数按总数量计算、新增清空二次确认；“我的”页面全屏适配。
- 后端：同步强制用户名必填与长度、登录密码 6～32 位、昵称 64 字上限；保留敏感字段加密、未加密载荷拒绝、HTTPS、会话与端口收口基线。
- 管理后台：本版无新增业务功能，随三端来源同步重建发布。
- 自动化测试：119 项全部通过。

### 本版未修复

- 运营协议正文未配置真实主体资料，线上协议页仍显示“暂未配置”。
- 数据库仍保留 2 个内部系统账户。
- 微信/支付宝支付、ERP 正式对接未包含。
- Android/iOS 安装包未包含。

### 待验证

- 真实邀请码 + 真实短信注册全流程。
- iPhone/Android 真机购物车与“我的”页面视觉效果。
- 正式支付渠道的重复点击、重复回调、退款、奖金与 7 天结算全链路。
- ERP 正式接入后的多包裹、合并包裹、导入幂等。

---

## 1.0.2（2026-08-03）

### 更新内容

- 管理后台登录页：商城 LOGO 与标题改为在白色登录区域内居中展示。
- 删除登录页“请使用已授权的管理员账号登录”说明。
- “商城品牌与界面”帮助说明补充：品牌名同时控制管理后台登录页标题。
- 修复后台加密登录请求被重复解密、一次性安全凭据被重复消费后误报“安全请求已失效，请重新提交”。
- 自动化测试：120 项全部通过。

### 本版未修复 / 待验证

同 1.0.1（协议正文、内部账户、正式支付、ERP、APP 安装包、真机验证等延续项）。

---

## 1.0.3（2026-08-03 17:05）

### 更新内容

- 修复商城与后台偶发“页面安全组件已更新”：一次性加密挑战防缓存、Axios 请求头兼容、后端从原始请求读取加密头。
- 未登录访问购物车/个人中心统一提示并跳转登录页；会话失效自动退出并保留原目标地址。
- 商城、后台、后端全入口统一 11 位中国大陆手机号校验；客服电话等座机字段除外。
- 重复手机号、用户名注册提示拆分为面向用户的明确文案。
- 删除 `M000000xx` 会员编号的生成、解析、查询和展示，统一使用登录账号；数值主键仅作内部关联键。
- 自动化测试：125 项全部通过。

### 本版未修复 / 待验证

同 1.0.1（协议正文、内部账户、正式支付、ERP、APP 安装包、真机验证等延续项）。

---

## 1.0.4（2026-08-03 21:57）

### 更新内容

- 商城前台与管理后台前端按 v1.0.4 预置发布包重建并发布，前端入口文件哈希与预置校验值完全一致。
- 后端服务与数据库保持不变（v1.0.4 为前端版本）。
- 线上版本由 1.0.3 升级为 1.0.4。

### 本版未修复 / 待验证

同 1.0.3（延续项不变）。

---

## 1.0.5（2026-08-03 22:10）

### 更新内容

- 余额钱包与奖金体系解耦：尚未进入奖金体系的商城账号也可由后台直接增加/扣减余额，不再要求先成为“会员（进入奖金体系）”。
- 会员全景管理“调整余额”对全部账号开放（仍要求财务处理权限 + 当前管理员密码二次验证）。
- 余额账户、余额流水、会员列表余额、会员详情余额、商城钱包余额全部改为按用户维度读取。
- 数据库迁移：`dms_member_asset_account.agent_id`、`dms_member_asset_flow.agent_id` 允许为空；余额账户增加 `(user_id, asset_code)` 唯一约束。
- 应会员要求补录余额：账号 15915626716 增加 50000 元（计入余额流水与操作日志）。
- 自动化测试：126 项全部通过（新增“非会员余额钱包”测试）。

### 本版未修复 / 待验证

- 延续项：正式支付、ERP、协议正文、内部账户、APP 安装包、系统更新重启。
- 新增待验证：非会员余额在商城钱包页、余额流水页和审计页的实际展示与使用。

---

## 1.0.6（2026-08-03 22:16）

### 更新内容

- 短信验证码增加错误次数限制：单个验证码连续错误 5 次立即作废并提示重新获取，防止 6 位验证码被暴力枚举。
- 覆盖全部短信业务：注册、短信登录、找回密码、提现、余额支付、交易密码。
- 短信发送增加每手机号每日上限（默认 10 条，`sms.daily-limit-per-phone` 可调），防止短信资费被恶意刷取。
- 注册、短信登录、找回密码统一走同一个校验通道，行为一致且全部纳入限次保护。
- 自动化测试：131 项全部通过（新增验证码限次与每日上限测试）。

### 本版未修复 / 待验证

- 延续项同 1.0.5。
- 新增待验证：短信限次在真实短信链路下的体验（连续输错 5 次后需重新获取）。

---

## 1.0.7（2026-08-03 22:34）

### 更新内容

- 后台支持把已进入奖金体系的会员调整为“非会员”（会员手工调级 → 选择“非会员”）。
  - 取消时完整下级团队自动移交原直属上级（无上级则成为根节点），团队不散。
  - 历史订单、历史业绩、历史奖金和余额钱包全部保留，不重算、不转移。
  - 有未结算奖金、未结算订单资金归集或退款追回欠款时不允许取消，提示先处理。
- 退款后自动取消会员资格：首单支付激活的会员，退款审核通过后名下已无任何有效支付订单时，自动退回非会员（同样执行下级移交与未结算保护）；后台手工授予的推广资格不因退款撤销。
- 自动化测试：134 项全部通过（新增取消会员资格、下级移交、未结算保护测试，并更新退款回归预期）。

### 发布说明

- 首次发布时发布脚本存在一处入口文件路径拼接错误，自动回滚保护立即生效、服务全程正常；定位修复后重新发布成功。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260803_223401`。

### 本版未修复 / 待验证

- 延续项同 1.0.6。
- 新增待验证：取消会员资格在真实业务下的场景验证（有下级时自动移交、未结算时被拦截、退款自动取消的端到端流程）。

---

## 1.0.8（2026-08-04 12:50）

### 问题原因

- 手机截图仍显示已经在 1.0.3 删除的 `M000000xx` 会员编号，说明页面保留了发布前加载的旧 JavaScript；旧页面继续打开按需加载的“邀请”页面时，可能引用已经替换的旧分块，因此点击后无响应。
- “注册用户”身份标签、“我的订单-全部”入口和带规格商品跳详情的行为，均存在于首次提交的前端代码快照中；1.0.4 重建商城前端时被再次打包发布，并非后续业务版本新增。
- 结算页右上角“切换”会离开确认订单页进入地址管理，返回后组件重新创建，未提交的备注自然丢失。

### 更新内容

- 普通商城账号不再显示“注册用户”身份标签，也不再展示旧推广识别码；只有已进入有效会员体系的账号才显示实际会员等级。
- 删除“我的订单”标题右侧无效的“全部”入口，保留按订单状态进入列表的功能入口。
- 个人中心“邀请”在网页端使用完整页面导航，确保重新获取最新入口资源；原生 APP 仍使用应用内路由。
- 增加页面版本自检：页面首次打开、重新聚焦或从后台回到前台时检查当前入口，发现新版本后自动完整刷新，避免长期运行发布前旧分块。
- 首页和分类页“立即加购”直接选择第一个有库存的有效规格加入购物车；无规格商品直接加入，全部规格缺货时明确提示。
- 结算页收货地址在当前页面展开和收起，不再跳转地址管理页，已填写备注及其他结算信息保持不变。
- 购物车删除和结算增加二次确认：弹窗展示商品种类、件数和金额，确认后才删除或进入确认订单页；清空购物车继续使用原有危险操作确认。
- 商品分类页增加商品搜索，复用首页搜索体验。
- 商品详情增加评分分布柱状图和物流三阶段时间线（已发货、运输中、已签收）。
- 新增订单→支付→激活→发货→收货→奖金全链路 E2E 测试，并覆盖库存还原、结算冷却期和 PV 校验（5/5）。
- 管理后台新增 Vitest 测试覆盖 Store、Router、Request、Login（27/27）。
- 商城前端回归测试 11/11，后台生产构建和商城生产构建均通过；后端全量测试 140/140 通过。
- 为订单奖金 E2E 测试使用独立 H2 数据库，避免测试定时任务污染其他测试类。

### 发布状态

- 已完成生产发布，线上版本标记为 `1.0.8`。
- 发布前后核心业务数据计数一致，未执行业务数据删除或改写；发布备份：`/opt/lingqimall/backups/full/20260804_125054`。
- 发布后验证：后端健康检查、管理后台与商城入口、未登录接口均正常，HTTP 自动跳转 HTTPS，Nginx、后端、MySQL、Redis 均为 active。

---

## 1.0.9（2026-08-04 17:02）

### 更新内容

- 余额页面四个功能入口调整为一行四列，布局更紧凑清晰。
- 余额转账改为独立页面，避免在余额首页直接混合填写；转账金额前后端统一限制为整数，小数输入会立即提示。
- 首次交易前的支付密码设置提示移动到余额信息顶部，安全设置、支付密码设置、转账提现和订单详情文案统一为“支付密码”。
- 团队业绩入口区分“尚未开通代理身份”和“代理权限未开放”，后台开放状态与前台提示保持一致。

### 构建与测试

- 商城回归测试：14/14 通过。
- 管理后台 Vitest：27/27 通过。
- `ShopWalletServiceTest`：5/5 通过。
- 商城、管理后台生产构建通过；后端生产打包通过。

### 发布与线上验证

- 线上版本标记：`1.0.9`；后端包哈希校验通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260804_165724`。
- 数据库核心计数发布前后均为 `4:2:0:0:1:50000.00:5:5:2`，未删除或改写业务数据。
- 发布后后端健康检查为 UP，商城与管理后台入口返回 200，HTTP 自动跳转 HTTPS；未登录管理接口和钱包接口返回 401，公网 actuator 返回 404；Nginx、后端、MySQL、Redis 均为 active。

---

---

## 1.0.11（2026-08-05 15:38）

### 更新内容

- 支付宝支付接入代码发布：服务端通过环境变量读取 APPID、应用私钥和支付宝公钥；异步通知先验签、校验应用与金额，仅 `TRADE_SUCCESS` 更新订单，并对重复回调保持幂等。
- 支付宝生产商户配置已通过服务器受保护环境文件启用，密钥未进入代码仓库；待后续完成真实小额订单、重复回调、退款和资金对账验证后，才能对外宣称正式支付稳定可用。
- 余额流水审计字段兼容迁移已执行并核验，支持变动前余额、变动后余额、管理员 ID 和管理员账号；迁移幂等，不删除或改写会员、订单、余额数据。
- 商品 PV 开关修复：关闭商品 PV 后，商品默认 PV 与 SKU PV 自动按 0 保存，订单 PV 快照为 0，不再参与 PV 汇总；销售价、成本价、库存和支付金额不受影响。
- PV 对外展示收口：PV 关闭时首页精选商品、商品详情和 SKU 接口统一返回 0，后台编辑页同步清理隐藏历史 PV，避免旧数据或旧页面继续显示 PV。
- 设置支付密码短信改为服务端固定业务类型的专用入口，避免旧前端传错类型导致“短信业务类型不支持”。
- 支付宝手机网站支付跳转修复：商城 CSP 原先仅允许本站表单提交，浏览器会拦截提交到支付宝官方网关，表现为点击提交后按钮结束但页面不跳转；现仅放行 `https://openapi.alipay.com`，不放开其它外站表单。

### 构建与测试

- 后端全量测试：145/145 通过。
- 本次修复后后端全量测试：146/146 通过。
- 管理后台 Vitest：27/27 通过；生产构建通过。
- 商城回归测试：18/18 通过；生产构建通过。
- 后端生产打包通过。

### 发布与线上验证

- 线上版本标记：`1.0.11`；后端包、前后台资源哈希校验通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260805_153635`。
- 数据库核心计数发布前后均为 `4:2:1:0:1:50000.00:5:5:2`，未删除或改写任何会员、订单或余额数据。
- 余额流水审计字段发布后 schema 校验为 `1:1:1`。
- 发布后后端健康检查为 UP，商城与管理后台入口返回 200，HTTP 自动跳转 HTTPS；未登录管理接口和钱包接口返回 401，公网 actuator 返回 404；Nginx、后端、MySQL、Redis 均为 active。
- 16:09 补发当前 1.0.11 前端资源：商城与管理后台前端目录安全替换，后端 JAR 未替换、数据库未执行删除操作；发布前全量备份 `/opt/lingqimall/backups/full/20260805_160821`，保留上一版前端目录用于回滚。
- 补发后核验：商城首页、验证码接口、商城入口、管理后台入口均返回 200；后端服务 active 且健康状态为 UP；Nginx 访问日志近期开启的 500 计数为 0；线上商城资源不再包含“收货地址已保存”提示。
- 16:55 配置支付宝生产密钥：服务器 `/etc/lingqimall/alipay.env` 权限为 `600`，后端支付配置接口返回 `alipayEnabled=true`，服务健康检查为 UP；修改前配置已备份为 `/opt/lingqimall/config/application.yml.before-alipay-20260805-1655`。
- 17:18～17:29 补发 PV 修复后的前后台与后端资源：发布前全量备份 `/opt/lingqimall/backups/full/20260805_171752`；后端 JAR 上传后校验哈希与本地构建一致，服务恢复 active/UP；商城与管理后台入口返回 200，未删除业务数据。
- 20:26 补发 PV 展示收口与支付密码短信修复：发布前全量备份 `/opt/lingqimall/backups/full/20260805_202639`；后端 JAR 哈希 `491bb6bba4852d8e029e8c00ff21ba7b161694ee1372eec6bd3dfde23a540abd` 与本地一致，服务 active/UP，商城与管理后台入口返回 200；线上首页 `showPv=0`、精选商品 PV 全部为 0，商品详情 SKU PV 全部为 0，未删除业务数据。
- 23:00 修复支付宝跳转：发布前备份 `/opt/lingqimall/backups/nginx/lingqimall.conf.before-alipay-csp-20260805`，仅替换 Nginx CSP 并 reload；带缓存刷新验证首页响应头的 `form-action` 已包含支付宝官方网关，商城回归测试 20/20 通过，未改动业务数据。
- 23:10 补发支付密码与 PV 前端修复：支付密码首次设置成功后增加“支付密码已保存”确认弹窗；后台商品保存前重新读取服务端 PV 开关并规避旧 GET 缓存，关闭 PV 时不再被历史 PV 拦截。商城 21/21、后台 27/27、商品发布后端 17/17 通过，商城/后台构建通过；发布前备份 `/opt/lingqimall/backups/frontend/20260805-payment-pv`，前后台入口返回 200，未改动业务数据。
- 23:15 补发支付密码成功弹窗样式，最终前台资源备份 `/opt/lingqimall/backups/frontend/20260805-payment-pv-final`；商城入口返回 200，后端未重启，业务数据未改动。
- 08:22 继续排查支付宝手机网站支付：后端确认 `QUICK_WAP_WAY` 表单生成成功，支付宝页面返回 `insufficient-isv-permissions`，根因是当前支付宝应用未签约/授权手机网站支付能力。代码补充权限错误识别和明确提示，支付密码首次设置成功弹窗统一为“支付密码已设置”，商城支付表单缺失时停止提交并提示重试；商城回归 21/21、后台 Vitest 27/27、后端全量 149/149、两端生产构建和后端编译通过。08:21 已安全替换线上前后台资源和后端包，备份 `/opt/lingqimall/backups/full/20260806_082124`，服务健康检查 UP，未修改或删除业务数据。

### 本版未修复 / 待验证

- 支付宝已完成生产配置，但尚未完成真实小额订单、重复回调、超时、退款和资金对账验证，暂不能对外说明为已完成正式支付验收。
- 支付宝手机网站支付仍需在开放平台为当前应用开通对应产品权限；开通后才可进行真实小额支付验收，代码无法替代支付宝侧签约授权。
- 商城基座清理及旧会员/余额数据删除本版未执行，待全量测试和清理方案确认后单独处理。

## 已补发变更（仍归属 1.0.11）

- 商城装修配置补齐：Banner 后台管理、首页模块排序与显隐、单个分类首页显隐、更多颜色配置、底部导航文案与显隐；前台已接入对应配置读取。
- 本地验证：后台 Vitest 27/27、商城回归 18/18、后端全量 145/145，后台与商城生产构建通过。
- 本次已于 2026-08-05 16:09 随 1.0.11 前端补发上线；后续仍需用户验收后台配置保存、前台刷新生效和手机底部导航等真实业务场景。

---

## 1.0.10（2026-08-04 18:11）

### 更新内容

- 商城首页与分类页恢复左上角品牌 Logo，支持后台配置的自定义 Logo；未配置时使用默认灵启品牌图标，并补齐浏览器标签页与移动端图标。
- 商品编辑“售后及服务”改为标签式保障选择与折叠式售后说明编辑，支持自定义保障弹窗和通用模板。
- 协议与规则页面增加用户服务协议、隐私政策、交易与售后规则默认模板，主体名称、客服电话和客服邮箱可自动替换。
- 协议与规则页面增加常见问题 FAQ 编辑、默认问答填充和覆盖前确认，奖金规则问答单独列出。
- 商城资料页增加营业执照前台展示/隐藏开关；商城经营资质页按开关展示营业执照。
- 数据库新增 `dms_tenant.show_business_license` 和 `dms_tenant.faqs` 字段，采用备份后幂等检查再迁移的方式上线。

### 构建与测试

- 商城回归测试：14/14 通过。
- 管理后台 Vitest：27/27 通过，生产构建通过。
- 后端全量测试：145 项通过，36 项按测试配置跳过；后端生产打包通过。
- 商城生产构建通过。

### 发布与线上验证

- 线上版本标记：`1.0.10`；后端包、前后台资源和数据库迁移文件哈希校验通过。
- 发布前全量备份：`/opt/lingqimall/backups/full/20260804_180902`。
- 数据库核心计数发布前后均为 `4:2:0:0:1:50000.00:5:5:2`，未删除或改写业务数据；新增租户展示字段迁移后 schema 校验为 `1:1`。
- 发布后后端健康检查为 UP，商城与管理后台入口返回 200，HTTP 自动跳转 HTTPS；未登录管理接口和钱包接口返回 401，公网 actuator 返回 404；Nginx、后端、MySQL、Redis 均为 active。

### 本版未修复 / 待验证

- 余额记录登录后的真实页面仍需使用管理员/会员账号实测；本次仅完成构建、接口安全状态和服务健康验证。
- 正式支付、ERP、真实短信注册、真机体验等全局待验证项继续保留。

## 当前仍未完成（全局）

1. 微信支付、支付宝支付尚未完成正式商户环境的端到端接入和真实资金验证，不能按“已上线支付”对外说明。
2. ERP 正式接口尚未对接；目前只有订单表格导出、发货表下载、物流导入和分批发货能力。
3. 线上运营主体、统一社会信用代码、客服电话、联系地址、第三方支付及短信服务商等协议内容尚未在后台配置。
4. 数据库仍有 2 个隔离的内部系统账户待彻底删除；当前奖金剩余金额和产品成本归集逻辑仍依赖它们，必须先完成归集、退款冲回和账务关联改造并回归测试。
5. Android 安装包未重新生成；iOS 包未构建（取决于最终采用远程网页壳还是内置前端资源方案）。
6. 服务器有系统安全更新待安装并重启，需另排维护窗口。
7. 低优先级代码优化（代码审查提出，尚未处理）：
   - 后台权限按 URL 前缀字符串匹配，建议改为注解/权限表驱动，降低新增接口漏配权限的风险；
   - 租户隔离通过改写 SQL 文本注入 `tenant_id`，建议改为显式查询过滤。

## 当前待验证（全局）

1. 使用真实有效邀请码和真实短信完成一次注册，确认短信到达、注册成功、自动登录及跳转 APP 下载页。
2. 正式会员在 iPhone、Android 真机验证购物车长名称、图片裁切、清空确认，以及“我的”页面全屏高度。
3. 微信/支付宝正式接入后验证：重复点击、超时回调、重复回调、退款、奖金快照、7 天结算及资金冲回。
4. ERP 接入后验证：一个订单多包裹、多个订单合并包裹、部分发货、导入失败行隔离及回传幂等。
5. 新增功能验证：非会员余额全链路展示；短信限次体验；取消会员资格/退款自动取消的真实业务场景。

## 已确认的长期安全基线

- 全站 HTTPS，HTTP 自动 301 跳转，HSTS 与 CSP 生效。
- MySQL、Redis 仅监听本机；后端健康接口不对公网开放。
- 敏感字段除 TLS 外使用一次性 RSA-OAEP-256 + AES-256-GCM 应用层加密，服务器拒绝未加密敏感载荷。
- 密码只保存不可逆哈希；管理后台会话失效、登录锁定、敏感操作再认证、余额流水与操作日志均已建立。

## 关联资料

- [版本发布总账](RELEASE_NOTES.md)
- [1.0.1 发版报告](release-20260803-v1.0.1.md)
- [1.0.2 发版报告](release-20260803-v1.0.2.md)
- [1.0.3 发版报告](release-20260803-v1.0.3.md)
- [旧版模块隔离说明](legacy-module-isolation-20260722.md)
- [生产环境优化建议](production-optimization-20260731.md)
