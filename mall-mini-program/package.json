{
  "name": "lingqi-mall-mini-program",
  "version": "1.0.136",
  "private": true,
  "description": "灵启商城微信小程序公共基座",
  "scripts": {
    "build": "npm run check",
    "check": "node scripts/sync-h5-rules.mjs --check && node scripts/check-project.mjs",
    "test": "node scripts/sync-h5-rules.mjs --check && node --test tests/*.test.mjs",
    "sync:h5-rules": "node scripts/sync-h5-rules.mjs"
  }
}
