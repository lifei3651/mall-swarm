[Service]
# Loaded by systemd as root; the referenced credential file must remain mode 0600.
# Direct Spring environment names bind even with an external spring.config.location.
EnvironmentFile=/etc/lingqimall/wechat-mini-program.env
