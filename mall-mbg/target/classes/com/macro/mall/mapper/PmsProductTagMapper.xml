<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.mapper.PmsProductTagMapper">
  <resultMap id="BaseResultMap" type="com.macro.mall.model.PmsProductTag">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="icon" jdbcType="VARCHAR" property="icon" />
    <result column="color" jdbcType="VARCHAR" property="color" />
    <result column="sort" jdbcType="INTEGER" property="sort" />
    <result column="status" jdbcType="INTEGER" property="status" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
  </resultMap>

  <select id="selectAll" resultMap="BaseResultMap">
    select id, name, icon, color, sort, status, create_time from pms_product_tag order by sort desc
  </select>

  <select id="selectByStatus" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select id, name, icon, color, sort, status, create_time from pms_product_tag where status = #{status} order by sort desc
  </select>

  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select id, name, icon, color, sort, status, create_time from pms_product_tag where id = #{id}
  </select>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from pms_product_tag where id = #{id}
  </delete>

  <insert id="insert" parameterType="com.macro.mall.model.PmsProductTag" useGeneratedKeys="true" keyProperty="id">
    insert into pms_product_tag (name, icon, color, sort, status)
    values (#{name}, #{icon}, #{color}, #{sort}, #{status})
  </insert>

  <insert id="insertSelective" parameterType="com.macro.mall.model.PmsProductTag" useGeneratedKeys="true" keyProperty="id">
    insert into pms_product_tag
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="name != null">name,</if>
      <if test="icon != null">icon,</if>
      <if test="color != null">color,</if>
      <if test="sort != null">sort,</if>
      <if test="status != null">status,</if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="name != null">#{name},</if>
      <if test="icon != null">#{icon},</if>
      <if test="color != null">#{color},</if>
      <if test="sort != null">#{sort},</if>
      <if test="status != null">#{status},</if>
    </trim>
  </insert>

  <update id="updateByPrimaryKeySelective">
    update pms_product_tag
    <set>
      <if test="name != null">name = #{name},</if>
      <if test="icon != null">icon = #{icon},</if>
      <if test="color != null">color = #{color},</if>
      <if test="sort != null">sort = #{sort},</if>
      <if test="status != null">status = #{status},</if>
    </set>
    where id = #{id}
  </update>

  <update id="updateByPrimaryKey">
    update pms_product_tag set
      name = #{name}, icon = #{icon}, color = #{color}, sort = #{sort}, status = #{status}
    where id = #{id}
  </update>
</mapper>
