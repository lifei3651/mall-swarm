# 1.0.164 售后退货地址补配阻断修复：本机验证记录

## 问题与根因

用户反馈：实体退货/换货申请在管理后台审核时提示“请先为该订单商品配置可用退货地址，再通过售后”；随后给商品补配退货地址，旧申请审核仍提示相同错误，退款流程无法继续。

源码核对：申请创建时仅把当时解析出的地址写入售后单快照；未配置时允许创建空快照。审核只检查该快照，不重新读取商品。商品后来补配地址不会改变旧售后单，因此持续阻断。新商品创建、商品管理表单和重新上架原先也未统一要求可用退货地址。

## 修复边界

- 仅待审核、审核通过的实体退货/换货申请，在历史快照为空时按该订单商品当前配置或所属商户默认退货地址重新解析，租户、商户、地址类型与启用状态均须匹配；混用不同仓库仍拒绝。与审核状态一同持久化；已有非空快照不覆盖。
- 新商品创建/发布时后端要求有效退货地址；商品后台仅列有效地址并在保存前校验。旧商品从下架重新上架也须补齐；旧在售商品不因本次代码更改被自动下架，尚需正式库只读扫描。
- 审核通过退货退款只进入“待寄回”；**不是立即原路退款**。顾客寄回、后台确认收货后才进入后续退款/账务链路；换货不进入退款链路。
- 无数据库迁移或批量修改。禁止直接改写历史售后地址快照、订单金额或退款账本。

## 本机验证与同类扫描

- 新增服务层回归：旧申请空地址→审核拒绝→补配地址→再次审核成功、地址持久化；将其他租户地址挂在商品上仍拒绝；新商品无地址不能发布、配置有效地址后可发布。
- 审核 `updateAuditScoped` 所有状态迁移共用同一 SQL；只在原快照为空时写入解析结果，拒绝/关闭和历史非空快照均不被改写。搜索申请、人工退款、商品创建/更新/发布/上架和后台地址选择入口，已覆盖同一缺口。
- `ShopFreightServiceTest` 37 项通过；后端全套 `./mvnw -pl mall-distribution -am test -q` 790 项通过、0 失败/错误；管理后台 `npm test` 218 项通过、生产构建通过；小程序 `npm run check` 通过。本机模拟数据不是正式库证据，完整跨端候选 C/T 仍须在冻结提交后重跑。

## C/T/S/W/R

| 阶段 | 状态 |
|---|---|
| C | 源码已修改；统一候选身份尚未冻结 |
| T | 本机回归与后台构建通过；完整候选封包、私有留档和准入未做 |
| S | 1.0.164 未部署；正式主机本地 `VERSION` 为 `1.0.158`、后端 health UP。正式库只读核对结果见下节，不等于新代码已发布 |
| W | 未上传 1.0.164；微信体验版/真机版本未变更 |
| R | 已取得受影响订单号并完成正式库只读定位；未重新申请、审核、寄回或做真实退款/真机复验 |

## 2026-09-24 正式库只读核对

用户提供的订单号仅在本次会话展示；仓库内以 SHA-256 `47ab78e8e746c19e9055f22cd732e7979d5898bce8ff4de22eb860e19e38501c` 标识，不保存收件信息。通过唯一正式主机 `VM-4-6-rockylinux` 的本机 MySQL socket 执行只读 SELECT，未修改订单、售后或地址：

- 订单已支付且已发货（状态 2），当前未标记签收。原售后单为实体退货退款（类型 2），创建于 2026-09-23 17:21:57，状态 2“已拒绝”，审核时间 17:32:59；原退货地址 ID 与快照均为空。
- 该商品的退货地址簿记录创建于 17:25:51，商品在 17:26:17 绑定该地址，均晚于售后申请创建。当前地址为同租户、平台自有、退货类型、启用、默认，联系人、电话及行政区/详细地址字段均非空。这与“先申请、后补地址、旧快照不更新”的根因一致。
- 该订单当前没有其他进行中的售后、没有财务退款记录、没有被进行中售后占用的商品数量。正式租户售后入口配置为“签收后 7 天”，当前订单未标记签收；按当前代码的窗口判定尚无已过期证据。这只能说明再次申请的已查条件满足，**不等于已实际重新申请或保证前端每个入口均通过**。
- 已拒绝申请不能直接重新审核，不得把历史状态强行改回待审核。若顾客确已收到货并可寄回，可由顾客重新提交退货退款申请以取得当前地址快照；若尚未收到货，不应假装有货可退，应按物流异常退款流程处理，且该新流程的 1.0.164 源码尚未上线。两种路径都没有在本轮执行。
- 同类只读扫描：当前“待审核且地址快照为空”的实体售后为 0；历史空快照仅有本单已拒绝记录 1 条。另有 2 个在售商品（内部 ID 1、4）未绑定有效退货地址，其中 ID 4 已有 2 张历史订单。未自动指定默认仓库、修改商品或下架；需商品负责人逐个确认正确仓库并配置，或按业务决定下架，再复扫。

## 关闭条件

订单与售后、原快照及当前地址的正式库只读核对和同类计数扫描已完成；2 个在售无有效地址商品仍须由业务负责人确认仓库并配置或下架，随后复扫。新候选完成独立封包/留档及授权的 S/W 后，再用原单或受控测试单完成审核、退货寄回、确认收货、渠道退款回执及订单/奖金/资金对账和真机 R。未取得上述证据时 P0-13 不关闭。
